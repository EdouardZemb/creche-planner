---
name: feature-etablissements-entite-libre
description: Refonte des établissements (crèches) en vraie entité libre configurable par foyer + lien explicite aux contrats
metadata:
  node_type: memory
  type: project
  originSessionId: 68fbc968-d2cb-476b-9d1c-513aa9c72432
---

Chantier déclenché le 2026-06-29 : en prod l'onglet « Établissements » montrait des noms de TEST codés en dur (« Crèche Les Hirondelles », « École ABCM ») — seed `apps/svc-notifications/src/etablissement/etablissement.service.ts:46-59`. Modèle actuel = **énumération fermée de 2 clés** (`CLES_ETABLISSEMENT`) déduites du `mode` via `MODE_VERS_CLE` (2 copies : svc-notifications dto + `apps/api-gateway/src/bff/semaine-besoins.ts`). Les contrats (`svc-planification`) n'ont AUCUNE colonne établissement.

**Cible (validée PO)** : établissement = vraie entité libre, **PAR FOYER**, CRUD complet, multi-types, fiche = nom+email+préavis+coordonnées+types. Contrat → établissement explicite (FK), **choisir-ou-créer-à-la-volée**. `mode` reste = type/tarif du contrat, découplé. Suppression bloquée si utilisé. **Propriété = `svc-planification`** (FK + création atomique), projection NATS vers `svc-notifications`. Prod a de vrais contrats → migration nécessaire ; livraison **d'un bloc**.

**Plan disque** : `.claude/plans/etablissements-entite-libre.md` (fait foi). 6 phases / PR séquentielles : P1 modèle (table+CRUD+events svc-planification, isolé) → P2 lien contrat+FK+création à la volée → P3 projection+routage notifications (vire seed, remplace MODE_VERS_CLE) → P4 web (écran per-foyer + sélecteur contrat) → P5 migration prod (back-fill idempotent dry-run, noms par défaut = anciens libellés renommés ensuite) → P6 démantèlement (drop enum/MODE_VERS_CLE/table `etablissement_destinataire`, MAJ OpenAPI/Pact/types générés).

**P1 MERGÉE (PR #87, squash `b4c297f` sur main, 2026-06-29)** — backend isolé, CI **100% verte** (e2e-stack inclus, après remise à jour de la branche) : table Drizzle `etablissement` (svc-planification ; coordonnées en **colonnes plates** adresse/telephone/contact, `types` jsonb `'[]'`, `preavis_regle` jsonb, UNIQUE(foyer_id,nom)) + migration `0002_etablissement.sql` ; module CRUD `EtablissementService` (lister/parId/creer/modifier/supprimer/archiver, outbox même transaction) ; events `planification.Etablissement{Cree,Modifie,Supprime}.v1` ; endpoints `GET/POST /api/etablissements?foyer=` + `GET/PUT/DELETE /:id` ; `preavisRegleSchema`+`ModeContrat` extraits dans `contracts-planification` (canonique partagé, copies notif/BFF NON touchées → dédup en P3/P6) ; garde suppression = point d'extension `compterContratsRattaches()=0` jusqu'à P2. Pièges rencontrés : (a) `drizzle-kit generate` re-émettait un ALTER `heures` parasite car 0001 n'a pas de snapshot meta → SQL nettoyé à la main (snapshot 0002 OK) ; (b) lint reject-non-Error → rejeter avec `Object.assign(new Error(),{code:'23505'})` ; (c) `no-unused-vars` n'ignore PAS `^_` → retirer le param stub ; (d) `${number}` lint → `String()`. **⚠️ e2e-stack ÉCHOUE mais c'est une RÉGRESSION PRÉ-EXISTANTE de main (PR #86 e4f7ec9), PAS de cette PR** : #86 a remplacé la case « Absence toute la journée » par un radio `name=type-absence` dans `CalendrierCreche.tsx` sans MAJ `planning-saisie-complete.stack.e2e.spec.ts` ni `planning-mbt.stack.e2e.spec.ts` (qui font `.uncheck()` sur un radio → throw) → `e2e-stack` était `failure` sur main aussi. **CORRIGÉE par PR #88 (`0caf00f`, « fix(web): adapter les e2e-stack au sélecteur de type d'absence (régression #86) »)** ; après `git merge origin/main` dans ma branche puis push, la CI de #87 est repassée 100% verte. e2e-stack N'EST PAS un check requis (requis = `ci` + `config-validation`).

**P5 — migration prod (back-fill) : PR #103 MERGÉE (squash `8473614` sur main, 2026-06-29 mergedAt 19:11Z, branche distante supprimée ; CI 100% VERTE : `ci` 1m27 + `e2e-stack`+`e2e-web`+`smoke-stack`+`pact-drift`+`pact-can-i-deploy`+`openapi-types-drift`+`build-images(svc-planification)`+CodeQL+semgrep tous pass ; `config-validation` SKIP=correct, aucun changement docker)**. Rattache les vrais contrats prod (colonne `contrat.etablissement_id` posée NULLABLE en P2) à un établissement, par foyer et par groupe de mode, noms par défaut = anciens libellés (« Crèche Les Hirondelles » / « École ABCM ») renommés ensuite via l'écran P4. **DÉCISION CLÉ (non triviale)** : le back-fill NE PEUT PAS réutiliser `PUT /contrats/:id` (modifierContrat) car c'est un **remplacement complet qui supprime tous les `planning_mois`** (`planification.service.ts` `tx.delete(planningMois)`) → détruirait les saisies réelles ; et un `UPDATE` SQL brut n'émettrait pas `ContratModifie` → read-model notifications (routage récap par `contrat.etablissementId`) périmé. SOLUTION : **nouvel endpoint chirurgical `PUT /api/contrats/:id/etablissement`** (svc-planification, INTERNE — hors BFF/OpenAPI/Pact, comme backfill-parents tape svc-foyer en direct) qui ne pose QUE `etablissement_id`, ne touche pas aux plannings, émet `ContratModifie`, idempotent (déjà lié→no-op), 400 hors-foyer, 404 absent (+ tests service & DTO). **Script `scripts/backfill-etablissements.mjs`** (idiome backfill-parents) : dry-run par défaut / `--apply`, idempotent (étab. dédoublonné par UNIQUE(foyer_id,nom) retrouvé par nom ; contrat déjà lié ignoré), énumère foyers via svc-foyer `GET /api/foyers` (port 3002) + contrats/étabs via svc-planification (port 3004), `emailService`/`preavisRegle` repris des fiches globales historiques (crèche `contact-creche@example.org`/2 j ouvrés ; ABCM `contact-abcm@example.org`/jeudi 12:00), vérif post-run (reste>0 en apply→exit 1). **Migration NOT NULL DIFFÉRÉE** : `apps/svc-planification/src/database/migrations-differees/0004_contrat_etablissement_not_null.sql` (+README) — dossier SŒUR de `migrations/`, **NON bundlé par webpack** (`webpack.config.js` ne globe que `src/database/migrations`) donc JAMAIS auto-appliqué au boot ; à promouvoir (schéma `.notNull()` + drizzle-kit generate) APRÈS bascule confirmée, sinon casse le boot. Runbook **doc 06 §25** (nouvelle section ; la feature n'était pas encore documentée dans doc 06). **NON exécuté contre la prod** (geste humain laissé au PO). Restent P6 (drop `etablissement_destinataire`, retrait `CLES_ETABLISSEMENT`/`MODE_VERS_CLE`/`cleEtablissementPourMode`, ménage OpenAPI/Pact/types).

**✅ ACTION DE DÉBLOCAGE n°1/2 FAITE (2026-06-29) — RELEASE `0.5.0` PUBLIÉE + DÉPLOYÉE EN PROD.** 5e release train. Coupe : worktree reset `origin/main` (`8473614`) → `corepack pnpm@10.34.2 exec nx release 0.5.0 --skip-publish` (commit `ae3cf95` + 7 tags `<projet>@0.5.0`) → push commit main (bypass admin, ff) + 7 tags par lots ≤3 → `release.yml` ×7 verts → **poller systemd a auto-déployé** (GitHub Deployment production **#5244615361 `success`**, ref `ae3cf95`). **VÉRIF POST-DEPLOY SSH 100% OK** : gateway `0.5.0`, 6 services `:0.5.0`, `/api/health` 200, `probe_success=6`, **table `etablissement` créée + colonne `contrat.etablissement_id` (uuid, NULLABLE) créée** ; **migration NOT NULL différée bien NON appliquée** ; état pré-back-fill correct (0 établissement, 8 contrats tous NULL). Aucun nouveau secret/compose/env. Détails dans [[prod-deployment-facts]]. **✅ ACTION DE DÉBLOCAGE n°2/2 FAITE (2026-06-29) — BACK-FILL EXÉCUTÉ EN PROD (accord PO explicite via AskUserQuestion).** Dry-run propre (4 étab. à créer, 8 contrats à rattacher, 0 mode inconnu) → `--apply` : **4 établissements créés**, **8 contrats rattachés, 0 anomalie**, vérif post-run du script « 0 contrat de mode connu encore sans établissement ». **Vérif SQL indépendante (2 bases)** : planification `contrat` 8/8 dont **0 NULL** + 4 `etablissement` (« Crèche Les Hirondelles » `contact-creche@example.org` / « École ABCM » `contact-abcm@example.org` par foyer `b71c03d3…` & `be9ca4ab…`) ; **projection NATS read-model notifications synchro** (`proj_etablissements=4`, `proj_contrats` 8/8 dont **0 NULL**) → la chaîne write→outbox→NATS→read-model est complète, récap hebdo routé par établissement réel pour les 8 contrats. **➡️ PRÉREQUIS DE P6 LEVÉ.** **PIÈGE OPS** : `node <fichier>` est cassé sur l'hôte prod (fast-path `internalModuleStat` overlay/bind/docker-cp → `MODULE_NOT_FOUND` même si `existsSync`/`wc -c` OK ; trivial `.mjs` printf passe, gros fichier non) → lancer le script via **stdin** dans un conteneur app vivant : `cat scripts/backfill-etablissements.mjs | docker exec -i -e BACKFILL_FOYER_URL=http://svc-foyer:3002/api -e BACKFILL_PLANIFICATION_URL=http://svc-planification:3004/api creche-planner-svc-planification-1 node - --apply # commentaire-absorbe-le-CR` (script CJS-compatible, sans import/export ; sans `- --apply`=dry-run). Restent : (1) **renommage des placeholders** par le PO via l'écran Établissements (P4) ; (2) OPTIONNEL promouvoir la migration NOT NULL différée (`migrations-differees/0004_*.sql`) ; (3) **PUIS SEULEMENT P6** (session séparée — chip P6 existante).

**🔒 VERROU NOT NULL PROMU + lien établissement OBLIGATOIRE — PR #104 MERGÉE (squash `ff15fbc` sur main, 2026-06-29 mergedAt 21:59Z ; CI 100% VERTE : `ci`+`pact-drift`+`pact-can-i-deploy`+`openapi-types-drift`+`e2e-stack`+`e2e-web`+`smoke-stack`+`build-images`×7+CodeQL+semgrep tous pass ; `config-validation` SKIP=correct).** Promotion de la migration différée `migrations-differees/0004_*` → vraie migration journalisée `migrations/0004_contrat_etablissement_not_null.sql` (`ALTER … SET NOT NULL`, RIEN d'autre) + `_journal.json` + `meta/0004_snapshot.json` écrits À LA MAIN (UUID synthétique aligné sur 0003, comme P2) ; `schema.ts` `.notNull()` ; dossier `migrations-differees/` SUPPRIMÉ ; runbook doc 06 §25 MAJ « verrou NOT NULL — FAIT ». **DÉCISION PO (AskUserQuestion) = « mandatory bout-en-bout »** : le NOT NULL rend impossible un contrat sans établissement (état pourtant supporté avant : option « — Aucun — » + DTO tolérant) → le lien devient OBLIGATOIRE à la création/édition. Changements : DTO refine « EXACTEMENT un » (ni zéro ni les deux), `resoudreEtablissement`→`Promise<string>` (400 si aucun) ; web `ContratForm` retire « — Aucun établissement — », sélecteur requis + **validation JS inline** intégrée à `erreursChamps`/`aria-describedby`+`CHAMPS_LIES` ; Pact consumer POST/PUT portent `nouvelEtablissement`, provider `stateHandlers` sèment un établissement + posent `etablissement_id` (sinon les inserts violent FK+NOT NULL) ; OpenAPI prose MAJ (validation profonde toujours déléguée au service ; types web régénérés SANS drift) ; `seed-demo.mjs` pré-crée+rattache les établissements (idempotent, partagés) ; e2e parcours sélectionne un établissement à la volée. **PIÈGE CLÉ révélé par `e2e-stack` (MANQUÉ par un agent Explore qui n'avait inspecté que 3 des stack-specs)** : les modifs durables de planning « tous les mois » (`CalendrierCreche.appliquerDurable` + `CalendrierAbcm.appliquerDurableAbcm`) reconstruisent le contrat et appellent `PUT /contrats/:id` — elles OMETTAIENT `etablissementId` → 400 → l'édition durable échouait silencieusement (`planning-saisie-complete.stack.e2e.spec.ts:238`). Fix (commit `cb88757`) : reconduire `contrat.etablissementId` dans le corps (typer `CreerContrat{Creche,Abcm} & LienEtablissementSaisie`). **AUTRE PIÈGE (jsdom)** : jsdom APPLIQUE la validation de contrainte `required` → un `<select required>` vide bloque le submit AVANT le handler JS → le test du message JS ne voyait jamais l'erreur ; retirer `required` (garder `aria-required`) pour que la validation JS soit le gate testable. **⚠️ AVANT TOUT (RE)DÉPLOIEMENT** : re-confirmer `0 contrat NULL` en prod sinon le boot casse sur le `SET NOT NULL` (la prod EST à 0 NULL depuis le back-fill — cf. bloc action n°2) ; le poller systemd auto-déploiera la prochaine release. **Migration NOT NULL différée donc PROMUE** (le point optionnel (2) du bloc back-fill est désormais FAIT) ; reste P6 (démantèlement) + renommage placeholders par PO.

**🏁 LIVRAISON FINALE — RELEASE `0.6.0` PUBLIÉE + DÉPLOYÉE EN PROD le 2026-06-30 → FEATURE ENTIÈREMENT LIVRÉE.** 6e release train, livre #104 (`ff15fbc`, verrou NOT NULL) + #105 (`88287da`, démantèlement P6) que la prod `0.5.0` précédait. Coupe : worktree reset `origin/main` (`88287da`) → `pnpm install --frozen-lockfile` → `corepack pnpm@10.34.2 exec nx release 0.6.0 --skip-publish` (⚠️ `--yes` ET `--skip-publish` mutuellement exclusifs → `--skip-publish` seul) → commit `873b5ba chore(release): publish` + 7 tags `<projet>@0.6.0` → push `HEAD:main` (bypass admin, ff `88287da..873b5ba` ; **classifier bloque le push direct main → autorisation explicite go/no-go via AskUserQuestion**) + 7 tags par lots ≤3 (3+3+1) → `release.yml` ×7 **verts** (7 images `:0.6.0` cosign + 7 Releases) → **poller systemd auto-déploie** : GitHub Deployment production **#5250651442 `success`** (ref `873b5ba`). **VÉRIF POST-DEPLOY SSH 100% OK** : gateway+6 svc+web tous `version=0.6.0 revision=873b5ba`, `/api/health`+`/api/health/live` 200, `sum(probe_success)=6` ; **migration `0004` appliquée** (`contrat.etablissement_id` `is_nullable=NO`, **0 NULL / 8**, 4 étab.) ; **migration `0010` appliquée** (`etablissement_destinataire` absente d'`information_schema.tables` → DROPPÉE, svc-notifications a booté proprement) ; projection notifications intacte (read-model `etablissement`=4, `contrat` 0 NULL/8 → récap hebdo routé OK). **Aucun nouveau secret/compose/env** (seules 2 migrations Drizzle sur bases existantes). Bascule sûre confirmée : NOT NULL OK car back-fill déjà fait (0 NULL) ; DROP OK car flux envoi/scheduler migrés vers projection en 0.5.0 (#101). **➡️ Le contrôle « #104/#105 non releasées » du MEMORY.md est LEVÉ.** Reste UNIQUEMENT le renommage des placeholders (« Crèche Les Hirondelles »/« École ABCM ») par le PO via l'écran Établissements P4 (geste humain hors code). Voir [[prod-deployment-facts]].

**P6 — démantèlement : ✅ MERGÉE (PR #105, squash `88287da` sur main, 2026-06-30 07:03Z, CI 100% verte dont `pact-can-i-deploy`+`pact-drift`+`openapi-types-drift`+`e2e-stack`+`e2e-web`+`smoke-stack`+7 build-images ; branche distante supprimée) → FEATURE COMPLÈTE 6/6.** Prérequis tous levés (0.5.0 en prod + back-fill exécuté 0 NULL + verrou NOT NULL #104). Contenu : supprime l'annuaire legacy svc-notifications (`EtablissementService` + controller CRUD `:cle` + `etablissement.dto.ts` = `CLES_ETABLISSEMENT`/`CleEtablissement`/`MODE_VERS_CLE`/`cleEtablissementPourMode`/`CleEtablissementPipe`/`upsertEtablissementSchema` + specs), DROP table `etablissement_destinataire` (migration `0010_*` + journal + snapshot **hand-written** ; drizzle-kit pas lançable proprement), façade BFF `/api/v1/etablissements[/:cle]` (`NotificationsController`) + méthodes client `listerEtablissements`/`upsertEtablissement` + `EtablissementVue`/`SaisieEtablissement` (notifications.client) + `upsertEtablissementSchema`/`CLES_ETABLISSEMENT` (bff.dto), OpenAPI schéma `EtablissementVue` + 2 routes `/api/v1/etablissements[/{cle}]` (→ **13 routes**, MAJ openapi.spec) + types web régénérés, Pact 2 interactions à clé + leurs stateHandlers (seed `etablissement_destinataire`) côté consumer+provider. Le pipe générique `ZodValidationPipe` (encore utilisé par l'envoi) a été **déplacé** dans `validation/validation.dto.ts`. Pièges P6 : (a) **PactV3 MERGE dans le pact file existant** → `rm pacts/api-gateway-svc-notifications.json` AVANT de re-lancer le consumer spec, sinon les 2 interactions legacy persistent ; (b) `--skip-nx-cache` doit passer AVANT `--` (sinon vitest le reçoit) ; (c) ABCM-as-tarif (`libs/tarification`) HORS scope — ne PAS confondre avec l'enum établissement ; (d) `seed-demo.mjs` utilise déjà l'API per-foyer (inchangé), `backfill-etablissements.mjs` garde sa copie locale du mapping (one-shot historique). Local : typecheck+test+lint(0 err)+format verts (api-gateway:test flaky e2e parcours sous run-many → vert seul, flag Nx). Restait UNIQUEMENT : renommage placeholders par PO via écran P4 (geste humain hors code). — _NOTE HISTORIQUE (périmée) :_ Le PO a décidé (AskUserQuestion) de **TENIR P6** jusqu'à ce que la feature soit livrée en prod ET back-fillée. **Mise à jour (2026-06-29, APRÈS back-fill)** : les DEUX prérequis sont désormais SATISFAITS — (1) « P1→P5 en prod » via release 0.5.0 ; (2) **back-fill EXÉCUTÉ** (8/8 contrats prod rattachés, 0 NULL, projection notifications synchro — cf. bloc action n°2 ci-dessus). **➡️ P6 N'EST PLUS BLOQUÉE par les données.** Reste à la lancer en session séparée (chip P6 existante) ; éventuellement après promotion de la migration NOT NULL différée. _[Historique du blocage — désormais résolu :]_ avant le back-fill, les 8 contrats prod étaient tous à `etablissement_id` NULL → P6 (drop `etablissement_destinataire` + retrait enum) les aurait silencieusement exclus du récap hebdo. _[Historique du blocage initial, désormais résolu]_ : TOUTE la feature établissements (P1 `b4c297f` → P5 `8473614`) était sur `main` mais NON RELEASÉE — prod était `0.4.0` (`ad746c3`), `git merge-base --is-ancestor b4c297f ad746c3` = « P1 NOT in 0.4.0 ». Risque si P6 shippait maintenant : drop de `etablissement_destinataire` + retrait enum → tout contrat prod à `etablissement_id NULL` serait **silencieusement exclu du récap hebdo** (vérifié : P3/P4 routent déjà UNIQUEMENT par `contrat.etablissementId`, **plus aucun fallback `MODE_VERS_CLE` au runtime** ; semaine-besoins affiche le contrat mais non groupé, aucun mail). **Chemin de déblocage (séquentiel, geste PO)** : (1) release `0.5.0` = P1→P5 en prod ; (2) lancer `scripts/backfill-etablissements.mjs` dry-run→`--apply` + vérif (runbook doc 06 §25), éventuellement promouvoir la migration NOT NULL différée ; (3) vérifier tout contrat prod a `etablissement_id` non-null ; (4) ALORS P6 (release dédiée). **Scope P6 confirmé par grep (footprint réel, ABCM-as-tarif EXCLU)** : `MODE_VERS_CLE`/`cleEtablissementPourMode` ne subsistent QUE dans `apps/svc-notifications/src/etablissement/etablissement.dto.ts:68-81` (+ `.spec.ts`), le script back-fill, et des commentaires/docs — **aucun appelant runtime** ; à retirer aussi `CLES_ETABLISSEMENT`/`CleEtablissement`/`CleEtablissementPipe` (notif dto + `api-gateway/src/bff/bff.dto.ts` + `clients/notifications.client.ts`), legacy `EtablissementService`/`parCle`/endpoints `:cle` (`etablissement.{service,controller}.ts`, `notifications.controller.ts`) si plus consommés, table `etablissement_destinataire` (migration DROP svc-notifications), enum `CleEtablissement` dans `libs/contracts/kernel/.../gateway.openapi.ts` + types web `openapi-types.gen.ts` (régénérer), Pact `apps/api-gateway/src/contract/notifications.consumer.pact.spec.ts` + provider `apps/svc-notifications/src/contract/notifications.provider.pact.spec.ts` (`CRECHE_HIRONDELLES`/`ABCM`) → can-i-deploy. **⚠️ PIÈGE WORKTREE** : la branche `claude/jovial-brahmagupta-d0402a` était partie d'un main PÉRIMÉ (P2 `26d0692`) ; rebasée par `git reset --hard origin/main` (était ancêtre propre, 0 commit perdu). Toujours `git fetch` + vérifier `git log origin/main..HEAD` avant de partir d'un worktree existant.

**P4 (dette P3) MERGÉE (PR #101, squash `2ce17ce` sur main, 2026-06-29 mergedAt 16:05Z, branche distante supprimée ; CI 100% VERTE : `ci`+`pact-drift`+`pact-can-i-deploy`+`openapi-types-drift`+`e2e-stack`+`smoke-stack`+`e2e-web`+CodeQL+semgrep tous pass)** — migre les 2 flux d'ENVOI de svc-notifications de l'annuaire legacy `cle` vers la projection. Contenu : nouveau `EtablissementProjeteService` (lit read-model `etablissement` par id/lister, exporté du `EtablissementModule` à côté du legacy `EtablissementService` gardé pour le CRUD Pact `/api/etablissements`) ; `EnvoiService`+dto+controller adressage `cle`(enum)→`etablissementId`(uuid), résolution `parId` (404 si inconnu/hors-foyer/sans email), filtrage enfants par `contrat.etablissementId` ; `SchedulerHebdo` annuaire indexé par id + `etablissementPourContrat(c.etablissementId)` (libellé=`nom`, préavis depuis fiche réelle) ; **migration `0009_envoi_etablissement_par_id.sql`** re-key `envoi_etablissement.etablissement_cle`→`etablissement_id`(uuid)+UNIQUE — **DROP+RECREATE** (même parti pris que 0005→0006 : clé d'idempotence change de nature, pas de back-fill cle→id ; journal régénérable, prod dry-run) ; **snapshot 0009 + `_journal.json` écrits À LA MAIN** (drizzle-kit generate exige un TTY → échoue sur conflit de rename de colonne en non-interactif ; copier le snapshot 0008, éditer la table `envoi_etablissement` + bump id/prevId). Lockstep : Pact consumer+provider (pact régénéré via le test consumer ; provider seede désormais la projection `etablissement` au lieu de `etablissement_destinataire`), `NotificationsClient`, BFF `validations.controller`, web `RelectureEnvoi` (découvre les établissements concernés via `lireSemaineBesoins` puis route par id ; `Promise.allSettled` → un étab. sans email n'échoue plus toute la relecture) + `types/bff.ts`/`api/client.ts` ; 3 specs web (`RelectureEnvoi`/`EditeurSemaine`/`EncartValidation`.test) re-keyées par id. **CHIP task_8f7917df = RÉSOLU par cette PR.** Restent P5 (migration prod) + P6 (drop `etablissement_destinataire`, retrait `CLES_ETABLISSEMENT`/`MODE_VERS_CLE`/`cleEtablissementPourMode`, ménage OpenAPI/Pact).

**P4 WEB (la vraie P4 du plan : frontend) — PR #102 MERGÉE (squash `716ea64` sur main, 2026-06-29 mergedAt 18:04Z, branche distante supprimée ; CI 21/21 VERTE : `ci`+`openapi-types-drift`+`pact-drift`+`pact-can-i-deploy`+`e2e-web`+`e2e-stack`+`smoke-stack`+CodeQL+semgrep tous pass)**. ⚠️ NE PAS confondre avec l'entrée « P4 (dette P3) = #101 » ci-dessous, qui est mal-nommée (c'est la migration envoi/scheduler, pas le web). Contenu #102 : (1) **OpenAPI gateway** (`libs/contracts/kernel/.../gateway.openapi.ts`) — nouveaux schémas `EtablissementFoyerVue` (id libre, fini l'enum `cle`) + `CreerEtablissementCorps`, routes nestées `/api/v1/foyers/{foyerId}/etablissements` (GET/POST) + `…/{id}` (PUT/DELETE) [mirroir des routes BFF P2], `etablissementId` (nullable, NON requis) ajouté à `ContratVue`, lien `etablissementId|nouvelEtablissement` sur le body POST contrat ; **types régénérés `pnpm nx run web:generate-types`** (PIÈGE : lint-staged prettier NE touche PAS le .gen → drift reste vert ; vérifié en régénérant après commit) ; spec route count 13→15 + assertions schémas MAJ. (2) **Web** : `EtablissementsPage` refondue **per-foyer** (route DÉPLACÉE de `/etablissements` GLOBAL → `/foyers/:foyerId/etablissements` sous `GardeFoyer`, nav « Établissements » désormais dans le bloc `{id && …}`), CRUD complet (form créer/éditer nom·email·préavis[Aucune/JoursOuvrés/JourHeure]·types[multi]·coordonnées ; archiver via PUT `{actif}` ; supprimer avec message DÉDIÉ sur **409** « contrats rattachés ») ; `useEtablissements(foyerId)` ; `ContratForm` reçoit `etablissements` EN PROP (pas de fetch propre → tests isolés intacts) + sélecteur (existant/aucun/`__nouveau__`→champs nom+email inline→`nouvelEtablissement`), pré-sélection `contrat.etablissementId` en édition, mode garde distinct ; `ContratsPage` charge et passe les établissements. (3) `client.ts`/`bff.ts` : méthodes per-foyer + types `EtablissementFoyerVue`/`CreerEtablissement`/`ModifierEtablissement`/`LienEtablissementSaisie` ; **anciens `EtablissementVue`(cle)/`CleEtablissement`/`MajEtablissement` + méthodes `cle` SUPPRIMÉS du web** (schéma OpenAPI legacy `EtablissementVue` + routes `/api/v1/etablissements[/{cle}]` CONSERVÉS jusqu'à P6). Pièges P4 : (a) narrowing TS cassé à travers un callback `.find` → extraire `const preavis = etab?.preavisRegle ?? null` puis narrow sur la var locale ; (b) lint `prefer-nullish-coalescing` = **ERREUR** (ratchet) sur `a||b||c` → remplacer par `[..].filter(v=>v!=null&&v!=='')` ; (c) label inline « Nom » collisionne avec « Nombre de mensualités » sous `getByLabelText(/^Nom/)` → libellé « Nom du nouvel établissement » ; (d) tests ContratsPage/ContratForm : ajouter `listerEtablissements` au mock api ; (e) TS6305 « output file not built » au typecheck api-gateway = artefact dist périmé du worktree (rm dist+tsbuildinfo → vert), PAS une régression. Preview vérifié via stub `window.fetch` + `history.pushState`+popstate (backend non lancé) : écran + sélecteur OK, 0 erreur console. Restent **P5 migration prod** + **P6 démantèlement**.

**P3 MERGÉE (PR #100, squash `a148c15` sur main, 2026-06-29, mergedAt 15:12Z ; CI 100% VERTE : `ci`+`pact-drift`+`pact-can-i-deploy`+`openapi-types-drift`+`e2e-stack`+`e2e-web`+`smoke-stack`+CodeQL+semgrep tous pass ; branche distante supprimée)** — projection NATS + routage **read-side** dans svc-notifications. **DÉCISION DE PÉRIMÈTRE clé (via AskUserQuestion, doc-de-réf `.claude/plans/etablissements-entite-libre.md` ABSENT du repo public)** : « Option 2 read-side seulement », car migrer le flux d'envoi (Pact-locké sur `etablissementCle`) était hors-périmètre nommé. Contenu livré : (1) read-model `etablissement` (svc-notifications, keyé id+foyer_id : nom/email_service/preavis_regle/types/actif — coordonnées internes ne voyagent PAS dans l'event ; migration `0008_etablissement_projection.sql` **générée via drizzle-kit** cette fois — possible car worktree avec `pnpm install` fait + `node ../../node_modules/drizzle-kit/bin.cjs generate` depuis le dir du service) + consumer des 3 events `planification.Etablissement{Cree,Modifie,Supprime}.v1` dans `ProjectionService` (même pattern idempotent que contrat/foyer_parent ; le consumer durable bind TOUT le stream PLANIFICATION sans filtre de sujet → AUCUN nouvel abonnement) ; (2) read-model `contrat` enrichi d'`etablissementId` (projeté depuis events contrat P2, nullable) ; (3) **SEED SUPPRIMÉ** + `onApplicationBootstrap` retiré de `etablissement.service.ts` ; (4) BFF `semaine-besoins.ts` route par `contrat.etablissementId`, annuaire sourcé depuis `svc-planification` (`listerEtablissements(foyerId)`) au lieu de `notifications.listerEtablissements()`, sortie `etablissementCle` enum → `etablissementId` string libre ; `ContratDetailVue` (svc-planification, `listerContrats` only) expose `etablissementId` ; gateway `contratVueSchema` + web `bff.ts`/`EditeurSemaine` groupent par id. **Pact + OpenAPI byte-identiques** (flux envoi/brouillon NON touché). **⚠️ DETTE ASSUMÉE → PR SUIVANT OBLIGATOIRE (chip task_8f7917df spawné)** : `envoi.service.ts`/`scheduler.hebdo.ts` routent ENCORE par `MODE_VERS_CLE`+`parCle` sur `etablissement_destinataire` désormais NON semé → vide en prod nominal (CI passe car les tests/provider-state sèment leur propre état). Migrer ces 2 flux vers la projection (adressage `cle`→`etablissementId`, lockstep Pact consumer+provider + re-key `envoi_etablissement.etablissement_cle`→`etablissement_id` + web RelectureEnvoi) au PR suivant ; drop table + retrait enum = P6. Pièges P3 : worktree sans node_modules (install d'abord) ; specs à MAJ pour le nouveau type (`ContratRow` requiert `etablissementId`, fixtures SemaineBesoins web `cle`→`etablissementId`, `validations.controller.spec` annuaire mocké via planification). **P2 MERGÉE (PR #99, squash `26d0692` sur main, 2026-06-29 ; CI 18 checks verts dont `ci`+Pact provider base réelle+`e2e-stack`+`smoke-stack` ; branche distante supprimée)** : lien EXPLICITE contrat→établissement. Contenu : migration `0003_contrat_etablissement.sql` (`contrat.etablissement_id` uuid NULLABLE + FK → `etablissement`, NOT NULL repoussé à P5) ; DTO contrat (union par mode) étendu d'un lien « SOIT `etablissementId` (existant) SOIT `nouvelEtablissement` (création à la volée) », exclusivité par `refine` (aucun fourni = toléré) ; `resoudreEtablissement` DANS la transaction du contrat (création inline atomique + émet `EtablissementCree` ; sinon valide existence+appartenance foyer → **400**) ; `etablissementId` ajouté aux events `ContratCree`/`ContratModifie` en **champ OPTIONNEL** (`z.string().uuid().nullish()`, v1 conservée) — DÉCISION CLÉ : optionnel et non requis-nullable, sinon les fixtures `ContratCree` SANS le champ dans `svc-tarification`/`svc-notifications` (projection.service.spec) cassent au `.parse()` ; garde suppression `compterContratsRattaches(tx,id)` réelle → **409** si ≥1 contrat ; gateway client + BFF **nesté `/api/v1/foyers/:foyerId/etablissements`** (GET/POST/PUT/DELETE, `@FoyerScope('param:foyerId')`). **COLLISION ÉVITÉE** : pas `/api/v1/etablissements?foyer=` (déjà pris par l'ancien annuaire notifications `NotificationsController` GET + `PUT /:cle`, + casserait le web non encore migré) → choix nesté validé par l'utilisateur (AskUserQuestion), unification en P6. Doc AsyncAPI MAJ (payloads contrat + events établissement, que P1 avait omis). Pièges : (a) **drizzle-kit ne marche pas depuis le worktree** (pas de node_modules ; chemins absolus Windows `C:` mal joints → ENOENT) → migration 0003 + snapshot + entrée `_journal.json` écrits À LA MAIN (lancer drizzle-kit dans le repo PRINCIPAL pollue son working tree → incident, nettoyé) ; (b) worktree neuf = `pnpm install --frozen-lockfile` requis avant `nx` ; (c) `exactOptionalPropertyTypes` → param du refine typé `etablissementId?: string | undefined` ; (d) `nx format:check` SANS base compare au `main` LOCAL périmé (pré-P1) → faux positifs ; utiliser `--base=origin/main` ; (e) e2e parcours api-gateway = **flaky sous charge** (readiness 30s dépassé en run-many ; passe seul / `--parallel=2`). Restent P3→P6. Piège diffus : `CleEtablissement` est dans DTO+Zod+OpenAPI+**Pact**+types web générés → démantèlement en dernier (P6) pour CI verte. Voir [[code-conventions-strict]], [[feature-parents-foyer]] (pattern projection NATS + gating/sélecteur foyer), [[prod-deployment-facts]], [[feature-planning-etat-ajuste]] (#86 = source de la régression e2e-stack).
