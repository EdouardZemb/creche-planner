---
name: audit-2026-07-plan-amelioration
description: "Audit global du 2026-07-01 (5 agents) — verdict ~8/10, plan d'amélioration en 6 lots dispatché en chips/sessions fraîches"
metadata:
  node_type: memory
  type: project
  originSessionId: 721e04ba-2a80-46d8-9eca-4e79abdfc7e4
---

Audit complet réalisé le 2026-07-01 (architecture, frontend, tests/CI, sécurité, docs). Verdict ~8/10, sécurité A−, aucun risque critique. Plan en 6 lots, chacun spawné en chip pour session fraîche :

- **Lot 0** (urgent) : fix AN-14 allowlist mailer (compare `to` entier → bloque foyers 2 parents, envoi réel ACTIF en prod) + `USER 1000:1000` dans Dockerfile.
- **Lot 1a** : ✅ **MERGÉ MAIN 2026-07-02** — couverture + seuils ratchet : api-gateway #129 `d1ac703`, 5 svc-* + web + doc 03 §6 #130 `1d8f69d`, Stryker foyer-domain (92,31 %) + referentiel-domain (82,11 %, matrice mutation.yml à 4) #131 `e756bbe`. La baseline coverage-compare couvre désormais les 7 projets apps (publiée au 1er run main post-merge). Pièges : Vitest ≥ 3 exige `include` explicite pour rapporter les fichiers non chargés ; mesure branches svc-tarification varie local (49,01) vs CI (48,36) → seuil 48 ; vérifs Pact provider NON idempotentes sur base locale sale (`etablissement_foyer_nom_uq`) → `docker compose down -v` avant de mesurer ; survivants Stryker referentiel = messages d'erreur non assertés (catalogue-versionne 73 %), pistes dans PR #131.
- **Lot 1b** : ✅ **MERGÉ MAIN 2026-07-02** — tests `EditeurContratSemaine` (12 tests) + extraction `besoinsSemaine.ts` (initBesoins/versCorps purs, 7 tests) = PR #132 squash `4d69afb` (ratchet web 79/71/68/82→82/74/71/84) ; complétion `EtablissementsPage.test.tsx` (4→12 tests : état vide, erreur réseau+Réessayer, validation 422 par champ, erreur 500, édition préremplie, suppression via ModaleConfirmation, annulation, erreur archivage) = PR #134 squash `d52c929` (ratchet →**83/75/72/85**). Pièges vécus : PR empilée #133 **fermée définitivement par GitHub** à la suppression de sa branche de base (pas de retarget auto, non réouvrable — recréée en #134) ; `git rebase origin/main` post-squash de la base = sans conflit (le commit squashé se résout seul) ; `gh pr merge --delete-branch` échoue en worktree (« main is already used by worktree ») APRÈS le merge distant → supprimer la branche distante à la main. NB : `EtablissementsPage.test.tsx` existait déjà (4 tests, PR #102) contrairement au constat « 0 test » de l'audit. Merges faits par l'agent sur demande explicite (« merge les pr »).
- **Lot 2a** : ✅ **MERGÉ MAIN 2026-07-02** (3 PR ciblées, auto-merge squash) — PR #135 : extraction module pur `apps/web/src/planning/saisieAbsence.ts` (fenetreAbsence, saisieAbsenceValide, typeAbsenceDepuisFenetre, plageGardeDuJour, TYPES_ABSENCE ; 22 tests) ; PR #136 : enveloppe commune calendriers = hook `useCalendrierContrat` (usePlanning+useAnnonce+useSaisieServeur+portée+flux PUT durable générique via construireCorpsDurable/reinitialiserSaisie, `socleContratDurable`) + `BarreStatutCalendrier`/`CalendrierMois`/`ModaleContratDurable` (9 tests hook ; suites Creche/Abcm inchangées ; warnings React Compiler 100→95, calKey useRef-au-rendu supprimé) ; PR #137 : cache opt-in `useAsync(fn, deps, {cle})` (Map module-level, dédup en vol, invalidation via reload(), abort dernier abonné, erreurs non cachées) adopté sur `useContrats` seulement — **useFoyer volontairement sans cache** (FoyerModifierPage mute puis navigue sans recharger). ⚠️ Piège découvert : importer statiquement un module qui touche `api/client` dans `test-setup.ts` fige la vraie classe ApiError AVANT les vi.mock des fichiers de test (casse les `instanceof` sur les ApiError mockées, 11 tests rouges) → import **dynamique** dans le beforeEach.
- **Lot 2b** : ✅ **MERGÉ MAIN 2026-07-02** (4 PR squash, CI verte sur chacune : #138 `3c925b2`, #139 `4d6f43c`, #140 `69db4fc`, #141 `0c64c15` ; branches distantes+locales supprimées ; le poller staging auto-déploiera le nouvel agrégat `:main`) — **#138** split `planification.service.ts` : génération pure extraite vers `libs/planification/domain/src/lib/generation-prestations.ts` (`genererPrestationMois`/`semaineTypeDepuisJson`/`dureeDePlage` + types JSON), couverture 100 %, **Stryker 84,09 %** (survivants = mutants équivalents : spreads de métadonnées non observables), + specs `ReferentielClient`/`ZodValidationPipe` ajoutées côté svc pour tenir le ratchet après retrait du code pur ; **#139** helper `appelResilient(config)` (`apps/api-gateway/src/clients/appel-resilient.ts`) + `foyer.client.ts` réécrit (525→~390 l.), 3 réexports `resilience.ts` supprimés + 2 specs dupliquées (lib = sur-ensemble), **pacts identiques prouvés** (`git diff --exit-code pacts/` après test fresh ; les specs pact consumer n'utilisent QUE fetch brut, jamais les clients) ; **#140** `scripts/services.json` source unique (registre + servicesApplicatifs×7 + providersPact×5) consommée par can-i-deploy/staging-poll, vérif cohérence compose-staging↔JSON à chaque tick (fail explicite), garde-fou WARNING-only try/catché dans deploy.mjs (jamais bloquant prod) — serveurs OK car pollers/deploy tournent depuis un clone git pullé ; **#141** `scripts/seed-oracle.json` (cibles + champ `cas` → CT-02/03/04 de docs/02 §6) + encart doc. Pièges vécus : retirer du code pur très couvert d'une app **casse son ratchet de couverture** (compenser par des tests app dans la même PR) ; commitlint `header ≤ 100` + `body lines ≤ 100` ; here-string PowerShell + `git commit -m` avec guillemets → passer par Bash.
- **Lot 3** : statuts réels des actions AUD-01→16 (doc 25) et AQ-01→18 (doc 27) — périmés au point de contredire la réalité ; CONVENTIONS.md (React 19/Compiler) ; index docs + CONTRIBUTING (AQ-16) ; politique rétention logs PII.

**Faux positifs à ne pas re-signaler** : FOYER_AUTHZ_ENFORCE est ACTIF en prod (le code default observe-only trompe les audits statiques) ; AUD-05/CD est FAIT (doc 25 périmé) ; NE PAS condenser les 13 migrations svc-notifications (déjà appliquées en prod, risque > gain) ; **« e2e-stack skippée sur changement web-only » est FAUX** (vérifié 2026-07-01 : `affected-images` filtre sur `"apps/svc-*,api-gateway,web"`, un diff web-only donne `["web"]` → `has_projects=true` → e2e-stack + smoke-stack tournent). Reportés sciemment : rate-limit Redis (mono-instance), lazy-loading routes, TanStack Query.

Lié : [[feature-profil-preferences-notif]] (AN-14), [[prod-deployment-facts]].
