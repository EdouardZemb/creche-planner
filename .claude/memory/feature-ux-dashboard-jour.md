---
name: feature-ux-dashboard-jour
description: "Chantier UX dashboard « Aujourd'hui » : audit + 4 lots validés PO — CHANTIER COMPLET 4/4, tout mergé main (2026-07-04) ; reste à déployer via release train"
metadata:
  node_type: memory
  type: project
  originSessionId: 36cc606b-f8fb-4bff-84fb-35b6c7b87678
---

Chantier UX « prototype → produit pro » sur le dashboard jour (suite du chantier planning [[feature-ux-planning-pro]]). Audit fait le 2026-07-04, plan 4 lots validé PO (profondeur « polish + parcours », « Demain » toujours visible compact).

- **Lot 1 ✅ MERGÉ main `50c31ef` (PR #157, squash, 2026-07-04)** — carte « Semaine à valider » en tête du dashboard (`CarteAValider` locale à `DashboardJourPage.tsx`, dédoublonnée PAR SEMAINE, silencieuse si rien/erreur) + fix cohérence `PastilleAValider` (comptait des contrats, annonce des semaines). Piège rencontré : merge refusé « head branch not up to date » (protection) après le merge de #156 → `gh pr update-branch 157 --rebase` + re-CI.
- **Lot 2 « Demain » ✅ MERGÉ main `534f762` (PR #159, squash, 2026-07-04, autorisé explicitement par l'humain ; branche distante supprimée à la main — piège worktree connu)** — section « Demain » toujours visible/compacte (`SectionDemain` locale, réutilise `RangeeJour`/`lignesDuJour` avec `jourSuivant(aujourdhui)`), 2e fetch UNIQUEMENT si demain change de semaine ISO (silencieux en erreur, pattern BandeauCoutMois), deep-link « Modifier » au mois de DEMAIN, aria-label suffixé « demain » (liens homonymes entre sections). Helper pur `jourSuivant` dans `apps/web/src/utils/dates.ts`. Piège tests : Léa apparaît dans LES DEUX sections → sélecteurs pluriels + nom accessible exact ; horloge factice `vi.useFakeTimers({shouldAdvanceTime:true})` + `setSystemTime` pour fixer le calendrier (vendredi 31/07 = même semaine + fin de mois ; dimanche 05/07 = semaine suivante).
- **Lot 4 finition/langage : ✅ MERGÉ main `c9e4096` (PR #160, squash, 2026-07-04, merge autorisé explicitement par l'humain ; branche distante supprimée à la main — piège worktree connu)** — lancé AVANT le lot 3 (chip lot 3 jamais démarré, aucune PR existante → l'ordre « lot 4 en dernier » n'était pas respectable). Contenu : « Ajusté »→« Horaires modifiés » + « ALSH »→« Centre de loisirs (ALSH) » (mapping LIBELLES_ETAT local à l'écran), état vide enrichi « Prochaine garde : lundi 6 juillet » (composant local `ProchaineGarde`, scan semaine chargée puis semaine ISO suivante en fetch silencieux, silence au-delà de ~2 sem.), helper pur `libelleDate()` dans `utils/dates.ts`, styles inline→classes styles.css (`.jour-pastille`, `.bandeau-cout*`, `.titre-avec-date`/`.sous-titre-date`/`.titre-carte`, `.etat-vide-texte`). Vérifié 375px sur stack locale (samedi réel → cas week-end grandeur nature).
- **Lot 3 hiérarchie/stabilité ✅ MERGÉ main `75f9747` (PR #161, squash, 2026-07-04, merge autorisé explicitement par l'humain ; branche distante supprimée à la main — piège worktree connu) → CHANTIER COMPLET 4/4** — `BandeauCoutMois` déplacé EN BAS (après journée + « Demain » : il rend null en chargement, son apparition ne décale plus rien), carte squelette de chargement (`RangeeSquelette` locale ×2, classes `.squelette-*` dans styles.css : fond `--bleu-clair`, `@keyframes squelette-pulse`, coupé sous `prefers-reduced-motion` ; annonce sr-only + aria-live conservées, silhouette aria-hidden), message d'erreur générique « Impossible de charger votre journée… » (le repli `e.message` de `messageErreur` fuitait du technique), inline style erreur → `.texte-erreur`. Vérif 375px : `docker compose pause api-gateway` pour FIGER l'état de chargement (piège : `preview_screenshot` timeout tant que des requêtes pendent — utiliser eval/inspect), positions h1/carte identiques avant/après chargement.
- **Chip hors périmètre — nav mobile : PR #158 ✅ MERGÉE main `fa8b8ec` (squash, 2026-07-04, autorisée explicitement par l'humain — le classifier refuse le merge agent spontané), branche supprimée** ; piège protection re-rencontré : « head not up to date » après merge intermédiaire → `gh pr update-branch 158 --rebase` + re-CI, puis `--delete-branch` échoue en worktree (main occupée par le clone principal) → merge OK quand même, supprimer la branche distante à la main. barre d'onglets fixe en bas <768px (Aujourd'hui/Planning+pastille/Coûts + « Plus » disclosure pour les pages de gestion), cloche sortie de la `<nav>` (haut droite), ≥768px `display: contents` restitue l'en-tête (UN seul DOM, pas de liens dupliqués → tests `getByRole` sains). Pièges : règle de base `main { padding }` déclarée APRÈS la section nav dans styles.css → `main#contenu` pour le padding bas ; e2e offline locaux = config Playwright temporaire sur :4201 (le :4200 sert un autre clone, `reuseExistingServer` testerait le mauvais code) ; nom accessible « Coûts annuels » conservé par `aria-label` (libellé visible court « Coûts », WCAG 2.5.3).

**Why:** le moment critique du mardi (validation → mail crèche) était invisible sur la porte d'entrée ; l'usage principal réel est « le soir, une main libre ».
**How to apply:** garder le pattern « composant local silencieux » (BandeauCoutMois/CarteAValider) pour tout encart secondaire du dashboard ; toute nouvelle formulation passe par les mots de parent (dates réelles via `libelleSemaine`, jamais d'ISO). Vérif visuelle : [[verif-ui-locale-stack]] (+ insert `notification_hebdo` pour voir la carte).
