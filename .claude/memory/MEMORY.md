- [Travail à distance (vacances 08/2026)](travail-a-distance-vacances-2026-08.md) — contexte versionné dans le dépôt (PR #276, `406c7d1`) ; ⚠️ la mémoire du dépôt n'est qu'un **miroir** à resynchroniser ; runbook Tailscale+RDP ; 2 routines cloud actives
- [Plan consolidation UI + qualité](plan-consolidation-ui-qualite.md) — audit 4 volets 2026-07-29, chantiers A(prod)/B(outillage)/C(UI)/D(code) dans `.claude/plans/consolidation-ui-et-qualite.md` ; **🏁 chantier A : 7/7 lots MERGÉS 2026-07-29 (#258→#265)** (incident 0-backup-depuis-17/06 résolu, heartbeat installé prod) ; **reste : vérif backup nightly 30/07, ops user (rclone A1b, Period/Grace A4), prochain train de release (active A2/A5/A6/A7 en prod + rejeu projection #257), puis 1 sem logs propres → bascule INTERSERVICE (user)** ; **2026-08-01 : C0 + B1/B2/B6 ✅ MERGÉS main (`bb3948e`/#267, `1d47636`/#268), non déployés → R1 plancher `bb3948e` ; C1-C2-C3-C4 ✅ MERGÉS main `9171228`/#272** (H4 tranchée : C2 avant le plan 31 ; outil de non-régression visuelle `nx run web:e2e-visuel` versionné au passage) ; **→ plancher R1 = `9171228`** ; prochaines sessions : release (R1/R2) puis C5 (préflight `pnpm preflight` — PAS `doctor`, nom réservé pnpm ; targetDefaults Nx n'atteignent pas les cibles écrites à la main ; port Pact 3999 squatté par un process fantôme) ; **plan révisé 2026-07-31 : chantier R (R1 = train n°16 ≥ `9aee291`) préalable universel** ; **2026-08-02 (session DISTANTE) : R1 ✅ DÉPLOYÉ PROD `0.15.0` + D1/D2/D3 MERGÉS** (#278 `e2e38e1`) ; **2026-08-03 (session DISTANTE) : C5 ✅ FAIT** sur `claude/implementation-du-plan-0isdve` — **chantier C clos hors C6 (optionnel)** : socle d'assemblage partagé (l'énoncé du plan était périmé, les « briques » l'étaient déjà — c'est l'ASSEMBLAGE qui était dupliqué, pas la logique), 3 gros composants découpés → **plus aucun composant web > 700 l.**, empreinte visuelle strictement identique (4 586 nœuds), baseline lint 658 → **649**, et **angle mort corrigé : les règles react-hooks ne voyaient aucun hook, tous en `.ts` sans JSX** ; **suite = R2 (b)/R3 (gestes PO) puis plan 31** (lot 4 peut atterrir sur les calendriers découpés) ; **2026-08-03 (session DISTANTE) : B3 ✅ MERGÉ (#285 `fca683a`) et D4 ✅ FAIT** sur `claude/implementation-du-plan-deakfu` — convention « frontières Nx & vocabulaire partagé » (CONVENTIONS.md §4) **outillée** par `pnpm frontieres` (step bloquant du job `ci`), qui a trouvé à son 1ᵉʳ run que **`context:notifications` n'était enregistré dans aucun `depConstraints`** ⇒ svc-notifications sans frontière de contexte depuis toujours ; **leçon : un tag Nx hors `depConstraints` n'est pas permissif, il n'est jamais évalué** (3ᵉ angle mort du même genre après D2 et C5 — le défaut est dans le périmètre de l'outil, pas dans le code) ; **2026-08-03 (session DISTANTE) : B7 ✅ FAIT** sur `claude/implementation-plan-zh02l1` — source unique CONTRIBUTING.md § « Pièges : ce que l'outillage garantit », **39 passages purgés dans 19 documents**, porte `pnpm pieges` (`scripts/verifier-pieges-doc.mjs`, step bloquant du job `ci`, 13 sondes négatives) ; **défaut réel trouvé** : `serve`/`dev`/`preview` d'`apps/web` sans aucun `dependsOn` ⇒ le piège « builder les libs d'abord » était **encore vrai sur le chemin Vite dev** (B2 n'avait câblé que test/typecheck/e2e/build) → arête posée, `web:serve` 1 → 3 tâches ; **leçon : « documenté » vs « encodé » ne se distinguent qu'en lisant l'outil** (4ᵉ du genre après D2/C5/D4, mais ici le défaut était dans le périmètre de la CROYANCE, pas de l'outil) ; **2026-08-05 (session DISTANTE) : D6 ✅ FAIT** sur `claude/implementation-du-plan-z3nrgg` — **l'oracle « exactement 27 routes » ne gardait rien du réel** (liste attendue écrite à la main EN FACE d'un document écrit à la main = la même main), et la garde qui le remplace a trouvé au 1ᵉʳ run que **12 opérations servies sur 49 n'étaient documentées nulle part**, dont **les 6 routes `/api/v1/notifications/*` — « valider ma semaine » en entier** ; garde = spec vitest qui confronte le document au **graphe de modules Nest** (lire les métadonnées de décorateurs ≠ booter Nest : seule l'INSTANCIATION est cassée sous vitest, pas la lecture — d'où une garde in-process en < 10 ms là où l'e2e doit lancer le bundle) ; document 27 → **37 paths / 49 opérations**, `/api/health` corrigé en readiness (mensonge introduit par B3), types web **dérivés** au lieu d'un miroir manuel de svc-notifications ; **leçon : 5ᵉ défaut d'affilée logé dans le PÉRIMÈTRE de l'outil, pas dans le code** (D2, C5, D4, B7, D6) ; **restent B4** (attend H3, décision PO) **et B5** (exige la pile Docker locale), **C6** non lancé (H2), **D5** opportuniste, **+ gestes PO R2(a) rclone / R3 INTERSERVICE** — détail dans le fichier
- [Chantier versionnement dates d'effet (SFD 30)](chantier-versionnement-dates-effet.md) — 7/7 lots ✅ **DÉPLOYÉ PROD `0.14.0` 2026-07-29** ; ⚠️ **défaut lot 2 : projection `grille_tarifaire` = PERISCOLAIRE seul (PK = grilleId partagé entre modes) → correctif PK surrogate **#257 mergé main `9818302`, PAS ENCORE DÉPLOYÉ** ; puis rejeu prod (≠ simple restart)** ; reste smoke PO ; pièges dans le fichier
- [SFD 30→33 extension famille](plan-sfd-30-33-extension-famille.md) — 4 SFD brouillons docs/30→33 + audit abstractions P1→P6 ; **SFD 30 exécutée → [[chantier-versionnement-dates-effet]]** ; **plans 31/32/33 + factures-reelles RÉVISÉS 2026-07-30/31** (ré-ancrés post-plan-30, lot 5 du 31 retiré → absorbé par 33, joursFeries en shared-kernel, ordre consolidation→31→32→33→factures) ; reste validation PO (SFD + hypothèses)
- [Plan fondations backend](plan-fondations-backend.md) — chantier n°10, 6 lots ✅ DÉPLOYÉ PROD `0.13.0` (2026-07-18) ; assertion HMAC + scoping LIVE en OBSERVE-ONLY ; **reste : enforce après ~1 sem de logs propres + smoke PO** ; décision fail-open `ScopeFoyerGuard` TRANCHÉE (runbook §5, PR #264, cf. [[plan-consolidation-ui-qualite]]) ; détail dans le fichier
- [Plan qualité Coquille + rappels + mail au service](plan-qualite-coquille-navigation.md) — chantier n°9, 9 lots ✅ DÉPLOYÉ PROD `0.12.0` (2026-07-17, #214→#222) ; exécution → [[chantier-coquille-execution]] ; détail lots/décisions dans le fichier
- [Exécution chantier Coquille](chantier-coquille-execution.md) — ✅ COMPLET + ops L7 faite (découplage `NOTIF_APP_URL` ≠ `SERVER_ORIGIN`, PR #223) ; reste smoke live PO ; pièges dans le fichier
- [Plan qualité Profil & communication](plan-qualite-profil-communication.md) — 8/8 lots ✅ DÉPLOYÉ PROD `0.12.0` (migrations svc-notif 0015+0016 OK) ; L1 anti-tempête récap = risque confiance n°1 ACTIF PROD ; gotchas dans le fichier
- [Veille alertes : outillage](veille-alertes-outillage.md) — les alertes CodeQL/Dependabot sont **illisibles** depuis une session distante (403 proxy + aucun outil MCP) ; passer par la conclusion du workflow `veille-alertes.yml` (cron 05:40 UTC), `success` = vérification faite **et** rien ≥ critical/high ; `failure` = lire le résumé, `POINT MORT` ≠ `ALERTES` ; **2026-08-05 : le 403 Dependabot = alertes DÉSACTIVÉES sur le dépôt, pas un défaut de jeton** (⚠️ **action PO : Settings → Code security**, aucun PAT n'y changera rien ; message d'aide + autotest corrigés) ; **tri des 5 alertes CodeQL ≥ seuil fait** — #20/#21 vrai positif corrigé (regex `.test()` = pas un validateur de type, `?mois[]=` passait en tableau ⇒ frais fixes ABCM jamais facturés), #17 corrigé, #12/#1 faux positifs **à dismisser**
- [Piège SCA rouge sans diff](piege-sca-rouge-sans-diff.md) — `main` rouge sur le job `security` sans qu'aucun commit ne touche aux deps = base CVE de Trivy qui bouge ; comparer au run précédent avant de fouiller le diff, lire la table de l'étape **bloquante** (pas l'informative, dev inclus) ; un override pnpm anti-CVE se re-périme (`brace-expansion` 2× en 8 j) ; ⚠️ `.trivyignore` CVE-2026-14257 à réviser depuis le poste (osv.dev bloqué par le proxy)
- [Piège CodeQL `init`/`analyze` désynchronisés](piege-codeql-action-init-analyze-desync.md) — check « Analyse CodeQL » rouge sur une PR Dependabot = `configuration error` (« Loaded a configuration file for version X, but running version Y »), **pas** une alerte de sécurité ; cause : pas de `groups` sur l'écosystème `github-actions` → 1 PR par sous-action (#269/#270) ; corrigé par le groupe `codeql-action`
- [Piège `prettier --check` local (CRLF)](piege-prettier-check-crlf.md) — échoue sur TOUS les .md/.json en local (autocrlf) ; juger sur `git diff`, jamais sur `--check`
- [Course build/typecheck : libs hors sujet](piege-course-build-typecheck-libs.md) — question tranchée par la mesure, **#274 ✅ mergée main `2e996b3`** (doc seule) : les 14 libs ont une arête d'ordre, ne PAS les « harmoniser » sur les apps
- [pnpm via corepack](pnpm-corepack-version.md) — toujours `corepack pnpm@10.34.2`, pas le pnpm global 8.x
- [Clone propre -public](repo-clean-clone-location.md) — bosser dans `-public`, jamais pousser depuis l'original ; main protégée, PR + check `ci`
- [Conventions strictes](code-conventions-strict.md) — ESLint 9 flat config type-aware (ratchet warn→error), React layer au root (lint-staged), verbatimModuleSyntax web-only, branded types
- [Chantier qualité établissements](plan-qualite-etablissements.md) — 4 lots ✅ mergés main 2026-07-15 (#200→#205) + fix CI #204 (pnpm audit mort → Trivy fs) ; détail/pièges dans le fichier
- [Migration react-router v7](dep-react-router-v7-migration.md) — ✅ MERGÉE (#149) ; piège worktree = builder libs contracts-kernel/shared-semaine avant `nx test web`
- [Piège pact-drift flaky (course pact-core)](piege-pact-drift-flaky-course-pact-core.md) — « expected but not received » aléatoire en CI = course pact-core sous charge, PAS un conflit de ports ; corrigé PR #266 mergée (fileParallelism CI + retry) ; **variante locale** : specs _provider_ en « provider non prêt après 40000 ms » sous `--parallel=3` + pile Docker up → rejouer isolément avant de crier à la régression, passer à `--parallel=2`
- [Migration pact v17](dep-pact-v17-migration.md) — ✅ MERGÉE (#150) drop-in ; piège = `/pacts` dans `.prettierignore` (sinon lint-staged casse pact-drift)
- [webpack-cli ^7.1.0](nx-webpack-cli-pin.md) — 6 services en `webpack-cli build` (nx:run-commands) ; bump 5→7 = `--node-env` → `--config-node-env` (#151) ; Nx 22→23 mergée (#148)
- [Feature notifications planning](feature-notifications-planning.md) — ✅ COMPLÈTE, envoi réel ACTIF en prod ; ⚠️ risque mail crèche réelle `jaudrey@cscpapin.asso.fr` (ne pas cliquer Envoyer) ; détail dans le fichier
- [Feature édition hebdo besoins](feature-edition-hebdo-besoins.md) — ✅ COMPLÈTE (#65→#68) ; décisions : semaine notifiée only / validation par contrat / 1 mail par établissement
- [Feature parents du foyer](feature-parents-foyer.md) — ✅ COMPLÈTE (#73→#83) ; ⚠️ AUTH isolation foyer ACTIVE en prod depuis 2026-06-28 (`FOYER_AUTHZ_ENFORCE=1`) ; détail/guards dans le fichier
- [Feature planning état ajusté](feature-planning-etat-ajuste.md) — ✅ COMPLÈTE ; `classerAbsence()` module pur web ; piège : `nx test web` ne typecheck PAS (`nx run-many -t typecheck test -p web`)
- [Feature établissements entité libre](feature-etablissements-entite-libre.md) — ✅ COMPLÈTE prod `0.6.0`, back-fill fait, table legacy droppée ; détail/pièges dans le fichier
- [Feature cycle de vie du foyer](feature-foyer-cycle-de-vie.md) — ✅ COMPLÈTE prod `0.7.0` ; guards `@FoyerScope`/`@CreationFoyerUnique` réels ; pièges (worktree, e2e identité) dans le fichier
- [Feature dashboard jour](feature-dashboard-jour.md) — ✅ COMPLÈTE prod `0.7.0` (page `/foyers/:id/dashboard` + P3a/b/c) ; piège worktree « faux vert » dans le fichier
- [Feature profil + préférences notif](feature-profil-preferences-notif.md) — ✅ COMPLÈTE prod `0.8.0` (secret `DESABONNEMENT_TOKEN_SECRET` posé, migrations 0002/0011/0012) ; ⚠️ récap mardi = 1 mail/parent + `List-Unsubscribe` LIVE ; détail dans le fichier
- [Gouvernance doc 2026-07](gouvernance-doc-2026-07.md) — ✅ MERGÉ (#142/#145/#144) : AUD 16/16, AQ-14/15 partiels, AQ-17/18 ouverts ; piège : PR empilée = FERMÉE à la suppression de sa base → rebase + nouvelle PR
- [Piège numéros de PR pré-publication](piege-numeros-pr-pre-publication.md) — PR < 2026-06-18 = ancien dépôt privé (import squashé `4f36e3e`) ; ne jamais les résoudre sur GitHub, vérifier dans le code
- [Audit 2026-07 + plan 6 lots](audit-2026-07-plan-amelioration.md) — lots 1a/1b/2a/2b ✅ mergés ; restent 0/3 ; faux positifs connus à ne pas re-signaler
- [Chantier UX dashboard jour](feature-ux-dashboard-jour.md) — ✅ COMPLET 4/4 mergé (#157/#159/#160/#161), déployé via `0.9.0`
- [Chantier UX planning pro](feature-ux-planning-pro.md) — ✅ COMPLET 5/5 mergé (#152→#156), déployé via `0.9.0` ; détail lots dans le fichier
- [Chantier valider ma semaine](feature-valider-ma-semaine.md) — ✅ 6/6 COMPLET, DÉPLOYÉ PROD `0.10.0` (migrations svc-notif 0013/0014 OK) ; reste smoke PO ; pièges (worktree fantôme, symlinks pnpm) dans le fichier
- [Chantier qualité foyer+onboarding](feature-qualite-foyer-onboarding.md) — ✅ COMPLET 5/5 (#194→#198, migration svc-foyer 0003), DÉPLOYÉ PROD `0.11.0` ; reste smoke live PO
- [Chantier qualité Coûts](feature-qualite-couts.md) — ✅ COMPLET 6/6 (#187→#193), DÉPLOYÉ PROD `0.11.0` ; reste **action PO : cocher « Première inscription » sur les contrats ABCM 2026** ; grille_tarifaire = dette → reprise en SFD 30 (DV-01)
- [Chantier contrats & besoins](feature-contrats-besoins.md) — ✅ COMPLET, DÉPLOYÉ PROD `0.9.0` + back-fill enfant_id (0 NULL/8) ; reste NOT NULL enfant_id + smoke ALSH PO ; pièges (pact doublons, e2e-stack destructif) dans le fichier
- [Client API retry+timeout](feature-client-api-retry-timeout.md) — `requeteIdempotente` (timeout 10 s + rejeu borné) + garde de séquence réhydratation ✅ MERGÉS (#170/#171/#172), déployés via `0.9.0` ; pièges dans le fichier
- [Angles morts de l'audit axe](a11y-axe-angles-morts.md) — axe e2e vert ≠ contraste OK : focus, bordures de champs, `:disabled`, `opacity` d'ancêtre lui échappent ; méthode de balayage `getComputedStyle` + piège de spécificité `.app-header a`
- [Vérif UI locale (stack+Vite)](verif-ui-locale-stack.md) — docker stack + seed puis stop web + Vite dev :4200 ; pièges : builder shared-semaine, shims `.bin` périmés après pnpm install
- [Accès serveur prod](prod-server-access.md) — `ssh edouard@192.168.1.129` ; clé chiffrée → passer par `ssh.exe` Windows (agent service), pas Git Bash
- [Poller staging digest agrégé](staging-poller-watches-gateway-only.md) — corrigé (#54) : le poller sonde un digest agrégé des 6 images ; secours `remote-deploy.ps1 -Environment staging`
- [Faits prod](prod-deployment-facts.md) — domaine `creche.testlens.dev` ; ▶ PROD @ `0.14.0` (2026-07-29, 15e train) ; ⚠️ Prometheus PAS sur localhost:9090 de l'hôte (faux négatif d'alertes) ; `deploy.mjs` = seule voie, poller auto, rollback auto, secrets sops+age ; ⚠️ cosign ne vérifie QUE la gateway ; AUTH isolation foyer active ; historique complet des releases + pièges ops dans le fichier
- [Outbox SemaineValidee](feature-outbox-semaine-validee.md) — svc-notifications émet `notifications.SemaineValidee.v1` (#168) ✅ mergé ; pièges : z.uuid(), z.array().readonly(), commitlint ≤100
- [Feature contrat enfantId](feature-contrat-enfant-id.md) — contrat.enfant_id = lien de référence, prénom dénormalisé rafraîchi par projection (#167) ✅ mergé + back-fill prod fait ; reste migration NOT NULL différée ; pièges dans le fichier
- [Chantier confiance & quotidien](plan-confiance-et-quotidien.md) — 7 lots ✅ MERGÉS main `0bc7a8d` 2026-07-18 (#231→#237, migrations tarif 0004 + notif 0018) ; ✅ **DÉPLOYÉ PROD `0.14.0` 2026-07-29** ; reste vérif live 375 px ; gotchas d'orchestration multi-agents réutilisables dans le fichier
