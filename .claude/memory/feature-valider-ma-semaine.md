---
name: feature-valider-ma-semaine
description: 'Chantier qualité du parcours « valider ma semaine » — plan .claude/plans/valider-ma-semaine.md (6 lots), audit 2026-07-06 validé PO'
metadata:
  node_type: memory
  type: project
  originSessionId: 879ef6bf-48f7-4614-970c-9e6a4444d2c2
---

Chantier « valider ma semaine » (audit 2026-07-06, périmètre + 3 décisions produit validés PO) : plan complet dans `.claude/plans/valider-ma-semaine.md` (6 lots). **Lot 1 ✅ MERGÉ main `95f9e75`** (PR [#180](https://github.com/EdouardZemb/creche-planner/pull/180), 2026-07-06). **Lot 2a ✅ MERGÉ main `835b4a2`** (PR [#181](https://github.com/EdouardZemb/creche-planner/pull/181), 2026-07-07). **Lot 2b ✅ MERGÉ main `a37263c`** (PR [#182](https://github.com/EdouardZemb/creche-planner/pull/182)). **Lot 3 ✅ MERGÉ main `965a0b7`** (PR [#183](https://github.com/EdouardZemb/creche-planner/pull/183), squash, 2026-07-08 ; CI 100 % verte ; doc 06 §29 + RGPD observabilite.md inclus dans la PR). **Lot 4 ✅ MERGÉ main `5e86650`** (PR [#184](https://github.com/EdouardZemb/creche-planner/pull/184), squash, 2026-07-09 ; CI 100 % verte y compris `e2e-stack`/`smoke-stack`/`pact-can-i-deploy`). **Lot 5 ✅ MERGÉ main `929fc80`** (PR [#185](https://github.com/EdouardZemb/creche-planner/pull/185), squash, 2026-07-09 ; CI verte, **`e2e-stack` a RÉELLEMENT tourné et passé** après 1 re-run — 1er essai `affected-images` n'a pas pu acquérir de runner GitHub, échec infra sans rapport avec le code → `gh run rerun <run> --failed`) → **CHANTIER 6/6 COMPLET, tout mergé main**. ✅ **CHANTIER DÉPLOYÉ PROD `0.10.0` (2026-07-09, ref `8abeb99`, Deployment #5373433550, 11e release train)** — les **2 migrations svc-notif** (`0013_notification_lien` Lot 1 + `0014_envoi_recap_hebdo` Lot 3 ; Lot 2a = 0 migration jsonb, Lot 2b/4 = code-only), toutes additives (rollback DROP), **vérifiées appliquées en prod** (`t|1`) ; 0 nouveau secret/env/compose. **Reste** : smoke fonctionnel LIVE du parcours par le PO (lien mail mardi → éditeur → heures réelles → validation) = geste humain. Détail du train dans [[prod-deployment-facts]]. ⚠️ Piège du train : la session a démarré en **HEAD détaché** (laissé par la session de fond du Lot 5) → `nx release` a commité/taggé hors branche, `git push origin main` disait « up-to-date » → fix `git checkout main` + `git merge --ff-only <sha>` avant push.

Lot 5 livré (filet e2e du parcours nº1) : la spec `apps/web/e2e/validation-semaine.stack.e2e.spec.ts` **existait déjà** (contrats #165, revalidation idempotente) → **2e test AJOUTÉ dans le même fichier** (choix confirmé PO) plutôt qu'un fichier neuf homonyme. Parcours nominal complet : lien profond `?semaine` → éditeur auto-ouvert → modale jour gardé (`getByLabel('Heure d’arrivée').fill('08:00')` sur contrat 08:30) → assert état déduit `/de plus que les horaires habituels.*facturé en complément/` → Confirmer via helper partagé `attendreEnregistrementPlanning` (piège #171) → per-contract « Valider » → `« Semaine validée (avec modifications). »` + `deltaModifs.jours` contient le jour ajusté (preuve catégorie `ajustements` traverse le BFF) → `RelectureEnvoi` (region « Dernière étape : prévenir les services ») : bandeau « Mode test » + `/horaires ajustés/` + bouton `Envoyer le récapitulatif à Crèche Les Hirondelles` → `ModaleConfirmation` « Envoyer (mode test) » → `/Test réussi/`. **Reload = CALENDRIER** (pas l'éditeur : après validation la semaine quitte `A_VALIDER` filtré strict côté `validation.service.ts:113` → l'éditeur ne se rouvre plus au deep-link) : `?mois&enfant` + onglet Crèche → `.fc-event-title` contient « Arrivée avancée » + `rechargerEtRelirePlanning` re-assert (garde `saisieServeurObsolete`) ; statut validé = revalidation API idempotente. **Sélection de contrat robuste** : `workers=1` + ordre de déclaration → le nouveau test s'exécute APRÈS l'idempotence (qui valide `.first()`), cible le contrat crèche encore `A_VALIDER` via `GET a-valider` filtré `CRECHE_PSU` (jamais le même → 0 conflit sur pile partagée). Bloc du contrat scopé depuis `<h5>` via xpath `ancestor::div[.//ul[contains(@class,"jours-liste")]]` `.last()`. Vérif : `nx run web:lint` 0 erreur/0 nouveau warning (e2e lintés SANS règles type-aware, eslint.config ~L175 `projectService:false`) ; `playwright test -c playwright.stack.config.ts --list` = 2 tests découverts, nouveau après l'idempotence ; e2e-stack CI (job `pnpm e2e:stack`, `needs: affected-images` → web affecté par le spec) = vérif bout-en-bout. **⚠️ Piège worktree ce run** : le worktree `nifty-jackson-350eee` était un **répertoire vide NON enregistré** (`git worktree list` ne le liste pas ; `git rev-parse --show-toplevel` → clone principal) → toute l'arbo réelle + node_modules vivent dans le **clone principal `creche-planner-public`** (branche `claude/interesting-cori-c3e175` = tip main) : bosser là (préfixer chemins), `Set-Location` vers le clone pour nx/pnpm. Docker Desktop était éteint (`docker version` → daemon down) — lancé via `Start-Process`, lent à stabiliser.

Lot 4 livré (langage parent — zéro jargon dans le parcours) : nouvelle fonction pure **`libelleSemaineFr`** (`libs/shared/semaine/src/lib/libelle-semaine.ts` + spec + export index) → `« semaine du 29 juin au 5 juillet 2026 »` (jours via `joursDeLaSemaine`, mois via `Intl.DateTimeFormat('fr-FR', {timeZone:'UTC'})`, mois de début seulement si chevauchement, **année de fin TOUJOURS** — A8 ; c'est un pendant serveur DISTINCT du `libelleSemaine` web de `utils/dates.ts` qui, lui, n'affiche l'année qu'à cheval sur 2 ans). svc-notifications : `recapMardi` (sujet `Valider le planning — <libellé>` + texte du lien distinct du sujet `Valider le planning de la <libellé>` + corps), `brouillonService` (sujet+corps), `inbox.message` (sujet+corps) ; le `lien`/URL garde l'ISO. web : `RelectureEnvoi.descriptionJour` via nouvel helper **`dateLongueFr`** (`utils/dates.ts`, `Intl` weekday/day/month en UTC, quantième « 1 » pas « 1er ») → `« mardi 1 juillet — modifiée / — journée retirée / — horaires ajustés »` (ajustements lus défensivement sur `apres` typé `unknown`). **⚠️ Piège du plan (slip) : le plan écrit « 2026-W27 → 6 au 12 juillet 2026 » mais l'ISO réel 2026-W27 = 29 juin→5 juillet (le « 6 au 12 juillet » = 2026-W28)** ; les 2 exemples du plan sont 2 semaines distinctes → appliquer le FORMAT et calculer l'ISO au cas par cas ; les fixtures `2026-W27` affichent « 29 juin au 5 juillet 2026 » (cohérent avec le `libelleSemaine` web déjà testé). **Piège coverage 100 % shared-semaine** : le garde défensif `if (lundi===undefined…) return` est INatteignable (joursDeLaSemaine renvoie toujours 7) → 1 ligne non couverte ; lint interdit `as string` (`non-nullable-type-assertion-style` = ERROR) et déconseille `!` (`no-non-null-assertion` = warning) → résolu par **`String(jours[0])`/`String(jours[6])`** (normalise l'index optionnel sans assertion, même idiome que `Number(m[1])` de `parseSemaineIso`). Fichiers hors liste du plan mais mis à jour car assertant l'ancien libellé : `scheduler.hebdo.spec` (sujet mail + sujet in-app), `inbox.service.spec` (fixture), web `ClocheNotifications/EncartValidation/EditeurSemaine.test` (sujets mockés). api-gateway laissé tel quel (hors périmètre verif + matchers pact `string()` type-only → 0 drift). Les `e2e/*.stack.e2e.spec.ts` n'assertent AUCUN des libellés changés (rien à toucher). Vérif : `nx run-many -t build -p shared-semaine` puis `lint typecheck test -p shared-semaine svc-notifications web` verts (shared-semaine 100 % couverture, 0 erreur lint ; svc-notif 149, web 586).

Lot 3 livré (le mail du mardi part toujours — statut persisté + reprise, svc-notifications) : **nouvelle table `envoi_recap_hebdo`** (PK `(foyer_id, semaine_iso)`, `statut` A_ENVOYER|ENVOYE|DRY_RUN|ECHEC, `destinataires` jsonb, `message_id`/`erreur`/`envoye_le`/timestamps ; sur le modèle `envoi_etablissement`), migration **`0014_envoi_recap_hebdo.sql`** (générée par `drizzle-kit generate --name` → SQL + snapshot + journal ; additive, rollback `DROP TABLE`). Scheduler `scheduler.hebdo.ts` **découplé en 2 phases** : _création_ (mardi ≥ heure, `estJourCreation`) = notifier + `reserver` slot A_ENVOYER par foyer (`onConflictDoNothing`) ; _envoi_ (chaque tick de la fenêtre `estFenetreEnvoi` = mardi≥heure OU mer→dim, jamais lundi, réutilise `jourEtHeureParis`) = `aRetenter` slots A_ENVOYER/ECHEC, **reconstruit depuis données courantes** (`envoyerRecapFoyer(ligne, contratsFoyer, annuaire)`), envoie, transitionne par **compare-and-set `WHERE statut <> 'ENVOYE'`** (`marquerAbouti`/`marquerEchec`). In-app déplacé **après** l'envoi abouti (jamais dupliqué au retry). Foyer sans contrat actif → ENVOYE sans mail. SQL isolé dans **`EnvoiRecapService`** (nouveau fichier `scheduler/envoi-recap.service.ts`, ajouté aux providers du `SchedulerModule` — écart assumé vs liste de fichiers du plan, noté dans la PR) car le double mocké du scheduler ne peut pas exercer le CAS → spec dédiée `envoi-recap.service.spec.ts` (fake db capturant les appels drizzle + `PgDialect().sqlToQuery()` pour asserter le filtre de reprise et la garde `<> ENVOYE`). Test central `scheduler.hebdo.spec.ts` (double `EnvoiRecapService` = journal en mémoire fidèle) : échec 1er tick → succès 2e = A_ENVOYER→ECHEC→ENVOYE, 1 mail, 1 in-app. A7 : aucun postgres-exporter custom sur ces tables → rien ajouté. Vérif `nx run-many -t lint typecheck test -p svc-notifications` = 149 tests OK. **⚠️ piège environnement ce run** : le clone `-public` (clone principal) avait ses **symlinks workspace `node_modules/@creche-planner/*` entièrement disparus** → `contracts-notifications` ne résolvait plus `contracts-kernel` (build TS2307, faux échec sans rapport avec le code) ; `pnpm install` (frozen) disait « Already up to date » sans relinker → **`corepack pnpm@10.34.2 install --force` depuis PowerShell** a recréé les liens (per-package `libs/*/node_modules/@creche-planner/…`). Cohérent avec la leçon Lot 2b : deps via PowerShell, jamais Bash.

Lot 2b livré (UX heures réelles, 100 % web, A1 = `CRECHE_PSU` seul) : `types/bff.ts` `AjustementJour` + **`SaisieJourBesoins.ajustements` REQUIS** (pas optionnel : le BFF le renvoie toujours ; a fallu ajouter `ajustements: []` à toutes les fixtures SaisieJourBesoins des tests) + `EcrireSemaineBesoins`/`EcrirePlanning.ajustements` optionnels. Fonction pure **`classerAjustement`** (etatJourGarde.ts, à côté de `classerAbsence` conservé) : Arrivée avancée/retardée, Départ avancé/retardé, Horaires ajustés + `presence` toujours string. Modale `EditeurContratSemaine.tsx` : radio Absence/Jour-ajouté supprimé ; jour gardé = heures arrivée/départ préremplies (`semaineType[jour][0]`) + case « Absent toute la journée » + ligne `aria-live` (helper module pur `etatDeduitAjustement`+`formaterDuree` « 1 h »/« 1 h 30 »/« 45 min », textes au mot près) ; jour non gardé = jour ajouté implicite ; écriture entrée `ajustements` (préavis/cert à 0/false si extension pure) ; résumé `« Arrivée avancée 08:00–16:30 »`. `besoinsSemaine.ts` init/versCorps étendus. Dashboard `jourFoyer.ts`+`DashboardJourPage.tsx` : jetons `arrivee-avancee`/`depart-retarde` ajoutés, pastille `--ambre`. **Découverte hors plan (data-loss)** : `PUT .../plannings/:mois` = **remplacement complet** (`onConflictDoUpdate set:{saisie}`, planification.service ~610) et `CalendrierCreche` ne relisait que absences/joursSup → un ajustement posé côté hebdo aurait disparu à la 1re édition du mois → **`CalendrierCreche` PRÉSERVE** ajustements (réhydratation + réémission dans le corps, dedup A3 vs absences/joursSup) + affiche ambre lecture seule (édition réservée à l'éditeur hebdo). Spec e2e `validation-semaine.stack.e2e.spec.ts` (créée par contrats #165, PAS lot 5) réf le radio « Jour ajouté » supprimé → alignée (mardi non gardé = jour ajouté sans radio). Vérif : `nx run-many -t lint typecheck test -p web` vert (582 tests, coverage OK). ⚠️ **piège worktree majeur ce run** : le worktree `nifty-jackson-350eee` était CASSÉ (pas de `.git`, git résolvait vers le clone principal) → recréé un worktree propre `.claude/worktrees/lot-2b` from `origin/main` ; le `pnpm install` lancé via l'outil **Bash (Git Bash)** a créé des **symlinks POSIX absolus `/c/Users/...`** que Node Windows natif ne suit pas (`Cannot find module 'jsonc-parser'`) → il a fallu **supprimer node_modules et refaire `pnpm install` depuis PowerShell** (symlinks Windows corrects). Leçon : installer les deps via PowerShell, pas Bash.

Lot 2a livré (catégorie datée « ajustements » = heures réelles, 0 migration, saisie jsonb) : `CATEGORIES_DATEES += 'ajustements'` (fenetre.ts) + `jourVide` ; schéma Zod `ajustementSchema` (plage réelle + `preavisJours` défaut 0 / `certificatMaladie` défaut false + `refine` fin>début) sur `EcrirePlanningDto`/`EcrireSemaineDto` ; `Pick` `BesoinsSemaine` étendu. Domaine (`contrat-creche.ts`) : `AjustementCreche` (présence = `PlageHoraire`), `SemaineType.plagesJour`, `ecartAjustement` (extension hors plage → complément ; réduction plage non couverte → déduction SI `estDeductible` **réutilisé** ; multi-plages OK), erreurs FR `AjustementJourNonGardeError` (A2) / `SaisieJourEnConflitError` (A3), no-op si plage=contrat, INV-05 garanti ; pont JSON `generation-prestations.ts` (forme prestations INCHANGÉE). BFF `semaine-besoins` expose `ajustements` (défaut []). Récap établ. `brouillonService` : ligne « présence HH:MM–HH:MM ». **Découverte importante non prévue au plan** : `memeJour` (validation.diff.ts) comparait par `JSON.stringify` brut → un snapshot historique figé SANS la clé `ajustements` aurait faussement diffé contre un snapshot recalculé (5 clés) → faux « validée avec modifs » + mail parasite crèche ; **corrigé** en canonicalisant par `CATEGORIES_DATEES` (catégorie absente ≡ []) + `resumeJour` défensif (`?? []`) pour les `delta_modifs` historiques. Pièges : coverage domaine **ratchet 100 %** (couvrir les branches `!estDansPeriode`/`fin>debut` faux (multi-plages)/absence sans date) ; `presta` helper des specs passe options via `{}` non typé (spread ok) ; pacts byte-identiques (champs optionnels) → `can-i-deploy` inchangé ; api-gateway:test **flaky** (e2e parcours 72s) — relancer.

Lot 1 livré (réparer l'entrée du parcours) : lien mail `scheduler.hebdo.ts:267` → `${appUrl}/foyers/${foyerId}/planning?semaine=` ; `PlanningPage` lit `?semaine` (regex `^\d{4}-W\d{2}$`, rejet silencieux) → prop `semaineInitiale` à `EncartValidation` qui auto-ouvre l'éditeur + `scrollIntoView` 1× ; colonne `notification.lien` varchar(300) nullable (migration svc-notif **0013_notification_lien.sql**, rollback DROP COLUMN) portée par `messageValidationHebdo({foyerId,...})` (chemin **relatif**) → BFF passthrough → cloche carte tapable `<Link>` + marquage lu fire-and-forget + panneau fermé à la navigation. Pièges rencontrés : `exactOptionalPropertyTypes` → prop `semaineInitiale?: string | undefined` (pas `?: string`) ; jsdom sans `scrollIntoView` → stub no-op dans `test-setup.ts` ; type web `NotificationInApp` **dérivé OpenAPI** (`gateway.openapi.ts` → `scripts/generate-openapi-types.mjs` → `openapi-types.gen.ts` commité) donc ajouter le champ = éditer l'OpenAPI **optionnel** (hors `required`) puis régénérer ; **aucun** pact ne couvre `/moi/notifications` (inbox ajoutée sans pact en PR6) → champ optionnel = pacts byte-identiques ; svc-notifications/api-gateway n'ont **pas** de target `pact` (les `*.pact.spec.ts` tournent dans `test`, provider verif skip local sans Postgres).

Découvertes d'audit clés (vérifiées dans le code) :

- **Lien du mail du mardi CASSÉ** : `scheduler.hebdo.ts:267` construit `${FOYER_URL}/planning?semaine=…` mais la route n'existe pas (tout vit sous `/foyers/:foyerId/planning`) → PageIntrouvable ; `?semaine` n'est lu nulle part.
- **Perte définitive du mail du mardi sur échec SMTP** : `notification_hebdo` (UNIQUE) est insérée avant l'envoi ; au tick suivant « déjà notifié » = no-op → jamais renvoyé, aucun statut persisté (contrairement à `envoi_etablissement`).
- **« Déposé plus tôt que le contrat » irreprésentable** : le DTO semaine ne connaît que absences/joursSupplementaires/exceptions/joursAlsh ; seul réceptacle = `complementMinutes` (scalaire mensuel, non daté). D'où le lot 2a : nouvelle catégorie datée `ajustements` (plage de présence RÉELLE + preavisJours/certificatMaladie).
- Levier d'architecture : `CATEGORIES_DATEES` (libs/shared/semaine/fenetre.ts) pilote à la fois `fusionnerSemaineDansMois` (svc-planification) et `extraireSemaine`/diff de validation (svc-notifications) → ajouter `'ajustements'` propage fusion + deltaModifs automatiquement. Saisie en jsonb → **zéro migration** côté planification ; tarification parse en Zod passthrough → tolérant.
- Faux positif à ne pas re-signaler : `EtablissementModifie` EST émis (etablissement.service.ts) — un rapport d'exploration l'avait dit manquant.

Décisions PO : extensions facturées en complément **et annoncées dans la modale** ; réductions = mêmes questions préavis/certificat que les absences (même règle d'éligibilité domaine).

Lots (1 PR chacun) : 1 entrée du parcours (lien mail `/foyers/:id/planning?semaine=` + `?semaine` auto-ouvre l'éditeur + colonne `lien` in-app, migration svc-notif 0013) ; 2a domaine/API ajustements ; 2b UX heures réelles (modale crèche préremplie contrat, état déduit aria-live) ; 3 table `envoi_recap_hebdo` + reprise d'envoi ; 4 langage parent (`libelleSemaineFr` dans shared-semaine) — délégable Sonnet 5 ; 5 e2e stack `validation-semaine.stack.e2e.spec.ts`. Ordre : 1 ∥ 2a ∥ 3, puis 2b, puis 4, puis 5.

Voir aussi [[feature-notifications-planning]], [[feature-edition-hebdo-besoins]], [[feature-client-api-retry-timeout]] (pièges e2e reload/#171), [[feature-contrat-enfant-id]] (NOT NULL enfant_id hors périmètre).
