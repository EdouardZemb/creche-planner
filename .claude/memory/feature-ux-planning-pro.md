---
name: feature-ux-planning-pro
description: "Chantier UX « prototype → produit pro » de l'écran Planning/validation semaine — audit + 5 lots, avancement"
metadata:
  node_type: memory
  type: project
  originSessionId: 02b03ece-dc11-441f-a8d9-ac81e8262470
---

Mission (prompt utilisateur `.claude/prompts/`) : élever les features existantes au niveau « produit professionnel » côté parent mobile, un écran à la fois, questions AVANT d'agir, audit → feu vert → lots vérifiables. Écran choisi n°1 : **Planning / validation de semaine** (feu vert utilisateur 2026-07-03, parcours + polish, tout améliorable, défauts pris : 0 nouvelle dépendance, 0 changement métier, a11y exigence).

**✅ CHANTIER COMPLET 5/5 (2026-07-04) — tous les lots mergés main.**

**Plan 5 lots validé** :

1. Fiabiliser le moment critique « la crèche est-elle prévenue ? » — ✅ **PR [#152](https://github.com/EdouardZemb/creche-planner/pull/152) MERGÉE main `60fc93e` (2026-07-03, squash, auto-merge après update-branch — piège : force-push refusé par le classifier, passer par `gh pr update-branch`)** : message « dernière étape », autofocus section RelectureEnvoi (titre « Dernière étape : prévenir les services », bordure ambre), encart persistant après dernière validation (« Plus rien à valider »), erreurs en `.debit`+role=alert (avant : VERTES via `.credit` !) + « Réessayer », fix bug statut ECHEC qui verrouillait le bouton sur « Envoyé ».
2. Parler parent — ✅ **PR [#153](https://github.com/EdouardZemb/creche-planner/pull/153) MERGÉE main `c73236e` (2026-07-03, squash, CI 100% verte e2e-stack inclus, branche supprimée ; piège : `gh pr merge --delete-branch` échoue depuis un worktree — merger sans puis `git push origin --delete`)** : `libelleSemaine()` factorisée `apps/web/src/utils/dates.ts` (« semaine du 6 au 12 juillet », mois/année seulement si chevauchement, « 1er », repli chaîne brute) ; `LIBELLES_MODE.CRECHE_PSU` → « Crèche » (source unique, test UT-08 du panneau coût rebasculé sur ALSH) ; bandeau « Mode test » (+modale/bouton/« Test réussi ») ; « Temps de garde en plus (minutes) » (nbsp avant « : » sinon il retombe seul à 375 px) ; « Signalée combien de jours à l'avance ? » ×3 ; e2e stack : onglet « Crèche ». Piège vérif : `email_service` à poser AUSSI dans le read model `etablissement` de la BDD **notifications** (l'UPDATE SQL direct côté planification n'est pas projeté par NATS) ; piège capture : `preview_screenshot` timeout systématique → captures via script Playwright posé dans `apps/web/` (résolution module)
3. Feedback d'enregistrement stable — ✅ **PR [#154](https://github.com/EdouardZemb/creche-planner/pull/154) MERGÉE main `4d99d5e` (2026-07-03, squash sur demande utilisateur — le classifier avait refusé le self-merge —, branche supprimée)** : `usePlanning`/`useEcritureSemaine` → « enregistre » PERSISTE (plus de retour idle 2 s) + `enregistreA` (« Enregistré à 21:43 », `formaterHeureFr` pur dans utils/dates) + `reessayer()` (rejoue la dernière écriture) ; `StatutSauvegarde` 4 états dont « Enregistrement… » (couvre le trou modale→debounce 800 ms), UNE seule région role=status → annonces aux seules transitions ; bouton « Réessayer » dans `BarreStatutCalendrier` + `EditeurContratSemaine` (l'`etatStatut` réduit a disparu des deux) ; `.statut-en-cours` (gris) dans styles.css ; tests hooks en fake timers + `setSystemTime` (horodatage déterministe), asserts « Enregistré » → regex `/Enregistré à \d{2}:\d{2}/`. Vérifié 375 px sur stack locale (rafale 3 jours stable ; stop svc-planification → erreur rouge + Réessayer OK après restart). Piège vérif : après le seed, 1er hit Vite/gateway peut renvoyer des 502 transitoires (cold start) — recharger.
4. Sécuriser « Tous les mois (modifie le contrat) » — ✅ **PR [#155](https://github.com/EdouardZemb/creche-planner/pull/155) MERGÉE main `91e5983` (2026-07-03, squash sur demande utilisateur, CI 100% verte e2e-stack inclus, branche supprimée)** : `ChoixPortee` libellés parent (« Seulement cette fois » / « Toutes les semaines, durablement (modifie le contrat) », option durable = liseré ambre + description `aria-describedby`) ; `ModaleContratDurable` = message mode-spécifique concret (construit dans les calendriers : « Tous les mardis deviendront des jours de garde, de 09:00 à 16:30 » / « la cantine ne sera plus réservée »…) + bloc commun (effet hebdo durable + saisies du mois effacées) via `children` de `ModaleConfirmation` ; **découverte : `btn danger` n'avait AUCUNE règle CSS** (bouton « destructif » bleu depuis toujours) → règle rouge ajoutée styles.css ; `useCalendrierContrat.succesDurable` = confirmation visible persistante horodatée (« Contrat modifié à HH:MM… »), affichée par `BarreStatutCalendrier` SANS role status (l'annonce SR passe par la région AQ-05 existante, sinon double annonce) ; e2e stack : radios assertés → `/Toutes les semaines/`. Vérifié 375 px stack locale (ajout+retrait durables crèche, message cantine, revert seedé, `seed-demo.mjs --verify` OK).
5. Polish mobile encart/modales — ✅ **PR [#156](https://github.com/EdouardZemb/creche-planner/pull/156) MERGÉE main `00d655f` (2026-07-04, squash sur demande utilisateur — classifier avait refusé le self-merge comme lots 3/4 —, CI 100% verte e2e-stack inclus, branche supprimée)** : placeholder chargement `EncartValidation` (spinner + « Vérification des semaines à valider… », `aria-busy`, même gabarit → pas de saut de layout) ; `.encart-ligne`/`.encart-actions` styles.css (boutons empilés pleine largeur <480px, côte à côte au-delà) ; **`--gris` #6b7280 → #4b5563** (gray-600 : l'ancien ne faisait que 4,4:1 sur `--gris-clair` — échec AA badges/statuts ; `--vert` 4,56:1 et `--rouge` 5,88:1 passaient déjà) ; aria-label distinct enfant/mode sur les « Valider » d'`EditeurContratSemaine` (friction repérée au lot 1, tests MAJ : nom accessible = « Valider la semaine du 29 juin au 5 juillet — Léa, Crèche ») ; 320/360/375 px sans débordement (gardes existantes suffisantes). Vérifié stack locale + axe 7/7 0 violation. Piège vérif : `preview_click` par sélecteur peut rater sa cible pendant un re-render (a cliqué « Éditer » au lieu de « Valider ») → cliquer via `preview_eval` `el.click()` ; état « chargement » capturable en gelant le gateway (`docker pause creche-planner-api-gateway-1`).

**Hors périmètre assumé** (itération suivante) : mode sombre, refonte édition 7 jours.

Audit complet (parcours mardi soir, grille, frictions classées) posté en session 2026-07-03. Frictions notables restantes : « Complément (min) » sans explication, 2 boutons « Valider » sans aria-label distinct dans EditeurSemaine (repéré en vérif), absence par défaut 09:00–16:30 au lieu de la plage de garde réelle.

Vérification live : recette [[verif-ui-locale-stack]] ; pour créer une semaine à valider : INSERT `notification_hebdo` (snapshot `'{}'`, statut A_VALIDER) + UPDATE `etablissement.email_service` (NULL dans le seed → établissement « non routable », brouillon vide). DRY-RUN actif en compose local.

Lié : [[feature-notifications-planning]], [[feature-edition-hebdo-besoins]]
