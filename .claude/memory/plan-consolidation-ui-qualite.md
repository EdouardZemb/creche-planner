---
name: plan-consolidation-ui-qualite
description: Audit 4 volets (UI/dev/code/infra) 2026-07-29 + plan A-D dans .claude/plans/consolidation-ui-et-qualite.md — chantier A LANCÉ 2026-07-29
metadata:
  node_type: memory
  type: project
  originSessionId: 1e7fd81f-b5ec-48c8-8639-c9375d4e6bd8
  modified: 2026-08-01T17:18:11.707Z
---

Audit complet mené le 2026-07-29 (4 sous-agents : UI, frictions dev, qualité code, infra). Plan consolidé : `.claude/plans/consolidation-ui-et-qualite.md` — **brouillon, aucun lot lancé, à valider PO**.

4 chantiers : **A** risques prod urgents (A1 = base `notifications` ABSENTE des backups + zéro hors-site ; A2 = cosign vérifié gateway-only au deploy ; A5 = authz enforce → recoupe [[plan-fondations-backend]]), **B** encoder les pièges dans l'outillage (doctor, `dependsOn` typecheck/build, readiness chaîne gateway = cause racine flaky seed 502/503), **C** consolidation UI (primitives Bouton/Champ/Badge → découpe App.tsx → tokens styles.css → factorisation des 2 calendriers jumeaux ~2300 l. EN DERNIER), **D** qualité code (appelResilient partagé nest-commons, seuils couverture nest-commons/observability, ratchet ESLint 28 règles warn, DV-01→04 → recoupe [[plan-sfd-30-33-extension-famille]]).

**Why:** éviter de re-payer l'audit ; l'ordre A→B→C1-C4→D→C5 est motivé (risque données réelles d'abord, primitives avant grosse refonte).

**How to apply:** si le PO valide, exécuter chantier par chantier en PR indépendantes ; détails, chemins et mesures de succès dans le fichier de plan du repo.

**Pilotage chantier A lancé 2026-07-29 (session dédiée) :**

- 🔴 **INCIDENT découvert : `creche-backup.service` en échec `203/EXEC` depuis le 17 juin** — un commit du 17/06 a écrasé le bit exécutable de 9 scripts `.sh` (mode git `100644`) ; **zéro backup prod depuis 6 semaines**. Le clone serveur suit `main` → le correctif git (`update-index --chmod=+x`) répare la prod au prochain pull ; en attendant, chmod manuel côté user (mutations SSH prod bloquées par le classifieur en auto mode, sops -d aussi).
- A1 ✅ **#258 MERGÉE** (5/5 bases + bits x sur 9 scripts + doc) et A2 ✅ **#259 MERGÉE** (cosign 7/7 images — topologie réelle = 5 svc + gateway + web, pas 8 —, défaut on, fail-closed si services.json illisible) le 2026-07-29 ~16h UTC. Le clone prod récupérera les perms au prochain pull release-poll ; chmod manuel donné au user en attendant.
- A3 ✅ tranché : PO valide (2026-07-29) de GARDER seed idempotent (`.seed-demo-state.json`, foyer réf `be9ca4ab`) + perf-smoke lecture seule → **doc #260 MERGÉE** (encart Porte 3 doc 24 + `DEPLOY_SKIP_*` dans `.env.server.example`).
- A1b ✅ **#261 MERGÉE main `e181d94`** (2026-07-29 ~17h25 UTC, PAS ENCORE ACTIVE sur le serveur — arrive au prochain pull du clone prod) : `backup-offsite.sh` (tar dernier jeu → age clé publique de `.sops.yaml` → rclone, opt-in `OFFSITE_REMOTE`, idempotent, rétention cloud 90 j) accroché à `backup-cron.sh` ; cible retenue par le user = **Google Drive** (rclone scope `drive.file`, OAuth headless) ; restore-one **rejoué** sur dump prod foyer 17/06 en conteneur Compose jetable (`COMPOSE_PROJECT_NAME` + override `!reset` ports) + cycle age/rclone validé SHA-256 en alpine jetable ; doc §7/§9 sauvegardes.md. **Reste ops serveur (user)** : installer rclone, créer remote `gdrive-creche`, poser `OFFSITE_REMOTE` dans `.env.server`(+`.enc`), démarrer creche-backup.service. ⚠️ rclone/age hors PATH systemd → le script préfixe `~/.local/bin` lui-même.
- A4 ✅ **#262 mergée `4904240` + INSTALLÉ PROD 2026-07-29 19:48 CEST** (heartbeat externe dead man's switch) : pings « HEARTBEAT: ping envoyé. » OK, timer enabled 5 min, `/etc/creche-heartbeat.env` root 0600 (URL `https://hc-ping.com/<uuid-du-check-non-versionne>`). heartbeat.sh pingue seulement si `/api/health` 2xx ; `--cacert` jamais sur l'URL de ping (remplace le magasin CA). **Reste (user)** : ⚠️ vérifier Period=5/Grace=5 min sur Healthchecks.io (défaut 1 jour → détection 25 h) + test bout-en-bout (stop timer ~10 min → mail down, start → up). Pièges : merge PR + sudo serveur = bloqués classifieur/mot de passe → gestes user ; hooks husky en worktree → `corepack pnpm@10.34.2 install` requis avant commit. — `scripts/heartbeat.sh` (ping `HEARTBEAT_PING_URL` seulement si `<SERVER_ORIGIN>/api/health` 2xx ; `--cacert` optionnel santé uniquement, jamais sur l'URL de ping car `--cacert` REMPLACE le magasin CA) + `creche-heartbeat.{service,timer}` (5 min, `OnBootSec=90s`) ; secret dans `/etc/creche-heartbeat.env` root 0600 (PID 1 lit EnvironmentFile avant User=). **Reste (user)** : créer le check Healthchecks.io period 5/grace 5 + poser l'env + installer les unités + test chaîne (stop timer ~10 min → mail). Piège worktree : hooks husky → `corepack pnpm@10.34.2 install` requis dans le worktree avant commit.
- A6 ✅ **#263 MERGÉE main `e06bed7` 2026-07-29 ~18h31 UTC (CI 16/16, e2e-stack incl.) — PAS ENCORE EN PROD, effet au prochain train de release** — 5 volets en 1 PR : limites mem/cpus prod (docker stats 29/07, ~9,5 Gio plafonds ; + fix loki/promtail sans `restart:`), healthchecks `/api/health/live` 5 svc-* (compose base + HEALTHCHECK Dockerfile via `$PORT`), caddy v2.11.4/cloudflared 2026.7.3 épinglés digest de liste = images déjà en marche prod, nginx non-root (`nginx-unprivileged`, **port 80 conservé** — le mapping tunnel Cloudflare dashboard→`web:80` ne bouge pas ; bind 80 non-root OK, Docker pose `ip_unprivileged_port_start=0` en conteneur), en-têtes sécurité nginx.conf (CSP même-origine, `style-src 'unsafe-inline'` tant que C4 pas fait). Vérifs : 3 merges `compose config` OK + conteneur test local (uid 101, headers présents). Effet prod au prochain train de release seulement.
- A5 ✅ **#264 MERGÉE main `07fa175` 2026-07-29 18:43 UTC (squash, après 2 rebases — mains mouvante #263/#265)** — diagnostic + correctif : les 3 « SCOPE AURAIT REFUSÉ » (svc-foyer, 10 j) étaient des **faux positifs à 100 %** — appelant = api-gateway elle-même (BFF `/moi/profil` + `/moi/notifications` → `resoudreParentCourant` → `parents()`/`preferences()`), routes `/moi/*` sans `@FoyerScope` ⇒ `req.foyersAutorises` jamais posé ⇒ assertion parent propagée SANS liste `foyers` ⇒ `couvert()` (`?? []`) refuse le propre foyer du parent (vérifié prod : `b71c03d3` = vrai foyer d'edouard.zemb, parent principal `61e4710c`). Fix : source `@FoyerScope('identite')` (guard résout `foyersParEmail`, pose `foyersAutorises`, pas de contrôle d'appartenance ; fail-closed sur échec en enforce) sur les 4 routes `/moi/*` + réutilisation de la liste dans le contrôleur. **Décision fail-open `ScopeFoyerGuard` TRANCHÉE** (runbook §5) : conserver — « référence introuvable » est porteur (`GET /foyers` liste globale filtrée gateway, `POST /foyers` amorçage) et n'élargit rien (exploiter = détenir le secret HMAC = pouvoir forger une assertion machine). **Reste** : merge #264 → déploiement → ~1 sem de logs propres → bascule `INTERSERVICE_AUTHZ_ENFORCE=1` (action user explicite). NB : `FOYER_AUTHZ_ENFORCE=1` est DÉJÀ actif (gateway) ; la bascule restante est bien INTERSERVICE.
- A7 ✅ **#265 MERGÉE 2026-07-29 18:52 UTC (squash après rebase auto-merge)** (migrations observables) : jauge OTel `migrations_en_attente` (0/1) + compteur `migrations_echecs_total` dans `MigrationService` (modèle outbox.relay, no-op sans MeterProvider) ; alerte `MigrationsEnRetardPersistant` (critical, `for: 10m`, groupe `migrations` d'alerts.yml, promtool OK) ; readiness `GET /api/health` down tant que schéma en retard via `MigrationsHealthIndicator` (MigrationService exporté du DatabaseModule global) — **sans risque de restart** : healthchecks compose + blackbox ne sondent que la liveness `/api/health/live` ; 8 tests unitaires ajoutés (nest-commons 81/81). Piège TS : `exactOptionalPropertyTypes` → `erreur?: string | undefined` pour autoriser `= undefined`. Lien lot B3 (gateway ready avant amonts) : non traité ici.
- Chips créés : A1b (hors-site + restore test), A4 (heartbeat), A5 (investigation), A6 (durcissement), A7 (migrations boot).
- **🏁 CHANTIER A : 7/7 lots MERGÉS dans main le 2026-07-29** (#258→#265). Vérifié sur prod 29/07 ~19h : scripts exécutables, clone à `e06bed7`, heartbeat timer actif. **Reste post-chantier : (1) vérifier backup nightly 5/5 du 30/07 ; (2) ops user A1b (rclone+OFFSITE_REMOTE) et A4 (Period/Grace 5 min + test chaîne) ; (3) PROCHAIN TRAIN DE RELEASE = active A2/A5/A6/A7 en prod + DOIT inclure rejeu projection #257 ; (4) après déploiement : ~1 sem logs propres puis bascule `INTERSERVICE_AUTHZ_ENFORCE=1` (geste user).**

**Exécution 2026-08-01 — C0 + B1/B2/B6 :**

- **C0 ✅ PR #267** (branche `fix/nav-mobile-pages-globales-contrastes`) : barre d'onglets conservée sur les pages globales + contrastes a11y. Deux défauts trouvés en revue adversariale et corrigés dans le lot : (a) `Entete` ne regardait pas `moi.loading` → pendant le round-trip `/moi`, `PastilleAValider` émettait un GET scopé sur un foyer non autorisé → 403 → `gateway_authz_refus_total{decision="refuse"}` → **faux positif de l'alerte `AuthzGatewayRefus`** ; (b) focus clavier bleu-sur-bleu (1:1) dans l'en-tête sous 768 px (préexistant), fermé par `.app-header nav > a:focus-visible`.
- **B2 ✅** — découverte structurante : **les `targetDefaults` de `nx.json` ne s'appliquent QU'AUX cibles inférées par les plugins**. `web` (test + typecheck écrits à la main dans `apps/web/package.json`), `api-gateway` et les 5 `svc-*` les ignoraient → `nx test web` = 1 seule tâche, le `test → ^build` réputé « déjà encodé » ne couvrait PAS web. Corollaire : un `targetDefaults.typecheck` ÉCRASE le `dependsOn` inféré (`["build","^typecheck"]` → `["^build"]`) sur toutes les libs — ne pas en poser.
- 🔴 **EFFET DE BORD DE B2 découvert le 2026-08-01 (chip `task_b78dfa5c`) : course `dist/` entre `build` et `typecheck` sur les 6 projets backend.** `tsconfig.app.json` a `"outDir": "dist"` (tsc y émet ses `.d.ts`) ET `webpack.config` a `output.path = …/dist` avec `clean: true` (webpack l'efface) ; le `test.dependsOn = ["build","typecheck"]` posé par B2 ne met AUCUNE arête d'ordre entre les deux → Nx les lance en parallèle → `ENOTEMPTY: rmdir dist/src/bff` puis **cascade de ~30 `TS6305`** (une seule vraie erreur : le ENOTEMPTY). **Ne se déclenche qu'à cache FROID** — d'où l'invisibilité jusqu'au commit de release (le bump des 7 `package.json` invalide tout le cache). Correctif recommandé : `outDir` distinct `./out-tsc/app`, en suivant la convention déjà présente (`tsconfig.spec.json` = `./out-tsc/vitest`). **Le `gh run rerun --failed` est repassé VERT du premier coup** (run 30704874030, main vert sur `0d10b75`, tous les jobs avals joués) ⇒ course **non déterministe** : un rerun vert ne prouve RIEN sur la présence du défaut, seule l'analyse `outDir` × `clean:true` × absence d'arête d'ordre fait foi.
- **B1 ✅ `scripts/preflight.mjs` / `pnpm preflight`** — ⚠️ **le nom `doctor` est inutilisable** : `pnpm doctor` est une sous-commande native de pnpm et masquerait le script sans message.
- **🏁 LES DEUX MERGÉS dans `main` le 2026-08-01** : #268 (B1/B2/B6) = `1d47636`, #267 (C0) = `bb3948e`. **NON DÉPLOYÉS** → à embarquer dans R1, dont le plancher monte de `9aee291` à `bb3948e`.
- 🔴 **Piège machine découvert** : un `mock-gateway.mjs` orphelin d'une session Claude du 28/07 tenait le port **3999** depuis 4 jours → tous les tests provider Pact échouaient sur « provider non prêt après 40000 ms » sans nommer la cause. Les 5 providers utilisent 3995-3999. Process tué ; `pnpm preflight` le détecte désormais. Second mode d'échec, même message : **saturation machine** sous `--parallel=3` (231 s rien qu'en imports) → en local, `--parallel=1` pour un verdict fiable.
- ⚠️ **CodeQL est bloquant en CI** : la règle `js/file-system-race` (HIGH) frappe tout `existsSync`/`lstatSync` suivi d'un `readFileSync`/`statSync` sur le MÊME chemin. Pour tout nouveau `scripts/*.mjs`, écrire les accès en `try { lire() } catch {}` d'emblée.

**Exécution 2026-08-01 (suite) — C1/C2/C3/C4, ✅ **MERGÉS main `9171228` (#272) le 2026-08-01**, CI 15/15 verte, branche supprimée — **NON DÉPLOYÉS** :**

- **H4 TRANCHÉ PO** : C2 se fait AVANT le plan 31 (les SFD 31-33 sont des brouillons, rien en vol → zéro rebase). App.tsx 670 → **102 l.** (`layout/Entete|GardeFoyer|PageIntrouvable|titreDepuisPathname`, `session/Accueil`, `foyer/MesFoyersPage`).
- **C1** : 0 littéral `className="btn"` (100 sites → `<Bouton>`/`<BoutonLien>`). Trois écarts au plan trouvés par la reconnaissance : les boutons sont **100 et non 96**, dont **12 `<Link>`/`<a>`** (d'où 2 composants, pas une prop `as`) ; les champs forment **11 patrons et non 1** (un `<ChampFormulaire>` universel cassait 7 tests qui font `getElementById(aria-describedby)` → primitive en Fragment, aide OPT-IN, patron canonique seul) ; les badges = **2 migrations réelles sur 13 objets** (« pastille » = 3 familles distinctes : badge textuel, compteur positionné, swatch décoratif).
- **C3** : 53 littéraux couleur tokenisés, 22 tokens. Distinctions à NE JAMAIS refusionner : `--surface`/`--texte-inverse` (même #fff, rôles opposés), `--gris-sur-blanc` #6b7280 ≠ `--gris` #4b5563 (assombri exprès pour `--gris-clair`), `--rouge-fond-survol` #fef2f2 ≠ `--rouge-fond` #fee2e2. 6 blocs `@media 768px` → 3 (les 2 restants NON fusionnables : leurs règles de base sont déclarées entre le bloc et la cible). **Règle MORTE supprimée** : `.actions-export{width:auto}` était écrasée par la règle de base déclarée plus bas — une media query n'ajoute aucune spécificité.
- **C4** : 305 → **193** `style={{` (112 supprimés). Vocabulaire fermé : règle de base cases/radios (19), `.champ-large`, `.bloc-champs`, `.liste-nue`, `.rangee-actions`, échelle `.mt-0..5`/`.mb-1..5`.
- 🔧 **OUTIL VERSIONNÉ par #272** : `nx run web:e2e-visuel` (`apps/web/e2e/contrastes.visuel.e2e.spec.ts` + `playwright.visuel.config.ts`) puis `node scripts/comparer-empreinte.mjs avant.json apres.json` (variable `SORTIE=<chemin.json>` pour nommer le balayage). **Empreinte des styles CALCULÉS** (37 propriétés × ~4 590 nœuds × 13 routes × 2 viewports) comparée avant/après — c'est ce qui a prouvé C3 et C4 iso-rendu. Plus une **sonde négative** (3 défauts injectés) et une **garde anti-balayage-à-vide**. HORS CI par défaut (`nx e2e web` ne le ramasse pas). **À utiliser systématiquement pour C5 et toute refonte CSS.**
- 🔴 **PIÈGE MAJEUR, découvert à mes dépens** : un balayage Playwright avec `waitUntil:'domcontentloaded'` + délai fixe mesurait `#root` VIDE sur les 26 pages → « 0 constat », indiscernable d'un succès. Cause seconde : un mock `/notifications/a-valider` renvoyant `{semaines:[]}` au lieu d'un tableau faisait tomber tout l'arbre React (aucun error boundary). **Toute mesure de page doit attendre le montage réel ET refuser de conclure si elle n'a rien mesuré.**
- ⚠️ L'empreinte est mesurée à l'ÉCRAN : elle ne couvre pas `@media print` (un agent a refusé une migration `.carte`/`.mb-5` pour ce motif).

**🏁 CHANTIER R — R1 EXÉCUTÉ 2026-08-01 : train n°16 → `0.15.0` DÉPLOYÉ EN PROD** (13 commits `fa585b6..9171228`, commit de release `0d10b75`, 7 tags, `release.yml` ×7 verts du 1er coup, **déploiement 100 % automatique par le poller**, cosign 7/7, p95 274 ms, Deployment #5705660994, 0 rollback). Détail dans [[prod-deployment-facts]].

- **✅ R1 CLOS : 6/6 critères done** — dont le rejeu #257 joué le 2026-08-01 (`UPDATE grille_abcm SET version_payload = 1;` + `docker restart creche-planner-svc-referentiel-1`) → `grille_tarifaire` = **9 lignes / 3 modes**, 0 nouveau dead-letter, **repli REST éteint** (1 appel/10 min contre 109/15 min). ⚠️ **Les mutations SSH prod sont REFUSÉES par le classifieur en auto mode** : préparer le geste (préconditions vérifiées en lecture seule + commande copiable) et le passer au PO — c'est le schéma qui marche.
- **Découverte réutilisable** : `deploy.mjs` recrée AUSSI la pile d'observabilité (prometheus, caddy, cloudflared, loki, promtail) → un changement d'`alerts.yml` embarqué dans un train se charge TOUT SEUL (8 groupes/21 règles constatés). `remote-apply-observability.ps1` n'est nécessaire que **hors** train de release.
- **Méthode qui a payé (à refaire) :** `docker compose config` à blanc sur le serveur avec le futur `IMAGE_TAG` AVANT de couper la release — prouve en lecture seule qu'aucune variable obligatoire ne manque (le piège `0.13.0`/`0.12.0`).
- ⚠️ **Piège outillage de cette session** : `jq` n'existe PAS dans le Git Bash de ce poste — un `Monitor` qui l'utilise tourne en silence 25 min sans rien évaluer (silence indiscernable du succès). Utiliser `gh ... --jq` (jq intégré à gh).
- **R2 (c) SOLDÉ** : sauvegardes nightly **5/5 bases, 3 nuits d'affilée** (30/07, 31/07, 01/08) dans `$HOME/backups/creche/<horodatage>/` — **PAS** dans `<repo>/backups` (défaut `BACKUP_DIR=$HOME/backups/creche`, `backup-cron.sh:29`). **R2 (a) TOUJOURS OUVERT** : `rclone` ABSENT du serveur → hors-site inactif.

**Exécution 2026-08-02 (session DISTANTE, vacances) — D1/D2/D3, branche `claude/plan-implementation-71rkuk` :**

- **D1 — client HTTP partagé.** **Maison cible TRANCHÉE : étendre `libs/resilience`** (`src/lib/appel-http.ts`), pas descendre dans `nest-commons` — elle porte déjà les briques, elle est `context:shared` (donc atteignable par la gateway, dont le `depConstraints` n'autorise que `context:gateway` + `context:shared`) et n'a aucun couplage sécurité. 3 enveloppes : `appelHttpResilient` (propagation), `appelHttpOuRepli` (dégradation), `executerAppelHttp` (one-shot sans retry — `desabonner`, jeton brûlé au 1ᵉʳ succès). **Les 2 mécanismes de sécurité restent séparés par CONSTRUCTION** : la lib ne connaît qu'un `FournisseurEntetes` injecté. 10 fichiers migrés, `fetch` brut résorbé, pacts identiques.
- 🔴 **PIÈGE RÉUTILISABLE (D1) : déplacer du code COUVERT vers une lib fait BAISSER le ratio de couverture du projet source.** `svc-tarification` est tombé sous son seuil de 80 % de branches (79,81 %) alors qu'AUCUN test n'avait été retiré — le numérateur et le dénominateur perdent les mêmes lignes, mais elles étaient mieux couvertes que la moyenne. Ne pas baisser le seuil (c'est l'inverse du ratchet) : compenser par un vrai test. À l'inverse la couverture de l'api-gateway MONTE (72,75 → 78,80 %) : le boilerplate supprimé y était majoritairement NON couvert. Mesurer les deux côtés avant de conclure.
- ⚠️ **Piège D1** : `svc-planification` ne dépendait pas de `@creche-planner/resilience` → `workspace:*` à ajouter dans son `package.json` (aucun `references` dans les tsconfig : la résolution passe par les liens pnpm, donc `TS2307` tant que le lien n'existe pas).
- **D2 — le défaut était pire que « pas de seuil »** : `nest-commons` avait `coverage.enabled: false` et `observability` n'avait ni test ni cible `test` ⇒ **aucune des deux n'émettait de `coverage-summary.json`**, donc toutes deux étaient **invisibles au ratchet CI d'AQ-06**, pas seulement à un seuil local. `observability` 0 → 100 % (11 tests) ; `nest-commons` 76,66 → 83,33 % (seuils au mesuré arrondi bas). ⚠️ **Créer une cible de test sur une lib qui n'en avait pas demande 4 fichiers**, pas un : `vitest.config.mts`, `tsconfig.spec.json`, l'**exclusion des specs de `tsconfig.lib.json`** ET l'ajout de `vitest.config.*` aux `ignoredFiles` de `@nx/dependency-checks` dans l'`eslint.config.mjs` de la lib — sans les deux derniers, `lint` échoue sur « uses vitest, missing from dependencies ».
- **D3 — ratchet ESLint relancé : 10 règles en `error`, 684 → 658 warnings.** ⚠️ **L'énoncé du plan se trompait : `require-await` n'est PAS quasi propre (196 occurrences)**. **Méthode à réutiliser : une règle ne monte en `error` qu'à ZÉRO occurrence** ; 6 des 10 l'étaient déjà — le code était propre, seul le ratchet ne l'avait jamais acté (chercher systématiquement les règles `warn` à 0 avant de coder quoi que ce soit : promotion gratuite). `no-unnecessary-condition` 15 → 8 sans promotion : les 8 occurrences de specs venaient de casts `as` qui MENTAIENT (`| undefined` masqué sur `mock.calls[0]`) ; **les 7 restantes portent du comportement dans les composants web → à embarquer dans C5**, qui rouvre ces fichiers.
- 🔧 **OUTIL VERSIONNÉ par D3** : `.github/workflows/scripts/lint-warnings.mjs` + `.github/workflows/lint-baseline.json`, step bloquant du job `ci`. Lint du dépôt **ENTIER** (hors `affected` : un total ne se compare que sur un périmètre constant), échec si le total remonte OU si une erreur apparaît, tableau par règle dans le résumé de run **signalant les règles tombées à zéro** (= promouvables). Baseline **versionnée** et non artefact roulant : la faire monter exige un diff visible en revue. Sonde négative jouée. ⚠️ A exigé d'ajouter `out-tsc` aux `ignores` de la config ESLint **racine** — sans quoi un `eslint .` racine sort ~280 erreurs « not found by the project service » sur les `.d.ts` de `tsc --build` (chaque config de projet les ignorait déjà, d'où l'angle mort).

**Exécution 2026-08-03 (session DISTANTE) — C5, branche `claude/implementation-du-plan-0isdve` :**

- 🔴 **L'ÉNONCÉ DU PLAN ÉTAIT PÉRIMÉ : les « mêmes briques » des deux calendriers étaient DÉJÀ partagées.** `CalendrierMois`, `LegendePlanning`, `ChoixPortee`, `BarreStatutCalendrier`, `ModaleContratDurable` sont communs, et `useCalendrierContrat` factorisait déjà toute l'enveloppe comportementale. Le duplicat réel était **l'assemblage et la présentation**, jamais la logique. Un socle + « stratégies » aurait fait transiter état, évènements, modales, listes et payloads par l'abstraction — soit tout. **Décision : extraire l'assemblage commun, laisser les règles de mode dans leur mode.** Résultat : `CalendrierCreche` 1307 → 517 l., `CalendrierAbcm` 942 → 547 l., `EditeurContratSemaine` 942 → 415 l. → **plus aucun composant web > 700 l.**
- **Briques nées du lot** : `SocleCalendrier`, `ListeJoursClavier` (3 listes au même balisage), `PiedModaleCalendrier` (3 pieds), `evenementJour`. Modules purs : `inscriptionsAbcm` + `evenementsAbcm`, `etatsJoursCreche`, `ajustementDeduit`, `indexBesoins`. Hook `useSaisieCreche` qui **enveloppe** `useCalendrierContrat` (réinitialisation d'après-PUT et écriture se referment l'une sur l'autre — les tenir côte à côte obligeait le composant à faire le chaînage).
- **Écarts assumés** : les ~9 libellés de case à cocher et les 2 blocs radios « type d'absence » n'ont PAS été unifiés — leurs styles inline diffèrent sur 5 axes chacun, un composant commun aurait exigé autant de props de style (gabarit paramétré ≠ abstraction), et C4 avait fermé ce vocabulaire à dessein. Le pied de `ModaleJourSemaine` ne passe pas par `PiedModaleCalendrier` (`mt-4` vs marge inline → classes rendues différentes pour rien).
- ⚠️ **L'énoncé de D3 se trompait sur la LOCALISATION des `no-unnecessary-condition`** : les 7 (en fait 6) n'étaient PAS dans les calendriers que C5 rouvre, mais dans `EtablissementsPage`, `ContratForm` (×3), `editeursSemaine`, `ui/Modale`. Elles ne venaient donc pas « gratuitement » avec le lot. Soldées : 8 → 2.
- 🔴 **ANGLE MORT DU RATCHET REACT-HOOKS, même nature que celui de D2** : les règles ne s'appliquaient qu'aux `**/*.tsx`, or **les hooks du dépôt n'ont pas de JSX et vivent en `.ts`** → `useAsync`, `useFoyer`, `useSaisieServeur` portaient 6 diagnostics jamais comptés. **Corollaire piégeux : déplacer un effet d'un `.tsx` vers un hook `.ts` faisait disparaître ses warnings sans rien corriger** — précisément ce que ce lot allait faire. Glob élargi à `apps/web/**/*.ts`, dans un bloc SÉPARÉ des règles `react/jsx-*` (sinon « Definition for rule not found » sur les `.ts`). `exhaustive-deps` 11 → 3 (les 10 des calendriers soldées en **déstructurant les fonctions de persistance** — stables, alors que l'objet porteur change d'identité quand `indisponible` bascule). Baseline 658 → **649**.
- 🔧 **L'outil de non-régression visuelle de C1-C4 a fait son office** : empreinte **strictement identique** (4 586 nœuds, 0 écart, 0 reclassé, 0 apparu, 0 disparu) avant/après, 760 tests verts, **aucun fichier de test modifié**. ⚠️ **Piège machine distante** : le conteneur embarque Chromium build 1194 et Playwright 1.56 en réclame 1228 → passer par une config locale NON versionnée qui surcharge `launchOptions.executablePath` sur `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` ; ne PAS lancer `playwright install`. Et builder `shared-semaine`/`contracts-kernel` avant, sinon `nx serve web` échoue sur l'import.

**Exécution 2026-08-03 (session DISTANTE, suite) — B3, branche `claude/implementation-du-plan-a3dk80` :**

- **Le défaut était bien réel** (contrairement à C5, l'énoncé du plan tenait) : la readiness de la gateway ne portait qu'une sonde de **liveness sur le seul svc-referentiel** — elle répondait « prête » alors qu'aucun amont n'acceptait de trafic. `AmontsHealthIndicator` la remplace : readiness des **5** amonts (`/api/health` de chacun ⇒ base + migrations + NATS), **une sonde terminus par amont** pour que le 503 nomme le service fautif dans le corps, toutes servies par un lot unique en **cache TTL 5 s** (timeout 2 s/sonde) — « pas de sonde synchrone par requête ».
- 🔑 **LEÇON RÉUTILISABLE : durcir une sonde de santé change la sémantique de TOUT ce qui la consomme.** `/api/health` de la gateway avait **trois** consommateurs invisibles depuis le contrôleur : la **Porte 3 de `deploy.mjs`** (une readiness plus stricte = rollback possible sur un amont dégradé — voulu, mais il faut élargir la patience : 10 → 20 essais × 3 s, sinon on échange un flaky de seed contre un rollback intempestif), le **smoke CI**, et le **heartbeat A4** (dead man's switch). Inventorier les consommateurs AVANT d'implémenter, pas après.
- **Décision PO n°1 (tranchée au lancement)** : sonder la **readiness** des amonts, pas leur liveness — seule la readiness distingue « le process répond » de « le service accepte du trafic », donc seule elle ferme la cause racine (un svc-foyer encore en migration est vivant et non prêt).
- **Décision PO n°2** : le **heartbeat bascule sur `/api/health/live`**. Sur readiness agrégée, le moindre amont dégradé ferait taire le battement → mail Healthchecks.io en doublon d'Alertmanager **et indiscernable d'une panne d'hôte**, seul scénario que ce moniteur détecte quand tout le reste est muet. Le canal externe garde une sémantique unique ; un amont dégradé reste du ressort de Prometheus/blackbox, qui le nomme. **⚠️ GESTE OPS AU DÉPLOIEMENT (PO)** : `sudoedit /etc/creche-heartbeat.env` → suffixer `HEARTBEAT_HEALTH_URL` en `/api/health/live` puis `sudo systemctl start creche-heartbeat.service` (procédure dans `scripts/systemd/README.md` § Heartbeat). Sans ce geste, le battement se taira au premier amont dégradé.
- **Pansement remonté à sa cause** : `attendreGateway` (seed) sondait `GET /couts` en acceptant **toute** réponse HTTP comme preuve de disponibilité ; il attend désormais la readiness de la chaîne. Le rejeu 30 s est **conservé** mais change de rôle — ceinture pour un **disjoncteur gateway encore ouvert**, qui est un état _client_ qu'aucune readiness d'amont ne peut voir.
- **Contrainte A6/A7 tenue** : healthchecks compose et sonde blackbox restent sur `/api/health/live` (aucune dépendance ⇒ aucun restart en cascade). Verrouillé par un test dédié, avec les 3 autres propriétés invisibles en dev (readiness ≠ liveness, amont nommé, lot mis en cache). 8 tests, gateway 284 verts, 0 warning ESLint ajouté.

**Exécution 2026-08-03 (session DISTANTE, suite) — D4, branche `claude/implementation-du-plan-deakfu` :**

- 🔴 **DÉFAUT RÉEL, trouvé par le script à son PREMIER run : `context:notifications` n'était enregistré dans AUCUN `depConstraints`.** `svc-notifications` et `contracts-notifications` n'étaient donc soumis à aucune frontière de contexte depuis la naissance du service. Le lot postulait ce mode de défaillance comme théorique ; il était déjà réalisé. Enregistré au périmètre du code existant (`notifications` + `shared` + `foyer` + `planification` : consommation des ÉVÉNEMENTS foyer/planification, même schéma que tarification). Sonde négative : resserré à `notifications`+`shared` → **8 erreurs** `enforce-module-boundaries`, donc la règle est bien évaluée désormais.
- 🔑 **LEÇON RÉUTILISABLE : un tag Nx absent des `depConstraints` n'est pas « permissif », il n'est JAMAIS ÉVALUÉ.** Rien ne distingue un contexte hors règle d'un contexte sain : le lint est vert quoi que le projet importe. Même famille que les angles morts de D2 (`coverage.enabled:false` ⇒ invisible au ratchet) et de C5 (règles react-hooks limitées aux `.tsx` ⇒ hooks `.ts` non vus) : **trois fois de suite, le défaut n'était pas dans le code mais dans le périmètre de l'outil qui le regarde.** Réflexe à garder : avant de conclure « c'est propre », vérifier ce que l'outil regarde vraiment.
- 🔧 **OUTIL VERSIONNÉ** : `scripts/verifier-frontieres.mjs` (`pnpm frontieres`, < 1 s, step bloquant du job `ci`). Vérifie (a) que tout tag porté par un projet est enregistré en `sourceTag`, (b) que tout projet porte exactement un `type:` et un `context:`, (c) que les tags cités en `onlyDependOnLibsWithTags` existent (un tag mal orthographié **resserre** la contrainte en silence), (d) que les miroirs de vocabulaire déclarés sont conformes à leur source. Il lit les `depConstraints` en **important** `eslint.config.mjs` (pas de parsing texte) et la liste des projets depuis `pnpm-workspace.yaml` (ajouter `tools/*` sans le dire au script rouvrirait le trou). 4 sondes négatives jouées, dépôt sain = 0/0 (21 projets, 12 contraintes, 5 copies).
- **Convention « miroir local documenté » écrite en CONVENTIONS.md §4** (nouvelle section, ex-§4/§5 renumérotées en 5/6) + renvois depuis doc 03 §7, CONTRIBUTING et l'en-tête des fichiers miroirs. 5 copies enregistrées : `MODES_GARDE`/`ModeGarde`/`MODES_ABCM` de `referentiel-domain` (relation `identique`), `PolitiqueTarifaireId` (**`sur-ensemble`** : 4 modes + 2 politiques internes — inscrit pour que personne ne « corrige » cet écart voulu), et les `enum` de `gateway.openapi.ts`.
- **Trouvaille** : `gateway.openapi.ts` recopie la liste des modes **6 fois** à la main. NON fusionné (`[...MODES_CONTRAT]` change l'ordre → régénération des types web + porte `openapi-types-drift`, hors d'un lot de convention) mais désormais **sous garde**.
- **Pour les plans 31/32/factures** : créer `context:famille`/`context:facturation` **sans** son entrée `depConstraints` dans le même commit = service hors frontière que rien ne signale. La CI le refuse maintenant.

**Exécution 2026-08-03 (session DISTANTE, suite) — B7, branche `claude/implementation-plan-zh02l1` :**

- **Le lot était énoncé « purger la prose » ; il ne pouvait pas tenir ainsi.** Un nouveau plan est écrit en recopiant la section « transverses » du précédent — c'est le mécanisme exact par lequel ces 5 pièges se sont retrouvés dans ~10 plans. Livré donc : une **source unique** (CONTRIBUTING.md § « Pièges : ce que l'outillage garantit » : tableau des 10 pièges neutralisés + ce qui les rend impossibles, puis la liste de ceux **encore réels**), la purge de **39 passages / 19 documents**, et une **porte** `scripts/verifier-pieges-doc.mjs` (`pnpm pieges`, < 1 s, step bloquant du job `ci`).
- 🔴 **DÉFAUT RÉEL, trouvé en traçant la frontière mort/vivant : `serve`, `dev` et `preview` d'`apps/web` n'avaient AUCUN `dependsOn`.** B2 n'avait posé `^build` que sur `test`/`typecheck`/`e2e`/`build` ⇒ « builder `contracts-kernel`/`shared-semaine` d'abord » était **encore vrai sur le chemin Vite dev**, ce que la session C5 avait d'ailleurs re-constaté à ses dépens (« sinon `nx serve web` échoue sur l'import ») sans que personne rapproche les deux. Correctif : arête posée, `web:serve` **1 → 3 tâches** (mesuré au graphe avant/après). Le piège est mort partout, donc purgeable partout.
- 🔑 **LEÇON RÉUTILISABLE : « c'est documenté » et « c'est encodé » ne se distinguent qu'en allant lire l'outil.** Après D2 (`coverage.enabled:false`), C5 (règles react-hooks limitées aux `.tsx`) et D4 (tag hors `depConstraints`), où le défaut était dans le **périmètre de l'outil**, celui-ci était dans le périmètre de la **croyance** : B2 était réputé avoir fermé le piège, le plan de C5 le re-documentait comme actif, et les deux affirmations étaient vraies — sur des cibles différentes. Vérifier laquelle, plutôt que d'arbitrer entre les deux prose.
- 🔴 **ANGLE MORT DU SCRIPT, corrigé pendant son écriture (et il rendait la porte verte à tort)** : découper le markdown au **paragraphe** (ligne vide) agrège une liste à puces ENTIÈRE en une unité ⇒ un seul `~~barré~~` dans une liste de 18 puces exemptait les 17 autres. L'unité de sens est **la puce**, pas le bloc.
- 🔧 **OUTIL VERSIONNÉ** : registre `PIEGES_MORTS` (7 motifs, chacun avec ce qui l'a tué et la phrase à écrire à la place), deux échappatoires versionnées (le barré `~~…~~`, déjà conventionnel ; un registre `EXCEPTIONS` par couple (fichier, piège) avec motif écrit, sur le modèle de `lint-baseline.json`), et **trois gardes anti-pourrissement** : exception devenue inutile, relevé déclaré introuvable, exception sur un piège inexistant — une allowlist qui pourrit est une porte qui s'ouvre en silence. Anti-balayage-à-vide inclus. **13 sondes négatives, 13 déclenchent** ; dépôt sain 0/0 (70 documents, 9 981 unités).
- **Exclus du balayage à dessein** : `.claude/memory/` (fiches datées + miroir délibérément incomplet d'un magasin local : une porte de CI s'y battrait contre sa source), le journal `docs/06`, l'ADR 0003, et CONTRIBUTING lui-même (il doit nommer les pièges morts pour dire qu'ils le sont).
- **Restent écrits car aucun outil ne les couvre** : la **saturation machine** sous `--parallel` — même message « provider non prêt après 40000 ms » que le port squatté, lui mort : les deux devaient être OPPOSÉS et non fusionnés —, `/pacts` dans `.prettierignore`, le prompt TTY drizzle `drop`/`rename`, `prettier --check` sous Windows, `git fetch` avant de brancher (le préflight est hors-réseau par construction), le `| tail` qui masque le code de sortie, les angles morts de l'audit axe.
- **Reste du chantier : B4** (attend H3, décision PO de cadence de release) et **B5** (exige la pile Docker locale — hors session distante) ; **D5** opportuniste par énoncé, **D6** dernier ; **C6** non lancé (H2). Plus les gestes PO **R2 (a)** (rclone absent du serveur) et **R3** (bascule INTERSERVICE).

**Exécution 2026-08-05 (session DISTANTE, suite) — D6, branche `claude/implementation-du-plan-z3nrgg` :**

- 🔴 **L'ORACLE NE GARDAIT RIEN DU RÉEL, et c'est la 5ᵉ fois de suite que le défaut est dans le périmètre de l'outil.** `gateway.openapi.spec.ts` vérifiait « expose exactement les 27 routes attendues » — mais la liste attendue y est écrite à la main **en face d'un document écrit à la main** : les deux côtés sont la même main. Une route ajoutée à un contrôleur et oubliée dans le document passait les deux gardes en silence. Résultat mesuré au premier run de la nouvelle garde : **12 opérations servies sur 49 n'étaient documentées nulle part** — `PUT`/`DELETE /contrats/{id}`, `GET …/plannings/{mois}` (seul le `PUT` était décrit !), `PUT …/plannings/semaine/{semaineIso}`, `GET /api/health/live`, `GET /api/referentiel/health`, et surtout **les 6 routes `/api/v1/notifications/*` — la fonctionnalité « valider ma semaine » EN ENTIER**.
- 🔧 **OUTIL VERSIONNÉ** : `apps/api-gateway/src/openapi/openapi.couverture.spec.ts` — pas un script `pnpm`, un **spec vitest** (donc déjà bloquant via `nx test api-gateway`, aucun câblage CI à ajouter). Il parcourt le **graphe de modules Nest** depuis `AppModule` (y compris les modules dynamiques `X.forRoot()`, qui portent leurs métadonnées sur l'objet retourné et non sur la classe), recompose chaque URL servie (préfixe global `api`, versionnage URI, `:param` → `{param}`) et exige l'égalité **dans les deux sens** : servie-non-documentée ET documentée-non-servie. Plus une garde anti-balayage-à-vide (≥ 40 opérations + 4 routes témoins) et 3 sondes du résolveur d'URL lui-même sur des contrôleurs synthétiques.
- 🔑 **LA CLÉ TECHNIQUE : lire les métadonnées de décorateurs ≠ booter Nest.** Le dépôt sait depuis Phase 7 que « booter Nest in-process sous vitest casse l'injection » (esbuild n'émet pas `design:paramtypes`) — d'où le sous-processus du bundle webpack dans `e2e/parcours.e2e.spec.ts`. Mais `path`/`method`/`__version__` sont posés par les décorateurs **au moment de la décoration**, indépendamment de `emitDecoratorMetadata` : seule l'INSTANCIATION est cassée, pas la lecture. C'est ce qui rend la garde possible en < 10 ms, in-process. (Corollaire : un script `.mjs` n'aurait PAS pu le faire — le type-stripping de Node ne sait pas transpiler les décorateurs de paramètres.)
- **Effet de bord documenté au passage** : `/api/health` était décrit « Liveness/readiness » avec un corps `{status}`, alors que **B3 en a fait une readiness agrégée** (5 amonts) et A7 une réponse terminus complète. Corrigé, avec le 503 et la distinction readiness/liveness verrouillées par un test — la contrainte A6/A7 (compose et blackbox sur `/api/health/live`) est désormais lisible dans le contrat lui-même.
- **Les types web des routes nouvellement décrites sont DÉRIVÉS, plus recopiés** (`apps/web/src/types/bff.ts`) : ~15 types de `types/bff.ts` passaient par un miroir manuel de svc-notifications, justifié en commentaire par « ces routes ne sont pas décrites dans l'OpenAPI → rien à dériver ». C'était vrai, et c'était la conséquence du trou. Le typecheck web est passé **du premier coup** : le miroir manuel était exact — mais rien ne le garantissait, et c'est précisément ce que D4 avait outlawé (un miroir non déclaré). Restent hand-typés à dessein les champs que le contrat laisse OUVERTS parce que la gateway les relaie tels quels (besoins datés, semaines-types, corps de planning) : ils sont greffés sur le type dérivé (`Omit<…> & {…}`), même patron que `ContratVersionVue`.
- **Chiffres** : document 2002 → 2768 l. (37 paths, 49 opérations, 14 schémas ajoutés), oracle 27 → 37 routes, api-gateway 284 → 290 tests, baseline lint **inchangée à 649** (le spec a demandé 3 tours de correction : `no-empty-function` sur les contrôleurs synthétiques, `Array.isArray` qui narrowe en `any[]` et masque les règles `no-unsafe-*`, assertions rendues inutiles par le document `as const`).

**Extension 2026-08-05 (session DISTANTE) — 9 lots ajoutés au plan, aucun code touché** (branche `claude/analyse-amelioration-qualite-ktjdjw`, reconnaissance sur `a24b2ac`). ⚠️ **Mesures STATIQUES uniquement** : le clone n'avait pas de `node_modules`, donc aucune cible Nx, aucun run ESLint, aucun `EXPLAIN`. Les constats ci-dessous viennent de la lecture des configs et du code ; ceux qui exigent une contre-mesure à l'exécution sont signalés dans le plan.

- 🔴 **DÉFAUT RÉEL n°1 — `apps/web` n'a AUCUNE frontière d'erreur React** (0 `ErrorBoundary`/`componentDidCatch`/`errorElement` ; `main.tsx` monte `<App/>` nu) : toute exception de rendu vide `#root` ⇒ **page blanche muette**. Ce n'est pas théorique — c'est exactement le mode de défaillance rencontré en C1 (mock renvoyant `{semaines:[]}` au lieu d'un tableau ⇒ tout l'arbre tombe, balayage muet sur 26 pages). Second volet : **aucune remontée d'erreur navigateur** (ni `window.onerror`, ni `unhandledrejection`) alors que les 6 services backend sont sous OTel/Loki/Prometheus ⇒ **le navigateur est le seul endroit du système où une panne ne produit aucun signal**. → lot **C7**.
- 🔴 **DÉFAUT RÉEL n°2 — l'outbox est balayée séquentiellement, sans index ni purge.** `OutboxRelay` tourne **toutes les 2 s** (`INTERVALLE_MS=2000`) dans **4 services**, en `SELECT … WHERE published_at IS NULL ORDER BY occurred_at LIMIT 50`, et la jauge `outbox_backlog` ajoute un `COUNT(*)` du même prédicat **à chaque scrape Prometheus**. Or `CREATE INDEX` ne sort que **6 occurrences dans TOUTES les migrations du dépôt**, aucune sur `outbox` ; et rien n'est jamais purgé (ni les lignes publiées, ni `processed_event`, une ligne par événement consommé à vie). Coût croissant **linéairement avec tout l'historique** pour un résultat qui ne concerne que la queue. ⚠️ **Le modèle d'outbox est RECOPIÉ par service** (le modèle partagé n'est qu'une référence de _type_) ⇒ `svc-famille`/`svc-facturation` hériteront du défaut : corriger le patron AVANT de le dupliquer. → lot **E1** (nouveau chantier **E « tenue dans la durée »**, avec E2 index de lecture — 46 tables / 6 index — et E3 rétention des journaux).
- 🔑 **SIXIÈME récurrence de la leçon « le défaut est dans le périmètre de l'outil »** (après D2 `coverage.enabled:false`, C5 react-hooks limité aux `.tsx`, D4 tag hors `depConstraints`, B7 « documenté ≠ encodé », D6 oracle écrit de la même main que sa cible — ce dernier mergé PENDANT la rédaction de l'extension, d'où le recomptage) : les **5 libs `libs/contracts/*`** ont un `vitest.config.mts` **sans `coverage.enabled` et sans reporter `json-summary`**, et la CI lance les tests **sans `--coverage`** (`ci.yml:136`) ⇒ elles n'émettent **aucun `coverage-summary.json`** ⇒ **invisibles au ratchet AQ-06**, dont `contracts-kernel` qui porte `gateway.openapi.ts` (**2754 l.** depuis D6). Elles ont pourtant des tests : ce n'est pas le code qui manque, c'est le rapport. + deux **seuils versionnés en retard sur le mesuré** (`api-gateway` 69 % de lignes versionné vs **78,80 %** mesuré après D1 ; `svc-foyer` 67 % de fonctions) — un seuil sous le mesuré **autorise** la régression jusqu'à ce plancher, et le ratchet CI ne rattrape que les baisses > 0,5 pt entre deux runs. → lot **D8**.
- Autres lots ajoutés : **C8** (axe ne visite que **9 des 16 routes** déclarées — jamais auditées : `/mes-foyers`, `/tarifs`, `etablissements` (667 l.), `modifier`, l'index foyer, la 404) ; **C9** (fermer `style-src 'unsafe-inline'`, **tout-ou-rien** : 193 inline restants, la directive ne se resserre qu'à zéro) ; **D7** (second tour du ratchet : 624 des 649 warnings dans 8 règles ; trois familles à gestes DIFFÉRENTS — bruit de specs, conventions Nest à désactiver avec motif, et `no-deprecated` **61** = le seul groupe qui annonce une casse future, à trier en premier) ; **B8** (aucune garde de poids de bundle : ni `size-limit`, ni budget Vite, ni step CI — et une seule frontière `lazy()` dans tout le front).
- **D5 corrigé** : son énoncé ne citait que `planification.service` (1425) et `foyer.service` (1147) ; la file réelle est de **7 fichiers** (+ `cout.service` 803, `scheduler.hebdo` 719, `referentiel.service` 665, `projection.service` 649, `envoi.service` 533). Le « aucun composant > 700 l. » atteint en C5 ne vaut que pour `apps/web`.
- **Hypothèses PO ajoutées : H5** (rétention `processed_event`/`correction_journal`/`dead_letter` — purger tôt rouvre la fenêtre de re-livraison NATS ; E1 est livrable **avec l'index seul** en attendant) et **H6** (C9 : fermer la CSP ou y renoncer explicitement — la laisser ouverte fait croire à une fermeture en cours).
- 🔑 **Six récurrences d'affilée ⇒ ce n'est plus une série d'accidents, c'est le mode de défaillance dominant du dépôt.** Une **mesure de succès méta** est inscrite au plan : toute porte de CI, existante ou nouvelle, doit être accompagnée d'une **sonde négative** qui prouve qu'elle voit son périmètre entier — la convention est déjà en vigueur de fait (B1, D3, D4, B7, D6 en portent une), il ne restait qu'à l'énoncer.
- ✅ `node scripts/verifier-pieges-doc.mjs` rejoué après rédaction : **0/0** (70 documents, 10 037 unités) — l'extension ne recopie aucun piège mort.

**Plan RÉVISÉ 2026-07-30/31** (révision multi-agents des plans à venir) : cases A synchronisées avec PR + ancrages re-vérifiés ; **nouveau chantier R « Livraison & activation prod » = PRÉALABLE UNIVERSEL de tous les chantiers** — R1 train de release n°16 (déployer main ≥ `9aee291`/#265, rejeu projection #257 `UPDATE grille_abcm SET version_payload=1` + restart, done en 6 points vérifiables), R2 restes ops user (rclone/heartbeat/restore cloud), R3 bascule INTERSERVICE (avant le lot 5 du plan 32) ; **nouveau lot C0 « atterrir la nav mobile non commitée »** (App.tsx/App.test.tsx/styles.css/BarreStatutCalendrier de la copie de travail) préalable de tous les lots web de tous les plans ; ordre réécrit R1+R2 → C0 → B2/B1/B6 → C1-C4 → D1-D3 → **C5 avant le lot 4 du plan 31** → R3 → reste au fil de l'eau ; section « articulation inter-plans » + hypothèses PO H1-H4 ajoutées. Cf. [[plan-sfd-30-33-extension-famille]] pour l'ordre global consolidation → 31 → 32 → 33 → factures.

**Exécution 2026-08-06 (session DISTANTE) — C7, branche `claude/implementation-du-plan-mveycs` :**

- 🔴 **L'ÉNONCÉ DU LOT SE TROMPAIT D'ENDROIT — et la preuve était dans l'exemple qu'il citait lui-même.** Le plan prescrivait « une frontière de route (`errorElement`) + une frontière autour du `<Suspense>` de `PlanningPage` », en justifiant par l'incident de C1 (mock `/notifications/a-valider` renvoyant `{semaines: []}` ⇒ tout l'arbre tombe). Or ce corps est consommé par `PastilleAValider` → **`Entete`** (`layout/Entete.tsx:99`), rendu par `Coquille` **au-dessus de `<main>`, donc HORS `<Routes>`** (`(data ?? []).map(...)` lève sur un objet). **Le seul plantage réellement rencontré par ce dépôt vit exactement dans l'angle mort de la frontière prescrite.** D'où **TROIS** frontières livrées : **racine** (sous `BrowserRouter`, autour de `MoiProvider`+`Coquille`), **route** (dans `<main>`, autour de `<Routes>`), **chunk** (autour du `<Suspense>` des calendriers).
- ⚠️ **`errorElement` n'existe pas dans ce routeur** : primitive des _data routers_ (`createBrowserRouter`), or l'app est en routage déclaratif. Migrer le routeur pour l'obtenir = un chantier à soi seul, plus risqué que le défaut corrigé → `layout/FrontiereErreur.tsx` écrite à la main, **seule classe React du dépôt** (aucun hook n'expose `getDerivedStateFromError`).
- 🔑 **PIÈGE STRUCTUREL DE TOUTE FRONTIÈRE : elle ne se démonte pas à la navigation.** Sans réarmement, le premier plantage fige l'écran de récupération sur TOUTES les destinations suivantes — le correctif transformerait une page cassée en app morte. D'où `clesReinitialisation` (`getDerivedStateFromProps`, pas un effet : un `setState` d'effet ferait clignoter la page d'arrivée) : `pathname` sur la frontière de route, `contrat.id` sur celle des calendriers. Deux tests opposés le verrouillent (réarme quand la clé change / ne réarme PAS sinon).
- 🔑 **Chaque écran de récupération a la sortie que son mode de défaillance permet** — un bouton qui ne peut pas marcher est une impasse déguisée. Route → « Réessayer » réarme (la coquille tient). Racine → **vraie navigation réseau** (réarmer rejouerait le rendu cassé). Chunk → **rechargement complet SEULEMENT**, car `React.lazy` **mémorise la promesse rejetée** : un « Réessayer » relèverait la même erreur indéfiniment.
- **Volet (b) livré aussi** (le plan le donnait comme arbitrable) : `api/signalerErreur.ts` → `POST /api/v1/erreurs-client` (même-origine, `fetch` **nu** + `keepalive` : sur un chemin de crash, timeout/rejeu/disjoncteur sont à contre-emploi), plafond **5 par chargement** + déduplication `origine|message`, ne lève jamais ; `window.onerror` + `unhandledrejection` branchés dans `main.tsx` **avant** le montage. Gateway : `observabilite/erreurs-client.controller.ts`, 204, ancre Loki **`PLANTAGE CLIENT`** (documentée dans `docs/exploitation/observabilite.md`), corrélation par le `trace_id` de la requête POST.
- **Deux bornes anti-flood, pas une** : sans le plafond client, un rendu en boucle ferait répondre **429 à toutes les requêtes du même client** par le `RateLimitGuard` global — l'app encore vivante serait cassée par sa propre télémétrie.
- **Vie privée** : `route` = `pathname` **seul**, jamais la query (`?semaine=`, `?enfant=<prénom>`). **Injection de journal** fermée à la source : message/pile/composant assainis (caractères de contrôle → espace) avant journalisation, test dédié — un message forgé ne peut pas fabriquer une seconde entrée.
- **Dérivation plutôt que miroir, d'emblée (leçons D4/D6)** : les 5 origines acceptées par zod sont **lues dans le document OpenAPI** (`as const` ⇒ tuple littéral consommé par `z.enum`) et le type du corps web est **dérivé** de `openapi-types.gen.ts` ⇒ **rien à inscrire au registre `MIROIRS`**. Un test parcourt les origines du contrat et vérifie que la gateway les accepte toutes.
- 🔑 **PIÈGE ESLint À RETENIR : un `eslint-disable-next-line` posé ENTRE un décorateur et sa classe ne s'applique pas** — le nœud signalé commence au décorateur. Sur `no-extraneous-class` d'un module Nest, le compteur est alors monté de **+2** (le warning non supprimé **et** la directive inutilisée) au lieu de descendre. Directive à poser **au-dessus de `@Module`**. Baseline **649 inchangée**.
- **Checklist « route BFF » complète** : document (+1 path, +1 schéma `ErreurClient`), oracle **37 → 38**, `web:generate-types` régénéré et commité, **pacts régénérés depuis zéro et strictement identiques**, `openapi.couverture.spec.ts` (D6) vert.
- **Chiffres/preuves** : web 760 → **784 tests** (+24), gateway 290 → **296**, `typecheck`+`build` verts sur **21 projets**, suite axe **10/10**, `pnpm frontieres`/`pnpm pieges` 0/0, et **empreinte des styles calculés IDENTIQUE** (4 586 nœuds, 0 écart, 0 reclassé, 0 apparu, 0 disparu, 0 contraste, 0 débordement 375 px).
- ⚠️ **Environnement de la session** : Node **22** dans le conteneur alors que `engines`/`.nvmrc` exigent 24 ⇒ `pnpm preflight` sort 1 (tout le reste OK) ; Playwright n'a que **Chromium 1194** préinstallé (`/opt/pw-browsers/chromium`) là où la version du dépôt attend le shell 1228 ⇒ `nx e2e web` échoue « Executable doesn't exist » **sans que rien ne soit cassé** : rejouer avec une config jetable qui force `launchOptions.executablePath` (jamais `playwright install`). Le test de capture d'écran `planning-creche-mobile` échoue aussi en « snapshot doesn't exist » — le PNG de référence n'est pas versionné, c'est indépendant du lot.
- **Reste (geste PO, exige la prod)** : vérifier la ligne `PLANTAGE CLIENT` dans Loki avec son `trace_id` au déploiement. **NON DÉPLOYÉ** → plancher de la prochaine release, avec D1-D4, C5, B3, B7, D6.

**Exécution 2026-08-07 (session DISTANTE) — D8, branche `claude/plan-github-elements-jlxl45` (sur `f8d683f`, C7 mergé #292) :**

- ✅ **L'énoncé était juste, et vérifié EN NÉGATIF avant correction** : `nx run-many -t test -p contracts-kernel contracts-foyer --skip-nx-cache` puis `find libs/contracts -name coverage-summary.json` ⇒ **zéro fichier**. Ce n'était donc pas « 100 % », c'était **aucune mesure** — `coverage.enabled` absent ⇒ v8 ne tourne pas ⇒ le glob de `coverage-compare.mjs` ne trouve rien ⇒ le projet n'est **pas une ligne du tableau**, ni en régression ni en progrès.
- **Après activation** (`include: src/lib/**/*.ts`, barrel exclu, mêmes réglages que `resilience`/`observability`) : **100 % sur les 5** — foyer 41/41 l., kernel 8/8, notifications 8/8, planification 32/32, referentiel 17/17. Vérifié **fichier par fichier** que le dénominateur couvre tout `src/lib` (Vitest ≥ 3 rapporte les fichiers jamais chargés).
- ⚠️ **PIÈGE DE LECTURE À RETENIR : le 100 % de `contracts-kernel` ne mesure presque rien.** `openapi/gateway.openapi.ts` fait **2810 lignes** et pèse **1 ligne** au rapport v8 — un unique `export const` dont le littéral entier compte pour une seule instruction. Le seuil prouve que le module est **importé**, point. Le contrat reste gardé par la spec de D6 et `verifier-frontieres.mjs` (D4) ; la couverture ne s'y substitue pas. C'est écrit dans la config pour que personne ne s'y trompe plus tard. **Corollaire général : sur un fichier de données, un pourcentage v8 est une mesure de chargement, pas de test.**
- **Seuils en retard, re-mesurés** : `api-gateway` **83,90/81,07/65,67/84,42** contre 69/70/59/69 (l'énoncé citait 78,80 % — C7 a depuis ajouté `erreurs-client` + specs) ⇒ **~15 pts d'autorisation de régression** ; `svc-foyer` **84,86/80,78/70,43/85,27** contre 82/78/**67**/83. Reposés au mesuré arrondi vers le bas (**83/81/65/84**, **84/80/70/85**), **sans la marge de ~2 pts** des lots précédents : c'est cette marge jamais rattrapée qui avait laissé l'écart s'installer, et la mesure est déterministe.
- **Sonde négative 1 (le seuil mord)** : fichier non testé ajouté à `contracts-referentiel` ⇒ 89,47 % de lignes, `nx run contracts-referentiel:test` **sort en 1**, 4 messages `does not meet global threshold`. Retiré ensuite.
- **Sonde négative 2 (le ratchet les VOIT)** : baseline fabriquée + un rapport dégradé à 90 % ⇒ `coverage-compare.mjs` **exit 1**, `🔴 -10.00 pt` sur `libs/contracts/referentiel`, les 4 autres `✅ +0.00 pt`.
- 🔑 **DÉFAUT ADJACENT trouvé PAR la sonde 2, dans le même outil — septième récurrence du « périmètre »** : le glob `**/test-output/…` matche aussi les copies rangées par Nx sous **`.nx/cache/<hash>/<projet>/test-output/`**. La sonde a sorti **7 lignes fantômes à côté des 7 vraies**. `projectName()` découpant sur `/test-output`, chaque copie devient un « projet » `.nx/cache/<hash>/…` ⇒ totaux de `test-summary.mjs` **comptés plusieurs fois** et baseline de couverture qui s'alourdit à chaque run de clés jamais recroisées. **Aucun faux échec** (pas de collision de clés), mais une mesure fausse — et **en CI le step « Cache Nx » restaure `.nx/cache` AVANT les tests**, donc le cas s'y produit. Filtre `HORS_PERIMETRE` (`.nx/`, `node_modules/`) posé dans **les deux** scripts. **Leçon : une sonde négative ne sert pas qu'à valider le correctif — c'est le seul moment où on regarde vraiment la sortie de l'outil.**
- **Preuves** : `nx affected -t lint typecheck test build --base=main` **vert sur 21 projets** (84 tâches), `pnpm frontieres` 0/0, `pnpm pieges` 0/0, ratchet ESLint **649 inchangée**, `nx format:write` appliqué. Aucun code applicatif touché — 7 `vitest.config.mts` + 2 scripts CI.
- **Ce que D8 NE fait pas** : il ne remonte pas la couverture (65,67 % de **fonctions** sur `api-gateway` reste le vrai creux — des tests, pas un seuil), et les 3 autres services n'ont pas été re-mesurés (leur seuil peut aussi avoir pris du retard).
- ⚠️ **Environnement** : Node **22** dans le conteneur alors que `.nvmrc`/`engines` exigent 24 (même constat qu'en C7) ⇒ `pnpm preflight` sortirait 1 ; sans effet sur ce lot.
- **NON DÉPLOYÉ** → s'ajoute au plancher de la prochaine release avec D1-D4, C5, B3, B7, D6, C7.

**État GitHub relevé le 2026-08-07 (à traiter côté PO, hors périmètre de ce lot) :**

- 🔴 **`main` est ROUGE sur le run CI du 2026-08-06** (run `31115591001`, HEAD `f8d683f`/#292) — mais **ce n'est pas une régression du dépôt** : les jobs `secret-scan` et `affected-images` échouent au step « Set up job », avant tout checkout, sur `##[error]Internal Server Error occurred while resolving "actions/checkout@…"` (+ `gitleaks-action`, `setup-node`, `nx-set-shas`). **Incident côté GitHub, pas côté code** : les 13 autres jobs sont verts, dont `ci`, `security`, `sast-semgrep`, `pact-drift`, `e2e-web`. ⇒ **un simple re-run des jobs échoués suffit** ; ne pas fouiller le diff (même famille que [[piege-sca-rouge-sans-diff]], cause différente).
- **3 PR Dependabot ouvertes**, dont **#284 (43 mises à jour mineures groupées) entièrement VERTE — 23 checks, aucun échec** (incluant `e2e-stack`, `smoke-stack` et les 7 `build-images`) : elle n'attend qu'un merge PO. #283 (`actions/checkout` 7.0.0 → 7.0.1) et #241 (`actions/setup-node` 6.4.0 → 7.0.0) sont ouvertes depuis plus longtemps.
