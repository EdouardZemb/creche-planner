---
name: feature-dashboard-jour
description: "Chantier tableau de bord « ma journée du foyer » — quels enfants où/quel mode aujourd'hui"
metadata:
  node_type: memory
  type: project
  originSessionId: 8fbb137e-328c-46d2-9693-fca8ddc589b7
---

**✅ DÉPLOYÉE EN PROD `0.7.0` le 2026-06-30 (soir)** via le 7e release train (commit `dbd85ca`, GitHub Deployment production #5260093957 `success`). La page `/foyers/:id/dashboard` + tous les enrichissements (accueil→dashboard P3b, deep-link « Modifier » P3a, bandeau coût du mois P3c) sont LIVE. Vérif post-deploy 100% OK (web `version=0.7.0 revision=dbd85ca`). Détail dans [[prod-deployment-facts]] (7e release train).

Nouveau chantier : un tableau de bord « ma journée » qui liste, pour le foyer et le **jour courant**, quels enfants sont gardés / à quel mode / quel horaire effectif. Découpé en PR (PR1 = logique pure isolée, PR2 = UI/routing, PR3 = enrichissements incrémentaux P3a/P3b/P3c, **une PR ciblée par item**).

**PR3 = enrichissements (sous-items isolables, confirmer priorité avec le PO avant chaque)** : **P3a deep-link « Modifier »→contrat précis (FAIT)** ; **P3b accueil→/dashboard (FAIT)** ; **P3c bandeau coût du mois (FAIT)** — RESTE seulement le volet optionnel **« semaine compacte » (7 jours)** de P3c, non livré ; P3d (PAS par défaut) endpoint BFF dédié `GET /foyers/:id/jour/:dateIso` (si retenu : OpenAPI+types+Pact en **rm le fichier Pact avant re-run** + `config-validation`). **→ PR3 close sur ses items par défaut ; la feature dashboard est fonctionnellement complète, NON déployée prod (attend `nx release`).**

**P3c MERGÉ — PR #117 (squash `b113f83` sur main, 2026-06-30, CI 100% verte dont ci/e2e-stack 4m32/smoke-stack/e2e-web/pact-\*/openapi-types-drift ; branche distante supprimée)** : bandeau « coût du mois » sur le dashboard ([apps/web/src/dashboard/DashboardJourPage.tsx], composant `BandeauCoutMois`). Coût RÉEL (`simule=false`) du mois courant via `api.lireCoutMois(foyer, mois, false)` (`mois`=`aujourdhui.slice(0,7)` Paris) + `centimesEnEuros` + lien « Détail »→`/foyers/:id/couts`. **Secondaire/non-bloquant** : `if(!data) return null` (couvre loading ET erreur → s'efface, ne masque jamais la journée). Pas de nouvelle API. Tests : +2 cas (montant+lien ; silencieux si échec). L'audit a11y racine couvre le bandeau (son mock sert déjà `/api/v1/couts`). **Cadré sur le bandeau seul** ; volet « semaine compacte » du plan P3c non pris.

**P3a MERGÉ — PR #116 (squash `536094d` sur main, 2026-06-30, CI 100% verte dont ci/e2e-stack 4m28/smoke-stack/e2e-web/pact-\*/openapi-types-drift ; branche distante supprimée ; NON déployé prod)** : le bouton « Modifier » d'une ligne du dashboard ([apps/web/src/dashboard/DashboardJourPage.tsx], composant `RangeeJour`) deep-linke vers le planning du contrat au lieu du planning générique : `?enfant=<prénom>&mode=<MODE>&mois=<YYYY-MM>` (params déjà lus par `PlanningPage` ; `mois`=`aujourdhui.slice(0,7)` Paris ; `URLSearchParams` pour l'encodage). Pas de nouvelle API. Test : l'assertion du lien vérifie pathname + params décodés.

**P3b MERGÉ — PR #115 (squash `2e30af7` sur main, 2026-06-30, CI 100% verte dont ci/e2e-stack 4m29/smoke-stack/e2e-web/pact-\*/openapi-types-drift ; branche distante supprimée ; NON déployé prod — attend `nx release`)** : `Accueil()` atterrit sur `/dashboard` au lieu de `/planning`. Bascule de TOUTES les redirections « ouvrir le foyer / rentrer chez soi » : `Accueil()` (cache localStorage / foyer unique mode borné / chemin hérité), `AccueilDecouverte` (1er foyer découvert), `MesFoyersPage` « Ouvrir le foyer N », `FoyerIntrouvable` « Revenir à mon foyer ». **Laissés inchangés** : NavLink « Planning » (voie d'accès au planning) et redirection **post-édition** de `FoyerModifierPage`→planning (autre intention, garde `foyer-modifier.stack` verte). Specs d'atterrissage MAJ : `App.test.tsx` (mock `DashboardJourPage`→`PAGE_DASHBOARD`, assertions landing + hrefs) ; `a11y.e2e.spec.ts` (la redirection racine audite le dashboard URL+titre « Aujourd'hui » ; mock ajouté pour `GET …/notifications/semaine/:foyer/:semaine/besoins` = endpoint du dashboard, état **vide déterministe** indépendant du jour réel). Seule `a11y.e2e.spec.ts` fait `goto('/')` → aucune spec e2e-stack ne dépend de l'atterrissage racine. Pas de nouvel endpoint (OpenAPI/Pact inchangés).

⚠️ **Le fichier de plan `.claude/plans/dashboard-jour.md` est ABSENT du dépôt** (jamais committé, vérifié sur toutes branches/stash 2026-06-30). La « table D3 » (états par mode) a été **reconstruite depuis le code de référence** que la logique généralise — `EditeurContratSemaine.resume(date)` ([apps/web/src/notifications/EditeurContratSemaine.tsx:349]) + `classerAbsence` ([apps/web/src/planning/etatJourGarde.ts], cf. [[feature-planning-etat-ajuste]]). Si l'utilisateur produit le vrai plan, revalider la D3.

_*PR2 MERGÉE (PR #114, squash `493dec7` sur main, 2026-06-30, CI 100% verte dont ci/e2e-stack/e2e-web/smoke-stack/build-images/openapi-types-drift/pact-* ; branche distante supprimée ; NON déployé prod — attend `nx release`)_* : page Dashboard branchée. `apps/web/src/dashboard/DashboardJourPage.tsx` (route `/foyers/:foyerId/dashboard` sous `GardeFoyer`) — `jourCourantParis(new Date())`→`semaineIsoDeDate`→`api.lireSemaineBesoins`→`lignesDuJour` ; pastilles `couleurAjoute/Ajuste/Retire` (déviations) + `couleurDuMode` (présence normale) ; lien « Modifier »→planning ; états loading/erreur/vide ; +4 tests. App.tsx : route + NavLink « Aujourd'hui » (non `moi.admin`) + regex titre. Dép. workspace `@creche-planner/shared-semaine` ajoutée au web. **Décision D6 respectée : `Accueil()` redirige toujours vers `/planning` (bascule = P3 séparé).** Aucune spec e2e n'asserte l'ensemble exact de la nav (vérifié) → rien à adapter. Reste après PR2 : P3 (redirection accueil→dashboard ?) + finitions UI éventuelles.

_*PR1 MERGÉE (PR #113, squash `6aeba90` sur main, 2026-06-30, CI 100% verte dont ci/e2e-stack/e2e-web/openapi-types-drift/pact-* ; branche distante supprimée ; NON déployé prod)_* : logique pure, zéro UI/routing.

- `libs/shared/semaine` : `jourCourantParis(now: Date): string` → date `YYYY-MM-DD` du jour en **Europe/Paris** (`Intl.DateTimeFormat('fr-CA',…).formatToParts`, instant passé en argument = déterministe ; même convention que le scheduler mardi `svc-notifications`). Exporté depuis l'index. **Le lib impose 100% branch coverage** → éviter tout `?? ''` défensif non couvert (j'ai dû remplacer `find(...)?.value ?? ''` par une boucle `for…of` sur les parts, dont les séparateurs `-` exercent les false-paths).
- `apps/web/src/dashboard/jourFoyer.ts` : `lignesDuJour(vue: SemaineBesoins, dateIso): LigneJour[]` + types `EtatJour` (jetons stables : garde/absent/depart-avance/arrivee-retardee/ajuste/jour-ajoute/cantine/peri/alsh) et `LigneJour {contratId,enfant,mode,etablissementLibelle,etat,horaire}`. Filtre les contrats non concernés ce jour (non gardés/week-end/sans cantine·péri/ALSH non réservé). Consomme la forme web `SemaineBesoins` ([apps/web/src/types/bff.ts]), réutilise `classerAbsence` + helpers `planning/heures`.

**Piège worktree « faux vert » (revécu en PR2)** : Write/Edit avec un chemin absolu qui OUBLIE le segment `.claude\worktrees\<nom>\` édite le **clone principal** → `git status` du worktree reste clean, et `nx test` (lancé dans le worktree) passe « 44/372 » sans voir tes fichiers (cache trompeur). Toujours préfixer par la racine worktree courante ; recoller : `cp` les fichiers neufs + ré-`Edit` les fichiers modifiés DANS le worktree, puis `git checkout`/`rm` côté clone principal. Vérif fiable seulement quand `git status` du worktree montre les fichiers ET que le compte de tests augmente.

**Piège worktree** : la worktree `friendly-feistel-b52f4b` n'avait **aucun `node_modules`** → `corepack pnpm@10.34.2 install --frozen-lockfile` (≈1m45) avant de pouvoir lancer nx. Voir [[pnpm-corepack-version]]. `pnpm nx …` échoue (cherche un script "nx") → utiliser `pnpm exec nx …`. Vérifs : `nx test shared-semaine`, `nx run-many -t typecheck test lint -p web`, prettier `--check` (les nouveaux fichiers sortent non formatés par défaut → `--write`).
