# Plan — Consolidation UI, corrections des frictions de dev, qualité code & infra

> Issu d'un audit à 4 volets (UI, frictions de dev, qualité code, infra/CI) mené le 2026-07-29 sur `main` (`fa585b6`, prod `0.14.0`) ; **mis à jour le 2026-07-30 sur `main` `9aee291` (HEAD = #265, lot A7 ; vérifié inchangé 2026-07-31)**.
> Statut au **2026-08-03** : **chantier A 7/7 MERGÉ** (#258→#265) ; **R1 DÉPLOYÉ PROD `0.15.0`** (train n°16) ; **C0 + B1/B2/B6 MERGÉS** (`bb3948e`/#267, `1d47636`/#268) ; **C1-C4 MERGÉS** (`9171228`/#272) ; **D1-D2-D3 MERGÉS** (`e2e38e1`/#278) ; **C5 FAIT** (2026-08-03, session distante, branche `claude/implementation-du-plan-0isdve`) — **D1-D3 et C5 NON DÉPLOYÉS**. Restent ouverts : **R2 (b)/(c bis)** et **R3** (gestes utilisateur), **B3-B5, B7**, **C6**, **D4-D6**. Hypothèses PO : **H4 TRANCHÉE** (C2 avant le plan 31) ; H1, H2, H3 ouvertes.
> ⚠️ **C5 étant fait, le chantier C est clos hors C6 (optionnel, H2)** : le lot 4 du plan 31 peut désormais atterrir sur les calendriers découpés.
> ⚠️ **Repères chiffrés de ce document périmés par C1-C4** pour `apps/web` : `App.tsx` 670 → **102 l.**, `styles.css` 1885 → **1982 l.** (tokens + commentaires de cascade), `className="btn"` 96 → **0**, `style={{` 305 → **193**. À re-relever avant C5.
> Repères de lignes : **re-relevés le 2026-07-31 après l'atterrissage de C0** (branche `fix/nav-mobile-pages-globales-contrastes`, commit `cbd3d74`) — la copie de travail est désormais propre. Valeurs à jour : `styles.css` 1885 l. (6 blocs `@media (min-width: 768px)` l. 308/1373/1529/1577/1743/1828 + 3 `max-width` l. 194/1156/1737), `App.tsx` 670 l., 96 `className="btn …"` (27 fichiers), 305 `style={{` (29 fichiers), 48 `aria-describedby` (14 fichiers), 90 `role="status"|"alert"` (36 fichiers). Les autres repères de l'audit sont inchangés.
> Dépendances (état réel au 2026-07-30) :
>
> - [[plan-fondations-backend]] — LIVRÉ (`0.13.0`) et dépassé : `FOYER_AUTHZ_ENFORCE=1` actif prod depuis 2026-06-28 ; INTERSERVICE observe-only ; #264 (`@FoyerScope('identite')` sur les 4 routes `/moi`, `apps/api-gateway/src/bff/moi.controller.ts:112-205`) mergé `07fa175`, **NON déployé** ; fail-open `ScopeFoyerGuard` tranché : conservé (`docs/exploitation/authz-inter-services.md`).
> - [[chantier-versionnement-dates-effet]] (SFD 30) — LIVRÉ prod `0.14.0` ; correctif #257 (PK surrogate projection `grille_tarifaire`) mergé `9818302`, **PAS déployé** → rejeu de projection obligatoire (lot R1).
> - [[plan-sfd-30-33-extension-famille]] — SFD 31/32/33 = brouillons `docs/31-33` NON validés PO ; leurs plans (écrits sur `cc8a708`, avant l'atterrissage de la SFD 30) ont des repères périmés ; la SFD 31 retouchera les mêmes calendriers que C5.
>
> **Ce plan est le PRÉALABLE des chantiers SFD 31/32/33 et factures-reelles** : R1, R3, C0, B2, C5 et la convention D4 y sont définis UNE seule fois — les autres plans y renvoient (cf. « Articulation inter-plans »).

## Synthèse de l'audit

**Forces (à préserver, ne pas « améliorer »)**

- Web : primitive `ui/Modale` unique et accessible, coquille `App.tsx/Coquille`, focus/annonce de route, 66 fichiers de test + suite axe e2e (11 routes) — filets qui rendent toute refonte UI sûre.
- Code : TS strict exemplaire (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`…), 0 TODO dans le code, frontières Nx en `error`, double garde anti-drift pact + OpenAPI types, mutation testing sur les 4 libs domaine.
- Infra : CI multi-portes (Trivy/gitleaks/semgrep/pact/coverage-ratchet/stack réelle), actions épinglées SHA, cosign+SBOM en CI, sops+age, rollback auto + DORA, staging isolé.

**Faiblesses structurantes**

1. UI : 8 composants > 500 lignes (App 657, CoutsAnnuelsPage 543, DashboardJourPage 670, EtablissementsPage 684, ContratForm 697, EditeurContratSemaine 963, CalendrierAbcm 962, CalendrierCreche 1325 ; hors composants : `api/client.ts` 854, `types/bff.ts` 643), 2 calendriers quasi jumeaux (~2300 l.), **96** `className="btn …"` littéraux (27 fichiers), ~40 blocs d'erreur formulaire copiés-collés, 305 styles inline, `styles.css` monolithique (1876 l., **6** blocs `@media (min-width: 768px)` + 3 `max-width` à contre-courant, couleurs hors tokens).
2. Dev : les mêmes pièges re-documentés dans ~10 plans au lieu d'être encodés dans l'outillage (worktree faux-vert, symlinks pnpm, `nx test` sans typecheck, builds de libs préalables/TS6305, shims `.bin`) ; flakiness e2e stack (≥8 commits) ; readiness gateway 502/503 non fermée à la racine ; churn CVE (18 commits).
3. Code : 28 règles ESLint en `warn` (23 `@typescript-eslint`, `eslint.config.mjs:136-168` + 5 `react-hooks`, l.218-227 — ratchet en panne), `nest-commons` (33 src/9 specs — A7 a ajouté 8 tests) et `observability` (3 src, 0 test) **sans seuil de couverture**, patron client HTTP répété (35 `fetchAvecTimeout(` dans 10 fichiers + 1 `fetch` brut), dette DV-01→04 **largement résorbée par la SFD 30** (résiduel réel : D4), god services (planification 1425 l., foyer 1147 l.).
4. Infra : **largement résorbée dans `main` par A1-A7** (#258→#265 : backups 5/5 + hors-site codé, cosign 7/7 fail-closed, heartbeat installé, limites+healthchecks conteneurs, nginx non-root + en-têtes sécurité, migrations observables) — mais prod `0.14.0` = `fa585b6`, ANTÉRIEUR à ces merges : A2/#264/A6/A7 n'entreront en vigueur qu'au prochain train de release (lot R1). Restent vrais : seed+perf-smoke joués contre la prod (choix désormais ASSUMÉ et documenté, #260, doc 24 §5 Porte 3) et authz interservice 100 % observe-only (bascule = R3).

---

## Chantier R — Livraison & activation prod (PRÉALABLE UNIVERSEL)

Le chantier A est mergé mais la prod ne le contient pas. R1 conditionne aussi les plans 31/32 (toute comparaison de coûts exige la projection #257 rejouée) et démarre l'horloge « 7 jours de logs propres » de R3, elle-même prérequis recommandé du lot 5 (revenus) du plan 32. **Aucun autre chantier (B/C/D, SFD 31-33, factures) ne démarre avant R1.**

- [x] **R1 — Train de release n°16 → `0.15.0` ✅ DÉPLOYÉ EN PROD le 2026-08-01** (commit de release `0d10b75`, Deployment #5705660994 `success`, déploiement **automatique par le poller**, 0 rollback). Done vérifié : (1) **cosign 7/7** ✅ ; (2) `grille_tarifaire` **✅ 9 lignes / 3 modes** après rejeu #257 (geste PO), 0 nouveau dead-letter, repli REST éteint ; (3) 5 dumps du 2026-08-01 ✅ ; (4) `migrations_en_attente=0` sur les 5 svc-* + readiness 200 ✅ ; (5) heartbeat pingue toutes les 5 min ✅ ; (6) en-têtes sécurité présents ✅. Bonus : 0 alerte active, 8 groupes/21 règles Prometheus (le groupe `migrations` s'est chargé seul — le deploy recrée aussi la pile d'observabilité, donc `remote-apply-observability.ps1` n'a pas été nécessaire). Détail complet dans [[prod-deployment-facts]].
- [ ] ~~**R1 — Train de release n°16.**~~ (énoncé d'origine conservé ci-dessous) Déployer `main` ≥ `bb3948e` (#267 — le plancher a monté : la release embarque désormais aussi #266 deflake, #268 outillage et #267 nav/a11y ; plancher fonctionnel inchangé = `9aee291`/#265) : active #257, #264 (A5), A2 (cosign 7/7), A6, A7 et la doc A3 (#260) — plancher = #265, sans lui la jauge `migrations_en_attente` du critère done (4) n'existe pas. Inclut le **rejeu de la projection `grille_tarifaire`** (reprise de données, ≠ simple restart : `reemettreGrillesEnV2()` filtre `version_payload < 2` et la prod est déjà à 2 → `UPDATE grille_abcm SET version_payload = 1;` PUIS redémarrage de svc-referentiel — détail et pièges dans [[chantier-versionnement-dates-effet]]). **Done vérifiable** : (1) journal deploy Porte 1bis = 7/7 images cosign vérifiées ; (2) `grille_tarifaire` : `SELECT mode, count(*)` > 0 pour les 3 modes (attendu : 3 modes × 3 tranches = 9 lignes), aucun nouveau `dead_letter` ; (3) 5 dumps datés du jour ; (4) `migrations_en_attente = 0` sur tous les services ; (5) heartbeat up ; (6) en-têtes sécurité web présents (`curl -I` : nosniff, XFO, CSP).
- [~] **R2 — Restes ops.** **(a) ✅ FAIT 2026-08-02** : rclone v1.75.0 installé, remote `gdrive-creche` autorisé (scope `drive.file`), `OFFSITE_REMOTE` posé dans `.env.server.enc` (committé `e76691f`) — **et défaut bloquant corrigé (PR #275)** : `backup-cron.sh` ne sourçait que le `.env.server` EN CLAIR, disparu depuis la Phase 11, donc la copie hors-site se sautait **en silence** chaque nuit malgré la variable. Hors-site **prouvé fonctionnel** : archive `creche_2026-08-02T06-05-06.tar.age` (276 744 o) sur le Drive, test joué en environnement systemd simulé. **(c) ✅** nightly 5/5 sur 4 nuits (30/07 → 02/08). **RESTE : (b)** Healthchecks.io Period/Grace 5 min + test bout-en-bout (dashboard = geste PO) et **(c bis)** restauration testée DEPUIS le cloud. Énoncé d'origine ci-dessous.
- [ ] ~~**R2 — Restes ops (gestes user).**~~ (a) A1b hors-site : installer rclone, créer le remote `gdrive-creche`, poser `OFFSITE_REMOTE` dans `.env.server`(+`.enc`) — **le hors-site est INACTIF tant que ce n'est pas fait** ; (b) A4 : vérifier Period 5 min/Grace 5 min sur Healthchecks.io (défaut = 1 jour → détection en 25 h !) + test bout-en-bout (stop timer ~10 min → mail « down », start → « up ») ; (c) post-incident backup : vérifier le nightly **5/5 du 30/07**, puis — une fois (a) fait — **tester une restauration DEPUIS le cloud** (le `restore-one` n'a été rejoué que sur dump local).
- [ ] **R3 — Bascule `INTERSERVICE_AUTHZ_ENFORCE=1` (A5-restant, geste user).** Après R1 : 7 jours de logs SANS « SCOPE AURAIT REFUSÉ » (Loki : `{} |= "SCOPE AURAIT REFUSÉ"` sur les 5 svc-*) → poser la variable, recréer les conteneurs. `FOYER_AUTHZ_ENFORCE=1` est DÉJÀ actif depuis 2026-06-28 : la seule bascule restante est bien INTERSERVICE. **Jalon inter-chantiers : à exécuter AVANT le lot 5 (revenus) du plan 32** ; tout service/client créé entre-temps embarque l'assertion machine dès le premier commit (cf. « Articulation inter-plans »).

## Chantier A — Risques prod ✅ 7/7 MERGÉS 2026-07-29 (#258→#265)

Incident découvert et résolu en cours de chantier : `creche-backup.service` en échec `203/EXEC` depuis le 17/06 (bits exécutables de 9 scripts écrasés) — **0 backup prod pendant 6 semaines** ; corrigé (#258), vérifié serveur 29/07 ; la preuve opérationnelle (nightly 5/5 du 30/07) est en R2 (c).

- [x] **A1 — Backups complets + hors-site.** ✅ #258 (5/5 bases dont `notifications` — `scripts/backup-all.sh:38`, `backup-all.ps1:72` — + bits x sur 9 scripts) + #261 (`scripts/backup-offsite.sh` : tar dernier jeu → age → rclone, opt-in `OFFSITE_REMOTE`, rétention cloud 90 j ; `restore-one` rejoué sur dump prod en conteneur jetable). Restes → R2 (a) et (c).
- [x] **A2 — Vérif cosign sur TOUTES les images.** ✅ #259 : `deploy.mjs` boucle sur les 7 images (`scripts/deploy.mjs:498-535`), **actif par défaut** (opt-out explicite `DEPLOY_VERIFY_COSIGN=0`, l.89), fail-closed si `services.json` illisible. Effet prod → R1.
- [x] **A3 — Seed/perf-smoke en prod.** ✅ tranché PO 2026-07-29 : ASSUMÉ (seed idempotent « foyer de référence » + perf-smoke lecture seule), documenté par #260 (doc 24 §5 Porte 3 + skips ponctuels `DEPLOY_SKIP_SEED`/`DEPLOY_SKIP_PERF`, `.env.server.example:284-285`).
- [x] **A4 — Dead-man's-switch alerting.** ✅ #262 + **INSTALLÉ PROD 2026-07-29 19:48 CEST** (pings OK, timer 5 min, `/etc/creche-heartbeat.env` root 0600). `scripts/heartbeat.sh` (ping seulement si `/api/health` 2xx) + `scripts/systemd/creche-heartbeat.{service,timer}` + doc (`scripts/systemd/README.md` §heartbeat, `docs/exploitation/observabilite.md` §heartbeat externe). Restes → R2 (b). NB pour B3 : une readiness agrégée plus stricte fera taire le heartbeat (cf. lot B3).
- [x] **A5 — Authz : diagnostic + correctif `/moi`.** ✅ #264 : les « SCOPE AURAIT REFUSÉ » (svc-foyer, 10 j) étaient des **faux positifs à 100 %** — routes `/moi/*` sans `@FoyerScope` ⇒ assertion parent propagée sans liste `foyers`. Fix : `@FoyerScope('identite')` sur les 4 routes (`moi.controller.ts:112-205`) + réutilisation de la liste dans le contrôleur. Décision fail-open `ScopeFoyerGuard` TRANCHÉE : conservé (`docs/exploitation/authz-inter-services.md`). Déploiement → R1 ; bascule INTERSERVICE → R3.
- [x] **A6 — Durcissement conteneurs.** ✅ #263 mergée `e06bed7` — 5 volets : limites `mem_limit`/`cpus` partout (dimensionnées `docker stats` prod 29/07, ~9,5 Gio de plafonds ; + fix loki/promtail sans `restart:` — oubli 13b, `docker-compose.server.yml:119-144`), healthcheck `/api/health/live` sur les 5 `svc-*` (compose + `HEALTHCHECK` Dockerfile), caddy v2.11.4 / cloudflared 2026.7.3 épinglés par digest, nginx non-root (`nginx-unprivileged`, port 80 conservé : mapping tunnel Cloudflare dashboard → `web:80` intouché ; bind 80 non-root OK car Docker pose `ip_unprivileged_port_start=0` en conteneur — vérifié prod ; piège réel si on change le port ou qu'on sort de Docker), en-têtes sécurité `nginx.conf` (nosniff, XFO DENY, Referrer-Policy, Permissions-Policy, CSP même-origine avec `style-src 'unsafe-inline'` tant que les styles inline subsistent — cf. C4). Effet prod → R1 (compose) ; `HEALTHCHECK` image aux prochaines images construites.
- [x] **A7 — Migrations boot observables.** ✅ #265 : jauge `migrations_en_attente` + compteur `migrations_echecs_total` (`libs/nest-commons/src/lib/database/migration.service.ts:29-37`), readiness `GET /api/health` down tant que le schéma est en retard (`MigrationsHealthIndicator`, `libs/nest-commons/src/lib/health/health.controller.ts:34-40` — le contrôleur des svc-*, pas l'homonyme de la gateway, cf. B3), alerte `MigrationsEnRetardPersistant` critical `for: 10m` (`docker/prometheus/alerts.yml:381-390`). Sans risque de restart : healthchecks compose + blackbox ne sondent que `/api/health/live`. 8 tests unitaires ajoutés (nest-commons 81/81). **B3 (gateway ready avant amonts) explicitement NON traité ici.** Effet prod → R1.

## Chantier B — Encoder les pièges dans l'outillage (anti-frictions durables)

Fil rouge de l'audit dev : contrats implicites non automatisés, re-documentés à chaque plan. Objectif : que le piège devienne impossible, pas mieux documenté.

- [x] **B1 — préflight de session.** ✅ `scripts/preflight.mjs` + script racine `preflight` (~6 s, aucun accès réseau, portable Windows/Linux). **Le nom `doctor` est INUTILISABLE** : `pnpm doctor` est une sous-commande native de pnpm, elle masquerait le script sans message — vérifié sur la machine. 10 vérifications : pnpm courant vs `packageManager`, Node vs `.nvmrc`/`engines`, clone principal vs worktree (`git-dir` ≠ `git-common-dir`) + worktrees sans `node_modules`, `node_modules` postérieur au lockfile, 51 symlinks `workspace:*` (ils vivent dans le `node_modules` de CHAQUE projet, **jamais à la racine** — un doctor naïf qui teste la racine conclurait à tort), 192 shims `.bin` (cible `.pnpm/<pkg>@<version>` existante), chaîne Nx alignée (10 paquets), **ports des providers Pact (3995-3999) libres**, hooks husky. Chaque vérification a été **testée en négatif** (lien retiré, `@nx/vite` rétrogradé en 22.7.5, port 3998 squatté) : elle crie, et le dépôt sain sort 0/0. **Piège CI à retenir pour tout nouveau script `scripts/*.mjs`** : le job CodeQL est bloquant et la règle `js/file-system-race` (HIGH) frappe tout couple « `existsSync`/`lstatSync` puis `readFileSync`/`statSync` sur le MÊME chemin ». Écrire les accès en `try { lire() } catch { }` d'emblée — un `Dirent` de `readdirSync({withFileTypes:true})` évite en plus un `lstat` par entrée. **La vérification des ports Pact est née d'un vrai incident découvert pendant ce lot** : un `mock-gateway.mjs` orphelin d'une session du 28/07 tenait le port 3999 depuis QUATRE jours et faisait échouer tous les tests provider sur « provider non prêt après 40000 ms », sans jamais nommer la cause — 13 min de suite complète gâchées avant de le voir. Process terminé, suite verte ensuite. **Vérification `dist/` vs `src/` VOLONTAIREMENT ÉCARTÉE** : sur un cache hit Nx affiche « existing outputs match the cache, left as is » et ne touche pas `dist/` — une heuristique de mtime crierait après chaque `git checkout` ; B2 le garantit désormais correctement par hachage de contenu.
- [x] **B2 — Typecheck couplé aux tests.** ✅ **Découverte structurante : les `targetDefaults` de `nx.json` ne s'appliquent QU'AUX cibles inférées par les plugins.** `web` (dont `test` ET `typecheck` sont écrits à la main dans `apps/web/package.json`), `api-gateway` et les 5 `svc-*` (`test` écrit à la main) les ignoraient totalement — le `test → ^build` réputé « déjà encodé » ne couvrait donc PAS `web` : `nx test web` = **1 seule tâche**, mesuré. Livré : `targetDefaults.test = ["^build", "typecheck"]` ; `dependsOn` explicite sur les 7 cibles écrites à la main ; `web:typecheck` rendu **cacheable** (il ne l'était pas — 2 × `tsc --noEmit` rejoués à chaque CI) avec `dependentTasksOutputFiles` en entrée. **Piège évité** : un `targetDefaults.typecheck` ÉCRASE le `dependsOn` inféré par `@nx/js/typescript` (`["build","^typecheck"]` → `["^build"]`) sur TOUTES les libs — mesuré, puis retiré ; seul `web` porte un `typecheck.dependsOn` explicite. **Impact mesuré** (critère du lot) : `nx test web` passe de 1 à 4 tâches (`contracts-kernel:build`, `shared-semaine:build`, `web:typecheck`, `web:test`) ; le graphe CI (`run-many -t lint typecheck test build --all`) reste à **83 tâches, identique** — aucune tâche ajoutée, seulement des arêtes d'ordre. Boucle locale à chaud : `nx typecheck web` = 8 s (3/3 en cache). Non-régression du cache vérifiée : un renommage d'export dans `contracts-kernel` fait bien échouer `web:typecheck` (pas de cache périmé servi).
- [x] **B3 — Readiness chaîne gateway.** ✅ **FAIT 2026-08-03** (session distante, branche `claude/implementation-du-plan-a3dk80`). `AmontsHealthIndicator` (`apps/api-gateway/src/health/amonts.health.ts`) : la readiness de la gateway agrège désormais la **readiness des 5 amonts** (`/api/health` de chacun, donc base + migrations + NATS), une sonde terminus **par** amont pour que le 503 **nomme** le service fautif, toutes servies par un lot unique en cache TTL 5 s (timeout 2 s par sonde). `referentiel.health.ts` supprimé : sa sonde de liveness sur le seul svc-referentiel était le défaut.
  - **Deux décisions PO prises au lancement du lot** : (1) sonder la **readiness** des amonts et non leur liveness — seule la readiness distingue « le process répond » de « le service accepte du trafic », donc seule elle ferme la cause racine ; corollaire assumé : au déploiement, un amont au schéma en retard fait échouer la Porte 3 → rollback (c'est le comportement voulu, il vaut mieux qu'un seed lancé sur un amont non prêt) ; (2) le **heartbeat A4 bascule sur la liveness** `/api/health/live` — sur readiness agrégée, le moindre amont dégradé ferait taire le battement, produisant une alerte Healthchecks.io en doublon d'Alertmanager et surtout **indiscernable d'une panne d'hôte**, seul scénario que ce moniteur détecte quand tout le reste est muet.
  - **Le pansement du seed est remonté à sa cause** : `attendreGateway` ne sondait que `GET /couts` (« toute réponse HTTP = prêt ») — il attend maintenant la readiness de la chaîne. Le rejeu 30 s est **conservé** mais change de rôle (ceinture pour un disjoncteur gateway encore ouvert — état client, que la readiness d'un amont ne peut pas voir).
  - **Fenêtre de patience élargie** partout où la porte santé est consommée (10 → 20 essais × 3 s ≈ 60 s : `deploy.mjs`, smoke CI, doc 24) — une porte devenue plus stricte doit couvrir les migrations d'un démarrage à froid, sinon on échange un flaky de seed contre un rollback intempestif. curl réessaie bien les 503.
  - **Contrainte A6/A7 respectée** : healthchecks compose et sonde blackbox restent sur `/api/health/live`, qui ne dépend toujours de rien — aucun risque de restart en cascade. Verrouillé par test.
  - **⚠️ Geste ops (PO)** : sur le serveur, `sudoedit /etc/creche-heartbeat.env` → suffixer `HEARTBEAT_HEALTH_URL` en `/api/health/live`, puis `sudo systemctl start creche-heartbeat.service` pour vérifier. **À faire au déploiement de ce lot** : sans lui, le battement se taira au premier amont dégradé. Procédure dans `scripts/systemd/README.md` § Heartbeat.
  - 8 tests unitaires (284 verts sur la gateway), 0 warning ESLint ajouté.
- [ ] ~~**B3 — Readiness chaîne gateway (cause racine des 502/503 seed).**~~ (énoncé d'origine) La gateway répond `ready` avant ses services amont : sa readiness (`apps/api-gateway/src/health/health.controller.ts:19-25`) ne sonde QUE svc-referentiel — ni DB, ni NATS, ni les 4 autres amonts ; #265 n'y a rien changé (il a enrichi l'homonyme des svc-*, `libs/nest-commons/src/lib/health/health.controller.ts:34-40`, qui sonde DB/migrations/NATS) ; le rejeu 30 s de `seed-demo.mjs` (#255, `scripts/seed-demo.mjs:267-296`) reste un pansement. Contraintes héritées de #265 : les healthchecks compose et la sonde blackbox DOIVENT rester sur `/api/health/live` (sinon restarts en cascade). Interaction A4 à trancher AVANT d'implémenter : `heartbeat.sh` ne pingue que si `/api/health` 2xx — une readiness agrégée stricte fait taire le heartbeat et déclenche Healthchecks.io dès qu'un amont est dégradé (voulu ?). Implémentation recommandée : indicateur terminus d'amonts derrière cache TTL, sur le modèle `MigrationsHealthIndicator` (#265) — pas de sonde synchrone par requête.
- [ ] **B4 — Politique CVE images immuables.** Le mail quotidien `[VEILLE CVE]` (`.github/workflows/image-scan.yml`) escalade sur des images figées que seul un train de release corrige : définir la politique (train mensuel minimum, ou rebuild-only `OS_PATCH` déclenchable — hypothèse H3, qui borne aussi le délai d'activation des lots mergés-non-déployés). **Chaîne à cadrer explicitement** : un rebuild produit de nouveaux digests ⇒ re-signature cosign OBLIGATOIRE (vérification fail-closed 7/7 au déploiement depuis A2) ⇒ poller release (digest agrégé des 6 images) ⇒ rollback auto — la politique doit décrire signature → poller → rollback. Réviser `.trivyignore` (2 CVE datées 2026-07-28 : CVE-2026-14257 brace-expansion sans correctif amont ; GHSA-qwww-vcr4-c8h2 → retirer à la migration react-router v8).
- [ ] **B5 — E2E stack : réduire la sérialisation.** Aujourd'hui `workers: 1` (`apps/web/playwright.stack.config.ts:20-21` ; la config mockée est `fullyParallel`). Le namespacing de foyers par spec **heurte les guards de prod réels** : `@CreationFoyerUnique`, isolation foyer par e-mail parent, seed idempotent mono-foyer (`.seed-demo-state.json`) — à résoudre avant de repasser à 2+ workers. Rentabilité croissante : chaque chantier 31/32/33/factures ajoute sa spec stack — re-évaluer après leur atterrissage. Garder `retries:1` + tracker le taux flaky (`e2e-summary.mjs`) comme métrique CI avec seuil.
- [x] **B6 — Dependabot.** ✅ Labels créés — `dependencies`, `security` **et `ci`** (celui-ci aussi était référencé par `.github/dependabot.yml` et manquant) ; `gh label list` les contient (critère done). + `docs/runbook-nx-migrate.md` versionné et indexé : procédure `nx migrate` pas-à-pas, pièges réels du split 22→23 (`webpack-cli` figé en ^5.1.4 parce que `@nx/webpack` 23 générait encore `--node-env`, renommé `--config-node-env` en 6/7), règle « toute PR Dependabot touchant `nx`/`@nx/*` se ferme ». + groupe `nx-toolchain` dans `dependabot.yml` : la chaîne Nx arrive désormais en UNE PR au lieu de cinq (c'est exactement le split #92/#93/#95/#96/#97 qui avait laissé le monorepo incohérent).
- [ ] **B7 — CLAUDE.md / docs dev.** Une fois B1-B2 en place, purger des plans/docs les pièges devenus impossibles ; ne garder que ceux encore réels (pact `/pacts` prettierignore, drizzle TTY drop-vs-rename → runbook 2 passes).

## Chantier C — Consolidation UI (apps/web)

Ordonné pour que les primitives existent avant la grosse refonte. Filets : tests unitaires + suite axe e2e existants. **Garde-fous transverses à TOUT lot C** (l'audit axe a des angles morts : il ne voit ni focus, ni bordures de champs, ni `:disabled`, ni `opacity` d'ancêtre — fiche mémoire a11y-axe-angles-morts) : balayage `getComputedStyle` des contrastes avant/après, passage 375 px, `nx run-many -t typecheck test -p web`, suite axe inchangée.

- [x] **C0 — Atterrir la nav mobile non commitée.** ✅ **MERGÉ dans `main` `bb3948e` (#267) le 2026-08-01** — CI complète verte. **NON DÉPLOYÉ** : à embarquer dans le train de release R1. Contenu : barre d'onglets conservée sur les 4 pages globales + la 404 (foyer de référence = route, puis cache validé, puis 1ᵉʳ foyer autorisé) ; onglet « Plus » actif sur ces pages ; corrections de contraste (tokens `--bordure-champ`/`--erreur`, `:disabled` par variante, placeholders, carte archivée, jours FullCalendar adjacents) ; index docs 30→33 + les 4 SFD versionnées. **Vérif live 375 px SOLDÉE** ([[plan-confiance-et-quotidien]]) : 0 écart de contraste sur 7 routes (balayage `getComputedStyle`), aucun débordement horizontal, panneau « Plus » dans le viewport. Repères re-relevés (cf. en-tête). Deux défauts trouvés en revue adversariale et corrigés dans le même lot : (1) le repli sur le cache ne regardait pas `moi.loading` → pendant le round-trip `/moi`, la nav pointait un foyer non autorisé et `PastilleAValider` émettait un GET scopé dessus (403 ⇒ faux positif de l'alerte `AuthzGatewayRefus`) — corrigé + test qui échoue sans le correctif ; (2) focus clavier bleu-sur-bleu (1:1) sur les liens d'en-tête sous 768 px — **défaut PRÉEXISTANT** reproduit à 375 px, fermé par `.app-header nav > a:focus-visible`. Reste : `.claude/plans/streamed-juggling-pudding.md` (plan `factures-reelles` suivi par git sous son ancien nom auto) est supprimé en copie de travail, **non commité** — décision à prendre (les autres plans ne sont pas versionnés).
- [x] **C1 — Primitives (quick wins).** ✅ **FAIT 2026-08-01** (branche `refactor/web-consolidation-ui-c1-c4`, non poussée). 0 littéral `className="btn"` : les **100** sites (et non 96) passent par `<Bouton>`/`<BoutonLien>` — 12 d'entre eux sont des `<Link>`/`<a>`, d'où DEUX composants et non une prop `as` (un lien ne peut porter ni `type` ni `disabled`). `<ChampErreur>` (34) et `<ChampFormulaire>` (29) : les champs forment **11 patrons distincts**, pas un seul — un composant universel cassait 7 tests qui passent l'unique valeur d'`aria-describedby` à `getElementById`, d'où un rendu en Fragment (pas de `<div>` intercalé), une aide OPT-IN et la migration du seul patron canonique. Badges : **2 migrations réelles sur 13 objets recensés** (« pastille » désigne 3 familles : badge textuel, compteur positionné, swatch décoratif). Écarts assumés : `btn secondaire danger contour` → `danger-contour` (`secondaire` prouvé inerte, styles calculés identiques) et −1,2 px sur le titre du panneau coût (enveloppe inline du badge supprimée). NON traités, volontairement : les 3 messages d'erreur rendus en `<div className="muted">` sans rôle ni liaison (`CalendrierCreche` ×2, `EditeurContratSemaine`) — les corriger change la couleur ET le comportement d'annonce, c'est un lot assumé et non une refactorisation à iso-rendu.
- [x] **C2 — Découper `App.tsx`.** ✅ **FAIT 2026-08-01** (commit `62ad274`). 670 → **102 lignes** : App.tsx ne porte plus que la coquille et le routeur. Sortis à l'identique (aucun changement de DOM, de classe ni de comportement) vers `layout/Entete.tsx`, `layout/GardeFoyer.tsx` (+ ses 3 écrans de récupération), `layout/PageIntrouvable.tsx`, `layout/titreDepuisPathname.ts`, `session/Accueil.tsx` et `foyer/MesFoyersPage.tsx` (page + `SelecteurFamilles` + `libelleFamille`). **H4 TRANCHÉ PO le 2026-08-01 : AVANT le plan 31** — les SFD 31-33 sont encore des brouillons, donc rien en vol à rebaser, et les trois vagues de routes atterriront sur la structure déjà découpée. Filet : `App.test.tsx` (812 l.) teste tout au travers de `<App />` ; la suite est restée verte sans aucune modification.
- [x] **C3 — `styles.css` : tokens & organisation.** ✅ **FAIT 2026-08-01** (commit `41276ea`). **53** littéraux couleur tokenisés — il y en avait bien plus que les 6 annoncés — : plus aucune couleur en dur hors `:root`. 22 tokens nommés par RÔLE + 3 de gabarit (`--largeur-page/-colonne/-modale` ; `600px` était écrit deux fois à l'identique). **Distinctions à ne JAMAIS refusionner, documentées dans le fichier** : `--surface` et `--texte-inverse` (même `#fff`, rôles opposés) ; `--gris-sur-blanc` `#6b7280` ≠ `--gris` `#4b5563` (ce dernier assombri exprès pour tenir 4,5:1 sur `--gris-clair`) ; `--rouge-fond-survol` `#fef2f2` ≠ `--rouge-fond` `#fee2e2` ; `--ambre-texte` ≠ `--ambre`. Les `--esp-*` et `.table-defilante` que les SFD 31/33 doivent consommer existaient déjà. **Media queries : 6 blocs `min-width: 768px` → 3.** Les 2 restants NE SONT PAS fusionnables et le disent désormais : `.liste-couts-mobile` et les règles de base `.fc` sont déclarées ENTRE le bloc et sa cible, à spécificité égale. **Les 3 `max-width` sont STATUÉS** : `.table-couts-desktop` converti en mobile-first (équivalent strict) ; la barre d'onglets mobile CONSERVÉE (sa protection n'est pas la spécificité mais la media query — six règles casseraient, dont une régression FONCTIONNELLE : le panneau « Plus » réapparaîtrait en feuille fixe sur desktop) ; `.actions-ligne` CONSERVÉE (la réécriture différerait dans la bande ]479;480[, seules bornes non contiguës du fichier). **Règle MORTE supprimée** : `.actions-export { width: auto }` du bloc 768px était écrasée par la règle de base déclarée plus bas à spécificité égale — le rendu réel était `100%` partout, l'inverse de ce qu'annonçait le commentaire. Preuve : empreinte des styles calculés **strictement identique** sur 4 586 nœuds.
- [x] **C4 — Styles inline.** ✅ **FAIT 2026-08-01** (commit `15b8da5`). 305 → **193** `style={{` (112 supprimés, au-delà des ~100 visés). Vocabulaire VOLONTAIREMENT fermé, chaque entrée supprimant au moins huit occurrences identiques : une règle de base `input[type=checkbox], input[type=radio] { width:auto; padding:0 }` (19 à elle seule), `.champ-large` (32), `.bloc-champs` + sa légende (11), `.liste-nue` (5), `.rangee-actions` (1), et une échelle de marges VERTICALES calée sur `--esp-*` — `.mt-0`…`.mt-5`, `.mb-1`…`.mb-5` (70 usages), bornée : ni left/right, ni pas intermédiaire, ni variante responsive (une marge de `0.4rem` reste inline plutôt que d'être arrondie). Placement de la règle cases/radios choisi AVANT les règles de formulaire : son `padding` gagne partout par spécificité, son `width` continue de céder à `.profil-form input` — soit exactement le rendu actuel. **Restes identifiés** : table de `CoutsAnnuelsPage` (15, règles descendantes sur mesure sous `.table-couts-wrap`) ; `.bloc-champs > legend` à rendre opt-in (débloquerait 2 fieldsets de `CalendrierCreche`) ; `.rangee-actions` sans `flex-wrap` (débloquerait 4 pieds de modale). La CSP `style-src 'unsafe-inline'` ne pourra être resserrée qu'à zéro inline — objectif long terme, hors de ce lot. Preuve : 4 440 nœuds identiques + **146 nœuds reclassés aux styles calculés identiques**.
- [x] **C5 — Factoriser les deux calendriers.** ✅ **FAIT 2026-08-03** (session distante, branche `claude/implementation-du-plan-0isdve`). `CalendrierCreche` **1307 → 517 l.**, `CalendrierAbcm` **942 → 547 l.**, `EditeurContratSemaine` **942 → 415 l.** → **plus aucun composant web > 700 l.** (mesure de succès atteinte ; le plus gros est `DashboardJourPage`, 670).
  - 🔴 **ÉCART D'ÉNONCÉ, tranché par la reconnaissance : les « mêmes briques » étaient DÉJÀ partagées.** `CalendrierMois`, `LegendePlanning`, `ChoixPortee`, `BarreStatutCalendrier`, `ModaleContratDurable` sont des composants communs depuis longtemps, et `useCalendrierContrat` (266 l.) factorisait déjà TOUTE l'enveloppe comportementale (écriture debouncée, statut, réhydratation + anti-clobber, annonces AQ-05, portée, PUT durable). Ce qui restait dupliqué était **l'assemblage et la présentation**, pas la logique. Un socle `CalendrierPlanning` + objets de « stratégie » aurait donc dû faire transiter l'état, la dérivation des évènements, les modales, les listes et les payloads — c'est-à-dire tout : l'abstraction n'aurait rien partagé de plus et aurait rendu les deux modes illisibles. **Décision : extraire l'assemblage réellement commun, laisser les règles de mode dans leur mode.**
  - **Briques communes nées du lot** : `SocleCalendrier` (coquille : région live + barre de statut + légende + consigne + vue mensuelle + confirmation durable, consommant directement le résultat de `useCalendrierContrat`) ; `ListeJoursClavier` (les **3** listes clavier avaient le même balisage) ; `PiedModaleCalendrier` (les 3 pieds Confirmer / action intermédiaire / Annuler) ; `evenementJour` (fabrique de pastille — l'`id` reste un PARAMÈTRE : crèche et ABCM ne l'attribuent pas pareil, et deux pastilles peuvent coexister sur une même date côté crèche).
  - **Découpe** : crèche → `useSaisieCreche` (état + persistance + réhydratation + envoi, qui ENVELOPPE `useCalendrierContrat` car réinitialisation et écriture se referment l'une sur l'autre), `etatsJoursCreche` (pur), `SaisieLotAbsences`, `ModaleJourCreche`. ABCM → `inscriptionsAbcm` (pur, contexte unique `{semaine, exceptions, joursAlsh}` : les 6 dérivations s'appellent entre elles, les passer une par une multipliait les `useCallback`), `evenementsAbcm` (pur), `ModaleAjustementAbcm`, `ModaleJourneeAlsh`. `EditeurContratSemaine` → `ajustementDeduit` (pur), `indexBesoins` (pur), `champsJourSemaine`, `ModaleJourSemaine`.
  - **Trouvaille** : `libelleAlsh` existait déjà dans `notifications/besoinsSemaine.ts` et était **réimplémenté deux fois** dans `CalendrierAbcm`.
  - **NON fait, volontairement** : (a) unifier les ~9 libellés de case à cocher des deux calendriers — leurs styles inline diffèrent sur 5 axes (gap, margin, `flexDirection`, `fontSize`), un composant commun aurait exigé autant de props de style : c'est un gabarit paramétré, pas une abstraction, et C4 avait déjà fermé ce vocabulaire à dessein ; (b) unifier les blocs radios « type d'absence » modale ↔ lot (5 différences de style sur ~12 lignes de balisage) ; (c) faire passer le pied de `ModaleJourSemaine` par `PiedModaleCalendrier` (il porte `className="mt-4"` là où les calendriers portent une marge inline → classes rendues différentes pour un gain nul).
  - **Warns embarqués** : `exhaustive-deps` **11 → 3** (les 10 des calendriers soldées : déstructuration des fonctions de persistance — stables, contrairement à l'objet porteur dont l'identité change quand `indisponible` bascule — et mémoïsation de `semaineType`) ; `no-unnecessary-condition` **8 → 2** (les 6 occurrences web soldées). ⚠️ **L'énoncé de D3 se trompait sur leur localisation** : elles n'étaient PAS dans les calendriers que C5 rouvre mais dans `EtablissementsPage`, `ContratForm` (×3), `editeursSemaine` et `ui/Modale` — gardes rendues mortes par le narrowing d'un `const` booléen, colonne cantine toujours affichée, `?.` superflu. Elles ne venaient donc pas « gratuitement » avec C5.
  - 🔴 **ANGLE MORT CORRIGÉ, de même nature que celui de D2** : les règles `react-hooks` ne s'appliquaient qu'aux `**/*.tsx`, or **les hooks du dépôt n'ont pas de JSX et vivent en `.ts`** — `useAsync`, `useFoyer` et `useSaisieServeur` portaient **6 diagnostics jamais comptés**. Ce n'était pas le code qui était propre, c'est le linter qui ne regardait pas. **Corollaire piégeux** : déplacer un effet d'un composant `.tsx` vers un hook `.ts` faisait DISPARAÎTRE ses warnings du compteur sans rien corriger — exactement ce que ce lot allait faire. Glob élargi à `apps/web/**/*.ts`, en bloc SÉPARÉ des règles `react/jsx-*` (non définies hors JSX → « Definition for rule not found »). +5 warnings désormais visibles. Baseline **658 → 649**.
  - **Preuve d'iso-rendu** : `nx run web:e2e-visuel` + `comparer-empreinte.mjs` → **4 586 nœuds, 0 écart de style, 0 reclassé, 0 apparu, 0 disparu**, 0 contraste, 0 débordement 375 px. 760 tests verts, **aucun fichier de test modifié**, aucune route ni pact touchés.
- [ ] ~~**C5 — Factoriser les deux calendriers.**~~ (énoncé d'origine) `planning/CalendrierCreche.tsx` (1325 l.) + `planning/CalendrierAbcm.tsx` (962 l.) = mêmes briques (FullCalendar, CalendrierMois, LegendePlanning, ChoixPortee, BarreStatutCalendrier, modales). Extraire un socle `CalendrierPlanning` + stratégies crèche/ABCM. **Le plus gros lot** — APRÈS C1/C4 ; en profiter pour lever les 5 warns `react-hooks` et découper `notifications/EditeurContratSemaine.tsx` (963 l. — il vit dans `notifications/`, pas `planning/`). **Séquencement tranché : C5 s'exécute AVANT le lot 4 du plan 31** (qui modifie exactement ces fichiers — une seule refonte), sinon y renoncer explicitement jusqu'après le 31 ; l'entre-deux est le pire. Done : garde-fous transverses + **aucune route ni pact modifiés, suite axe inchangée**. Filet mince à compenser : 2 specs stack seulement (`planning-*.stack.e2e.spec.ts`), aucune non-régression visuelle outillée → balayage manuel des 2 calendriers (mois avec absences/fériés/ajustements) avant merge.
- [ ] **C6 (option) — Message/feedback unifié.** Composant `<Message>` pour les ~90 `role="status"|"alert"` posés à la main (91 relevés / 37 fichiers, tests inclus) + variantes locales (`bandeau-test`, `jour-message-succes`, `texte-erreur`). Prépare aussi un éventuel dark mode (aucun `prefers-color-scheme` aujourd'hui). **Hypothèse H2 : non demandé, ne pas lancer sans besoin exprimé.**

## Chantier D — Qualité code (backend & transverse)

- [x] **D1 — Client HTTP partagé.** ✅ **FAIT 2026-08-02** (branche `claude/plan-implementation-71rkuk`). **Maison cible tranchée : étendre `libs/resilience`** (`src/lib/appel-http.ts`) et non descendre dans `nest-commons` — elle porte déjà `fetchAvecTimeout`/`executerResilient`/`executerOuRepli`, elle est `context:shared` donc atteignable par la gateway ET les `svc-*` (`context:gateway` ne peut dépendre que de `context:gateway`+`context:shared`), et elle n'a aucun couplage sécurité. Trois enveloppes : `appelHttpResilient` (propagation, BFF), `appelHttpOuRepli` (dégradation propre, replis synchrones), `executerAppelHttp` (one-shot **sans** retry ni disjoncteur, pour `FoyerClient.desabonner` dont le jeton est brûlé au premier succès). **Les deux mécanismes de sécurité restent DISTINCTS** : la lib ne connaît qu'un `FournisseurEntetes` injecté (`entetesAval` côté gateway via `clients/appel-resilient.ts` devenu une simple liaison ; `entetesAssertionMachine` côté services, un closure privé par client). 10 fichiers migrés, `fetch` brut résorbé. **Done vérifié** : en-têtes sortants identiques (test golden dédié `appel-http.spec.ts`, seuil 100 % de la lib tenu), pacts régénérés depuis zéro et **strictement identiques**, 834 tests verts sur les 5 projets touchés, lint 0 erreur, `nx format:check` propre. **Effet de bord assumé et souhaité** : le `fetch` brut de `svc-planification` (`joursNonFacturables`) était le seul appel NON borné du monorepo — il pouvait retenir une lecture de prestations aussi longtemps que le Référentiel restait muet ; il hérite du timeout + retry + disjoncteur (dégradation en liste vide inchangée). **Piège rencontré** : `svc-planification` ne dépendait pas de `@creche-planner/resilience` → `workspace:*` à ajouter dans son `package.json` (les tsconfig n'ont pas de `references`, la résolution passe par les liens pnpm). **Second piège** : déplacer du code COUVERT vers une lib fait BAISSER le ratio de couverture du projet source — `svc-tarification` est passé sous son seuil de 80 % de branches (79,81 %) ; compensé par `cout.controller.spec.ts` (gardes de paramètres, dont le mois borné 01-12 d'AQ-04) → 85,32 %. À l'inverse la couverture lignes de l'api-gateway MONTE de 72,75 % à 78,80 % (le boilerplate supprimé était majoritairement non couvert). **Restent hors lot, volontairement** : les 2 `fetch` bruts de sonde de santé (`api-gateway/src/referentiel/referentiel.controller.ts`, `health/referentiel.health.ts`) — ce ne sont pas des clients REST et le second relève du lot B3.
- [x] **D2 — Couverture des libs d'infra.** ✅ **FAIT 2026-08-02.** Le défaut réel était pire que « pas de seuil » : `nest-commons` avait la couverture **désactivée** (`enabled: false`) et `observability` n'avait ni test ni cible `test` — donc **aucune des deux ne produisait de `coverage-summary.json`**, donc toutes deux étaient invisibles au ratchet CI d'AQ-06 (`coverage-compare.mjs`), pas seulement à un seuil local. **`observability`** : cible de test créée sur le patron de `resilience` (vitest.config.mts, tsconfig.spec.json, specs exclues de `tsconfig.lib.json`, `ignoredFiles` vitest dans son `eslint.config.mjs`) ; 11 tests, **100 %** sur les deux modules → seuil posé à 100 d'emblée (`buildLoggerParams` : défauts, `LOG_LEVEL`, pino-pretty sur `LOG_PRETTY=true` SEULEMENT, priorité `x-request-id` > `traceparent` > vide ; `startTracing` SDK mocké : garde d'idempotence + arrêt propre SIGTERM/SIGINT). **`nest-commons`** : 76,66/76,68/62,37/76,79 → **83,33/79,82/75,24/83,54**, seuils posés au mesuré arrondi vers le bas (83/79/75/83) ; trois fichiers à 0 % couverts — `trace.ts` (dont le cas piège du `traceId` tout-à-zéro d'un span non enregistré, qui donnerait un identifiant de corrélation partagé par TOUS les événements hors requête), `domain-exception.filter.ts` (contrat `DomainError` → 400 que le front lit), et les sondes de santé (dont la propriété non négociable héritée d'A7 : `/api/health/live` ne sonde AUCUNE dépendance). Reste non couvert, **assumé et documenté dans la config** : les `*.module.ts` (câblage DI sans logique), les fabriques d'options, `nats.service.ts` (connexion réelle → intégration).
- [x] **D3 — Relancer le ratchet ESLint.** ✅ **FAIT 2026-08-02. 10 règles passées en `error`, 684 → 658 warnings, 0 erreur.** ⚠️ **L'énoncé d'origine se trompait sur `require-await`** : elle n'est pas « quasi propre » mais à **196 occurrences** (presque toutes des `async () => {}` de specs) — candidate n°1 du prochain lot, pas de celui-ci. **Méthode retenue, à réutiliser : une règle ne passe en `error` que lorsque le dépôt est à ZÉRO occurrence** (un `error` avec de la dette restante bloque la CI ou fait fleurir les `eslint-disable`) ; c'est le compteur qui interdit au reste de remonter. Six règles l'étaient **déjà** (`no-unsafe-member-access`, `no-unsafe-call`, `no-unsafe-return`, `no-explicit-any`, `restrict-plus-operands`, `no-unnecessary-type-conversion`) — le code était propre, seul le ratchet ne l'avait jamais acté ; quatre soldées par le lot : `no-dynamic-delete` (7 → omission par déstructuration `const { [cle]: _retire, ...reste }` ; `Reflect.deleteProperty(process.env, c)` côté spec d'env), `prefer-optional-chain` (3), `no-unused-expressions` (1), `no-unnecessary-type-parameters` (3, disable de bloc justifié : l'astuce d'égalité stricte de types d'`openapi-types.spec` EST son paramètre générique). `no-unnecessary-condition` **15 → 8 sans promotion** : les 8 occurrences de specs venaient de casts `as` qui MENTAIENT (un `| undefined` masqué sur `mock.calls[0]`), corrigés à la source ; **les 7 restantes sont dans les composants web et portent du comportement** (props optimistes, branche morte) → à traiter **avec C5**, qui rouvre ces fichiers. `no-unused-vars` codifie la convention `^_` déjà employée (`_url`/`_init`/`_strength`) : 7 → 2. **Compteur CI livré** : `.github/workflows/scripts/lint-warnings.mjs` + `.github/workflows/lint-baseline.json` **versionnée** (et non un artefact roulant comme la couverture : la faire monter exige un diff visible en revue), step bloquant dans le job `ci`. Lint du dépôt **ENTIER**, volontairement hors `affected` — un total ne se compare que sur un périmètre constant. Tableau par règle dans `GITHUB_STEP_SUMMARY signalant les règles **tombées à zéro**, donc promouvables. **Testé en négatif** : un `async`sans`await`ajouté fait bien sortir 1 (659 > 658). Effet de bord requis :`out-tsc`rejoint les`ignores`de la config ESLint **racine** (chaque config de projet les ignorait déjà) — sans quoi un`eslint .`à la racine sort ~280 erreurs de parsing sur les déclarations émises par`tsc --build`.
- [ ] **D4 — Modes & barèmes : résiduel + convention transversale.** **DV-01→04 sont LIVRÉS par la SFD 30 (lots 2/3/7)** : modes centralisés dans `libs/contracts/kernel/src/lib/modes.ts:11-27` (`MODES_CONTRAT`/`ModeContrat`, `MODES_ABCM`) ; grilles, barème PSU et seuils servis par la projection Référentiel (RM-30-04) ; les constantes `GRILLE_ABCM_2026`/`BAREME_EFFORT_PSU_2026` **n'existent plus** (seules des fixtures de specs non exportées subsistent : `grille-abcm.fixtures.ts`, `bareme-effort-psu.fixtures.ts`). Résiduel réel : (1) le miroir volontaire `libs/referentiel/domain/src/lib/mode-garde.ts:4-13` (frontière Nx `domain` ↛ `contracts`) — **l'ériger en convention transversale « miroir local documenté »** (patron referentiel-domain, #239), car trois chantiers en auront besoin : `calendrier-ouverture.ts` (plan 31), `famille-domain` (plan 32), `facturation-domain` (factures) ; (2) règle associée : **tout nouveau contexte Nx (`famille`, `facturation`) doit être enregistré dans les depConstraints d'`eslint.config.mjs`** — sans enregistrement, un contexte n'est PAS contraint (dérive silencieuse) ; l'enregistrement s'exécute dans les lots 1 respectifs de ces plans, la convention est portée ICI ; (3) seuils RFR versionnés = périmètre SFD 32, pas ce lot. Calé par H1 (validation SFD 31-33).
- [ ] **D5 — God services.** Découper `planification.service.ts` (1425 l.) et `foyer.service.ts` (1147 l.) en cas d'usage. Opportuniste : quand un lot fonctionnel touche ces fichiers, pas en big-bang.
- [ ] **D6 — OpenAPI.** Le document manuel vit dans **`libs/contracts/kernel/src/lib/openapi/gateway.openapi.ts`** (2002 l. — l'app gateway n'a qu'un `openapi.controller.ts` qui le publie), gardé par l'oracle « expose exactement les 27 routes attendues » (`gateway.openapi.spec.ts:14`) et la chaîne `scripts/generate-openapi-types.mjs` → `apps/web/src/api/openapi-types.gen.ts` (job `openapi-types-drift`). Étendre la garde ou générer la spec = toucher ces trois maillons ENSEMBLE. Effort élevé, gain moyen — dernier.

---

## Articulation inter-plans (SFD 31/32/33, factures-reelles)

Ordre validé inter-audits : **consolidation (ce plan) → SFD 31 → SFD 32 → SFD 33 → factures-reelles**, exécution **SÉQUENTIELLE** — chaque chantier touche les mêmes surfaces partagées à fort taux de conflit (`gateway.openapi.ts` + oracle « exactement 27 routes » + `openapi-types.gen.ts` + pacts + `services.json` + `TYPES_NOTIFICATION`) : deux chantiers parallèles = conflits garantis.

Ce plan porte les préalables des autres (référencés une seule fois ici) :

- **R1** (train n°16) : préalable universel — les plans 31/32 comparent des coûts qui exigent la projection #257 rejouée ; R1 démarre aussi l'horloge de R3.
- **R3** (bascule INTERSERVICE) : AVANT le lot 5 (revenus) du plan 32.
- **C0** : avant tout lot web de n'importe quel plan (mêmes fichiers).
- **B2** : avant le plan 31 (supprime le piège builds-de-libs recopié dans les pièges des plans 31 et 33).
- **C5** : avant le lot 4 du plan 31 ; **C2** : avant le 31 ou après le 33 (H4).
- **Convention D4** (miroir local de modes + enregistrement des contextes Nx) : consommée par les lots 1 des plans 31/32/factures.
- **Checklist « route BFF »** (tous plans, chaque lot exposant une route) : `gateway.openapi.ts` + oracle 27→N + `nx run web:generate-types` + pacts verts.
- **Sécurité** : R3 arrivera PENDANT ces chantiers → tout nouveau service (svc-famille, svc-facturation) et tout nouveau client inter-services embarque `AssertionIdentiteModule` + `entetesAssertionMachine` + propagation des foyers autorisés (#264) dès le premier commit — un service livré observe-only serait cassé net à la bascule.

## Ordre recommandé

1. **R1 + R2** (train n°16 + ops user) — débloque la prod, démarre l'horloge R3, préalable de tout le reste. **Gestes utilisateur (accès prod/SSH/Healthchecks.io) : à déclencher par le PO.**
2. ~~**C0**~~ ✅ fait (`cbd3d74`, à pousser + PR).
3. ~~**B2, B1, B6**~~ ✅ **MERGÉS dans `main` `1d47636` (#268) le 2026-08-01**.
4. ~~**C1-C4**~~ ✅ **MERGÉS dans `main` `9171228` (#272) le 2026-08-01** (squash, CI 15/15). Contenu : 4 lots + retrait de `streamed-juggling-pudding.md` + balayage de non-régression visuelle versionné (`nx run web:e2e-visuel`, hors CI par défaut). **NON DÉPLOYÉS → le plancher de R1 monte de `bb3948e` à `9171228`.**
5. ~~**D1-D3**~~ ✅ **FAITS 2026-08-02** (session distante, branche `claude/plan-implementation-71rkuk`) : client HTTP partagé dans `libs/resilience`, couverture des 2 libs d'infra mesurée + plancher-née, ratchet ESLint relancé (10 règles en `error`, compteur CI versionné). **NON DÉPLOYÉS** → plancher de la prochaine release.
6. ~~**C5**~~ ✅ **FAIT 2026-08-03** (session distante, branche `claude/implementation-du-plan-0isdve`) : socle d'assemblage + listes clavier + pied de modale partagés, découpe des 3 gros composants, `exhaustive-deps` 11 → 3, `no-unnecessary-condition` 8 → 2, ratchet react-hooks élargi aux hooks `.ts`. **NON DÉPLOYÉ** → plancher de la prochaine release, avec D1-D3.
7. **R3** dès 7 jours de logs propres post-R1 (indépendant des étapes 2-6 ; avant le lot 5 du plan 32).
8. ~~**B3**~~ ✅ **FAIT 2026-08-03** (session distante, branche `claude/implementation-du-plan-a3dk80`) : readiness gateway agrégée sur la readiness des 5 amonts, heartbeat A4 basculé sur la liveness, `attendreGateway` du seed remonté à la cause. **NON DÉPLOYÉ** → plancher de la prochaine release, avec D1-D3 et C5. **⚠️ porte un geste ops** (`HEARTBEAT_HEALTH_URL` → `/api/health/live`) **à jouer au déploiement**.
9. **D4** (convention consommée par 31/32/factures), **B4-B5-B7**, **D5-D6** au fil de l'eau.

## Mesures de succès

- Prod : train n°16 déployé — cosign 7/7 au journal, `grille_tarifaire` 3 modes servis par le read-model (fin du repli REST massif), `migrations_en_attente=0`, en-têtes sécurité présents.
- Backups : 5/5 bases (✅ #258) + hors-site ACTIF (`OFFSITE_REMOTE` posé) + 1 restore testé DEPUIS le cloud.
- Authz : `INTERSERVICE_AUTHZ_ENFORCE=1` actif sans refus légitime (`FOYER_AUTHZ_ENFORCE=1` l'est déjà depuis 2026-06-28).
- CI : taux flaky e2e suivi et < 5 % ; 0 job rouge « pour cause externe » (CVE images figées) pendant 1 mois.
- Web : plus aucun `className="btn"` littéral ni bloc d'erreur formulaire manuel ; aucun composant > 700 lignes ; styles inline < 100 ; aucun écart de contraste introduit (balayage `getComputedStyle`) ; parcours 375 px intacts.
- Lint : ✅ **ATTEINT au lot D3**, resserré en C5 — baseline CI versionnée et bloquante (`lint-baseline.json`, **649**), **10 règles** en `error` (cible : ≥ 5) ; ratchet react-hooks désormais appliqué AUSSI aux hooks `.ts` (angle mort C5).
- Composants : ✅ **ATTEINT au lot C5** — plus aucun composant web > 700 lignes (max : `DashboardJourPage`, 670). `api/client.ts` (854) et `types/bff.ts` (643) ne sont pas des composants (D6 / hors périmètre).
- Plans : plus aucune section « pièges » recopiant worktree/symlinks/typecheck (encodés dans l'outillage).

## Hypothèses à valider PO

- **H1 — Validation des SFD 31-33.** Brouillons `docs/31-33` non validés : leur validation cale D4 (miroirs de modes des nouveaux contextes) et le séquencement C5/C2. Ce plan n'attend PAS cette validation pour R1/R2/C0/B1-B2-B6/C1-C4/D1-D3.
- **H2 — C6.** Message unifié / préparation dark mode : confirmer qu'il reste non lancé sans besoin exprimé.
- **H3 — Cadence de release (B4).** Train mensuel minimum vs rebuild `OS_PATCH` déclenchable : cette décision borne aussi le délai d'activation des futurs lots mergés-non-déployés (le cas chantier A/#257 — 6 lots dormants dans `main` — vient d'en montrer le coût).
- **H4 — C2 avant ou après le plan 33.** Trancher au lancement du chantier C : avant les trois vagues de routes (31 lot 2, 32 D8, 33 D7) ou après la dernière — jamais entre deux.

## Pièges connus (hérités des audits)

- Les repères chiffrés sont à jour au 2026-07-31 (post-C0, cf. en-tête) — les re-relever après C1/C3/C4, qui les font bouger par construction.
- `prettier --check` échoue sur **tous** les fichiers du dépôt sous Windows (`core.autocrlf=true` ⇒ CRLF sur disque, `endOfLine: lf` côté prettier) : la seule mesure probante est `nx format:check --base=origin/main --head=HEAD`, ou une comparaison EOL-normalisée. Ne pas conclure « le lot casse le format » sur un `prettier --check` brut.
- Le balayage de contraste `getComputedStyle` doit neutraliser les **transitions CSS** : le pane du navigateur intégré ne composite pas de frames, une propriété en transition (ex. `.skip-link { transition: top }`) reste figée sur sa valeur de départ et fait croire à un défaut. Injecter `transition: none !important` avant de mesurer.
- 🔧 **OUTIL DE NON-RÉGRESSION VISUELLE (né en C1-C4, VERSIONNÉ par la PR #272)** : `nx run web:e2e-visuel` (`apps/web/e2e/contrastes.visuel.e2e.spec.ts` + `playwright.visuel.config.ts`) puis `node scripts/comparer-empreinte.mjs avant.json apres.json`. Il fournit ce que le lot C5 déclare manquant (« aucune non-régression visuelle outillée ») : une **empreinte des styles CALCULÉS** — 37 propriétés × ~4 590 nœuds × 13 routes × 2 viewports — comparée avant/après ; c'est elle qui a prouvé C3 et C4 à iso-rendu. Embarque une **sonde négative** (3 défauts injectés, dont l'opacité d'ancêtre et la bordure de champ) et une **garde anti-balayage-à-vide**. **HORS CI par défaut** (`nx e2e web` ne le ramasse pas) : c'est un outil de revue, pas une porte. **À utiliser systématiquement pour C5.**
- 🔴 **Piège appris en C1 : un balayage qui ne mesure rien rend « 0 constat », indiscernable d'un succès.** Avec `waitUntil: 'domcontentloaded'` + délai fixe, `#root` restait VIDE sur les 26 pages. Cause seconde : un mock `/notifications/a-valider` renvoyant `{semaines: []}` au lieu d'un tableau faisait tomber tout l'arbre React (aucun error boundary). Toute mesure de page doit attendre le MONTAGE réel et refuser de conclure si elle n'a rien mesuré.
- ⚠️ L'empreinte est mesurée à l'ÉCRAN : elle ne couvre PAS `@media print`. Une migration `.carte`/`.mb-5` a été écartée pour ce seul motif en C4.
- Toute refonte UI : suite axe + garde-fous C (contrastes `getComputedStyle`, 375 px). ~~`nx run-many -t typecheck test -p web` (pas `nx test web` seul) ; builder `contracts-kernel`/`shared-semaine` avant~~ — **plus nécessaire depuis B2** : `nx test web` tire son typecheck et les builds de libs. À purger des plans 31/33 (lot B7).
- `nx run-many -t test --all` avec `--parallel` : les 5 tests provider Pact ouvrent chacun un port fixe (3995-3999). Deux modes d'échec distincts, même message « provider non prêt après 40000 ms » : (a) un **process fantôme** squatte le port — `pnpm preflight` le détecte désormais ; (b) **saturation machine** — sous `--parallel=3` le Nest éphémère ne boote pas en 40 s (`import: 231 s` mesuré). En local, préférer `--parallel=1` pour un verdict fiable. Le deflake #266 (`b7739ee`) ne couvre QUE les pacts CONSUMER de la gateway, pas ces providers.
- Toujours `git fetch` avant de brancher : `origin/main` local était périmé de deux jours (#266 mergée le 30/07 invisible), d'où deux branches créées sur une base morte.
- Ne jamais conclure d'un `commande | tail` que tout est vert : le code de sortie est celui de `tail`. Une suite complète a été lue comme « exit 0 » alors que 7 cibles échouaient.
- `pact-drift` : ne jamais retirer `/pacts` de `.prettierignore` (l.13).
- Rejeu projection #257 : un simple restart ne suffit PAS (`version_payload` déjà à 2 en prod) — procédure dans R1 et [[chantier-versionnement-dates-effet]].
- `gateway.openapi.spec.ts` : oracle « exactement 27 routes » — tout ajout de route = spec + oracle + `nx run web:generate-types` + pacts (checklist « route BFF » ci-dessus).
- Migration drizzle ambiguë (drop-vs-rename) : scinder en 2 passes, pas de TTY en CI.
